# P1.1 — تقريرٌ نهائيّ

**الحالة: `DONE` · commit `370777d` · `make gate` = PASS 15 · FAIL 0 · BLOCKED 1**
**P1.2 لم تبدأ.**

> قاعدةُ هذه الوثيقة كقاعدةِ ما قبلها: لا PASS لشيءٍ لم يُشغَّل، والأرقامُ
> من تشغيلٍ فعليّ لا من تقدير، وما لم يُقَس يُقال إنه لم يُقَس.

---

## ١ · ما طُلب وما نُفِّذ

| المطلوب | الحال |
|---|---|
| تفكيكٌ داخليّ بلا تغييرِ سلوك | ✔ مُثبَتٌ لا مدَّعًى — §٤ |
| طبقةُ إذنٍ مركزية `can(subject, action, resource)` | ✔ `falah/authz.py` |
| لا ارتباطَ بأسماء المسارات | ✔ فُحص آليًّا — §٣ |
| منعُ IDOR | ✔ ٨٣ بندًا جديدًا — §٧ |
| عقدُ API غيرُ متغيّر | ✔ مطابقٌ لبصمته — §٥ |
| كلُّ اختبارات P0 باقية | ✔ صفرُ حذف — §٦ |
| قياسُ الأداء قبل وبعد | ✔ §٨ |
| تحديثُ وثيقة المعمارية | ✔ `P1_ARCHITECTURE.md` |
| commit مستقلّ ثم توقّف | ✔ `370777d` |

**ولم يُنفَّذ شيءٌ من P1.2**، ولا ميزةٌ جديدة، ولا تغييرُ واجهة، ولا
تفكيكٌ خارج ما طُلب.

---

## ٢ · ما تغيَّر بنيويًّا

| الملفّ | قبل | بعد |
|---|---:|---:|
| `api.py` | ٦٣٥ سطرًا · `do_GET` = **٣٠٨** | ٨٢ سطرًا · `do_GET` = **١٥** |
| `app.py` | ٧٥٨ سطرًا · `app_post` = **١٩٨** | ٤٤٠ سطرًا · `app_post` **اختفت** |

بديلُ `app_post` هو `app_request` = ٥٢ سطرًا، وليست معالِجًا بل **دورةَ
حياةِ الطلب**: تبني السياق، وتنادي الموزِّع، وتترجم الأخطاء. وكانت هذه
الترجمةُ مكرَّرةً مرّتين بكتلتَي `except` متشابهتين لا متطابقتين — فصارت
واحدة.

### الوحداتُ الجديدة

| الوحدة | أسطر | مسؤوليتُها |
|---|---:|---|
| `falah/authz.py` | ٣٢٠ | القرار: `can` · `POLICY` · تحميلُ المالك |
| `falah/routing.py` | ٢٥١ | جدولُ المسارات · دورةُ التنفيذ · ترجمةُ المنع |
| `falah/app_routes.py` | ٣٢٧ | معالِجٌ لكلِّ مسار `/app` — **بلا فحصِ إذنٍ فيه** |
| `falah/content_routes.py` | ٣٧٢ | جدولُ القراءة العامّة ومعالِجاتُه |
| `falah/cards.py` | ٢٩١ | بناءُ البطاقة — نزلت من `api.py` |
| `falah/web.py` | ٦٩ | `Request` و`Response` — بلا منطق |
| `falah/ratelimit.py` | ٤٧ | جدولُ الحدود وفحصُها |
| `falah/settings.py` | ٣٤ | قراءةُ البيئة في موضعٍ واحد |
| `authz_test.py` | ٣١٤ | ٨٣ بندًا |
| `bench.py` | ٢٨٩ | القياسُ نفسُه — قبل وبعد بالأمر ذاته |

**وليس هذا نقلَ ١٩٨ سطرًا إلى ملفٍّ آخر.** الفرقُ أن **الاختيار** انفصل
عن **العمل**: `ROUTES` جدولٌ يُقرأ في نظرة، وكلُّ معالِجٍ دالّةٌ تُنادى في
اختبارٍ بلا خادم — وهو ما فعله §٨ فعلًا لقياس الاستعلامات.

---

## ٣ · طبقةُ الإذن

```
can(subject, action, resource) → Decision(allowed, reason)
```

- **`subject`**: `user_id` + أدوار. يُبنى من جلسةٍ مُتحقَّقٍ منها، **لا من
  حمولة طلبٍ أبدًا**.
- **`action`**: قائمةٌ مغلقة (٩ أفعال). فعلٌ خارجها يرفع `ValueError` —
  خطأُ برمجةٍ يُرى، لا منعٌ صامت.
- **`resource`**: نوعٌ ورقمٌ و**مالكٌ مقروءٌ من القاعدة**.

### الخصالُ الثلاث التي تمنع IDOR

1. **المالكُ يُقرأ لا يُصدَّق.** `authz.load` يُصدر
   `SELECT user_id … WHERE id=?` ثم يُقارَن الناتجُ بصاحب الجلسة. لا يُؤخذ
   رقمٌ من الطلب على أنه له.
2. **«ليس لك» = «غيرُ موجود»** — حرفًا بحرف. مُثبَتٌ باختبارٍ يقارن
   الجسمين نصًّا.
3. **المنعُ أصل.** `POLICY` جدولٌ مغلق؛ زوجٌ (مورِد، فعل) ليس فيه يُمنع
   بسبب `no_policy`، ومسارٌ جديدٌ بلا سياسةٍ يُسقط `authz_test.py`.

### لا ارتباطَ بأسماء المسارات

القاعدةُ مكتوبةٌ على «مشروعٌ يُعدَّل» لا على `/app/projects/update`.
يُفحص آليًّا:

```
✓ ولا مسارَ يُقرَّر إذنُه باسمه   ← لا سلسلة "/app/" في أيّ قيمةٍ في POLICY
```

فإعادةُ تسميةِ مسارٍ لا تمسّ حرفًا من الإذن، ومسارٌ جديدٌ لمورِدٍ قائمٍ
يرث سياستَه.

### الاستعدادُ لـP1.2 — شكلًا لا تنفيذًا

الشرطُ مكتوبٌ على **الصلاحية** لا على اسم الدور:

```python
("subscription", "grant"): NEEDS("manage_billing")
```

والأدوارُ تُشتقّ في `authz.roles_for` وحدها؛ اليوم مصدرُها الوحيد
`FALAH_ADMIN_KEY` — كما كان `trusted` تمامًا. **وP1.2 يضيف قراءةً من
القاعدة هناك ولا يمسّ معالِجًا ولا مسارًا.** ولم يُنفَّذ من P1.2 شيء.

### الحائطُ الثاني — بقصدٍ لا بنسيان

استعلاماتُ النطاق ما زالت مقيَّدةً بـ`AND user_id=?` (`projects.owned`،
`jobs.get`). **ليس ذلك تكرارًا للسياسة**: السياسةُ في `POLICY` وحدها،
وهذا تنفيذٌ لا يُتخطّى حتى لو أخطأ سطرٌ في الجدول. القرارُ واحد،
والحائطان اثنان.

---

## ٤ · «بلا تغييرِ سلوك» — كيف أُثبت

### أ · مقارنةٌ رمزًا برمزٍ وجسمًا بجسم

نُسخت `api.py` من `HEAD` قبل التفكيك، وشُغّل المسارانِ جنبًا إلى جنبٍ في
عمليةٍ واحدة على **٥٢ عنوانًا** فيها مساراتُ النجاح والخطأ والحجب:

```
مطابق: ٥٢ · مختلف: ٠  (من ٥٢)
```

المقارنةُ على `(رمز الحالة، json.dumps(الجسم, sort_keys=True))` — لا على
«يشبه».

### ب · العقد

```
sha256(API.md) قبل = b2fea169e6c496fee1ef8f061f6e3dd927879c93939f30105831b0341a402da0
diff baseline/API_BEFORE.md API.md → لا فرق
python3 api_contract.py --check → QUALITY_GATE = PASS
```

٦٠ مسارًا، صفرُ انحراف.

### ج · حزمُ P0 كلُّها

`isolation_test.py` — ٧٤ بندًا — مرّ **بلا تعديل حرفٍ فيه**، وهو الدليل
الحاسم الذي طلبتْه الخطّة.

### د · ترتيبُ تقييم الحقول

حُفظ عمدًا. `POST /app/video` بحمولةٍ ناقصةِ `item` على مشروعِ غيرك يردّ
اليوم «حقل ناقص: 'item'» كما كان يردّ — لأن `int(b["project"])` ثم
`int(b["item"])` كانتا تُقيَّمان قبل فحص الملكية، فبقي الترتيبُ مُعلَنًا في
`routing.py` بدل أن يكون مصادفةً في سطر.

---

## ٥ · تغييراتُ العقد

**لا شيء.** لا مسارَ أُضيف ولا حُذف ولا غُيِّر اسمُه، ولا رمزَ حالةٍ
تبدّل، ولا مخطَّطَ ردٍّ تغيَّر، ولا سلوكَ خطأٍ اختلف.

`api_contract.py` نفسُه تغيَّر — لكنّه **قوي** لا ضعُف: كان يستخرج
المساراتِ بتعبيرٍ نمطيّ من نصّ `api.py` و`app.py` («ابحث عن `p == "…"`
وارجُ ألّا يفوتك شيء»)، فصار يقرأ الجدولين المُعلَنين. ومخرَجُه مطابقٌ
حرفًا بحرف.

---

## ٦ · الاختبارات — قبل وبعد

| الحزمة | قبل | بعد |
|---|---:|---:|
| `tests.py` | ٥٢٨ | ٥٣٠ |
| `audit.py` | ١٢٤ | ١٢٤ |
| `ui_audit.py` | ٥٣ | ٥٣ |
| `failure_test.py` | ٩٥ | ٩٥ |
| `leak_test.py` | ٥٤ | ٥٤ |
| `isolation_test.py` | ٧٤ | ٧٤ |
| **`authz_test.py`** | — | **٨٣** |
| **المجموع** | **٩٢٨** | **١٬٠١٣** |

**صفرُ حذف.**

### تصحيحٌ في العدّ — يُقال ولا يُخفى

كان يُكتب «٩٢٩» في `P0_RELEASE_STATUS.md` و`P1_ARCHITECTURE.md`. العدُّ
كان يجمع أسطرَ `✓` في مخرَج `tests.py`، وآخرُها **سطرُ الخلاصة** لا بندَ
فحص. العددُ الصحيحُ قبل P1.1 هو **٩٢٨**. صُحِّح في الوثيقتين.

### خمسةُ بنودٍ غُيِّر ملفُّ مصدرها — ولم يُحذف بندٌ

بنودٌ تقرأ **نصَّ الشيفرة** لتتحقّق من بنيةٍ ما. انتقل ما تقرؤه، فانتقلت
معه:

| البند | كان يقرأ | صار يقرأ | أقوى؟ |
|---|---|---|---|
| «مسار التصدير لم يعد يُصيّر داخل الطلب» | `app.py` | `app_routes.py` **و**`app.py` | ✔ ملفّان بدل واحد |
| «يضعان في الطابور» · «الحصّة تُحجز عند الوضع» | `app.py` | `app_routes.py` | = |
| «المصدر يقارن الطول قبل الأرقام» | `api.py` | `falah/cards.py` | = |
| «حدُّ الجسم من البيئة» | `app.py` | `settings.py` **و**`app.py` | ✔ |
| «العدُّ عند القبول لا عند الطلب» | `app.py` | `app_routes.py` (تصديرٌ **ومقطع**) | ✔ مساران بدل واحد |
| «لا مسار قراءةٍ يغيب عن خادم التطبيق» | تعبيرٌ نمطيّ على `api.py` | `content_routes.declared()` | ✔ إعلانٌ لا نصّ |

وأُضيف بندان يمنعان أن يصير أحدُها فحصًا على لا شيء:

```
✓ وجدول المحتوى ليس فارغًا — الفحص أعلاه ليس فحصًا على لا شيء  (٢١ مسارًا)
✓ والمساراتُ ذاتُ الحدّ القبليّ معلَنةٌ في الجدول لا مبثوثةٌ في المعالِجات
```

---

## ٧ · اختباراتُ الإذن الجديدة — ٨٣ بندًا

### أ · `can()` وحدها (٢٦ بندًا) — بلا خادمٍ ولا قاعدة

صاحبٌ · غيرُ صاحبٍ · مجهول · مورِدٌ مفقود · مورِدٌ بلا مالكٍ مقروء ·
فعلٌ بلا سياسة · مورِدٌ بلا سياسة · فعلٌ خارج القائمة المغلقة · العامّ ·
الصلاحيةُ المسمّاة · مورِدُ الذات · وملفُّ التصدير بستّة مساراتِ خروج.

### ب · اكتمالُ الجدول (١٠ بنودٍ)

```
✓ كلُّ مسارٍ أعلن مورِدَه وفعله                        (٣٨ مسارًا)
✓ ولكلِّ زوجٍ (مورِد، فعل) سياسةٌ في POLICY             ([])
✓ وكلُّ فعلٍ من القائمة المغلقة                        ([])
✓ وكلُّ مسارٍ يأخذ رقمَ مورِدٍ من الطلب يُحمَّل مورِدُه   (١١ مسارًا)
✓ ومسارٌ غير معلَنٍ يقع على القاعدة العامّة
✓ وطريقةٌ غير معلَنةٍ كذلك
✓ ولا مسارَ يُقرَّر إذنُه باسمه
```

### ج · أ ← موردُ ب حيًّا (٤٧ بندًا)

| الطريق | ما جُرّب | النتيجة |
|---|---|---|
| **رقمٌ مباشر** | ٩ مساراتِ مشروع + مهمّة + إلغاء + ملفّ + سرد | ٤٠٠ «المشروع غير موجود» · ٤٠٠ «المهمّة غير موجودة» · ٤٠٤ «ملف غير متاح» |
| **رقمٌ مخمَّن ومتسلسل** | مسحُ ١..١١ للمشاريع والمهامّ | صفرُ ٢٠٠ · رمزٌ واحدٌ لا يفرّق |
| **الردُّ لا يفرّق** | مورِدُ ب × رقمٌ لا وجود له | الجسمان متطابقان نصًّا |
| **طلبٌ مُحوَّر** | `user_id`/`user`/`owner` في الجسم · منحةٌ لنفسه · مفتاحٌ مخمَّن · تعديلُ ملفٍّ لغيرك | لا يُصدَّق حقلٌ منها |
| **بلا وثيقة** | ١٥ مسارًا قراءةً وكتابة | ٤٠١ «يلزم تسجيل الدخول» |
| **وثيقةٌ انتهت** | خروجٌ ثم إعادةُ استعمال الكعكة | ٤٠١ |
| **والعامُّ يبقى عامًّا** | ٧ مسارات | ٢٠٠ |

### د · إعادةُ فحوص P0 الأمنية

XSS/حقنُ CSS · SSRF · الخروجُ من المسار · الوصلاتُ الرمزية · حقنُ SQL ·
العزل · تجاوزُ الحصّة · تجاوزُ حدّ المعدّل · تسريبُ الأخطاء — كلُّها في
`tests.py` و`isolation_test.py` و`leak_test.py`، وقد مرّت كما هي.

---

## ٨ · الأداء — قبل وبعد

قياسٌ بالأمر نفسه (`bench.py`)، ٨٠ عيّنةً لكل مسار، الوسيطُ لا المتوسّط.

### أ · استعلاماتُ القاعدة — لم تتغيّر في مسارٍ واحد

```
/health ١٢ · /card آية ٦ · /card حديث ٥ · بحثُ القرآن ١ · بحثُ الحديث ١
/chapters ٢ · /templates ٢ · /topics ٤٩ · /sources ١      ← قبل = بعد
```

### ب · زمنُ المسارات

| المسار | قبل | بعد |
|---|---:|---:|
| `/card` (آية) | ٢٫٠٧ | **٢٫٠٥** |
| `/card` (حديث) | ٢٫٥٣ | ٢٫٥٧ |
| `/app/projects` | ١٫٣٩ | ١٫٥٢ |
| `POST /app/export` | ٥٫٥٣ | ٦٫٢٥ |
| `POST /app/video` | ٥٫١٠ | ٥٫٢٩ |
| التصيير (عامل) | ٣٫٢ث · ٢٦ث | لم يُمَسّ |

### ج · هل هذه فروقٌ أم ضجيج؟

شُغّلت الشيفرةُ **نفسُها** مرّتين متتاليتين لقياس تشتّت الجهاز:

| المسار | تشغيلٌ أوّل | تشغيلٌ ثانٍ | الفرق |
|---|---:|---:|---:|
| `/healthz` (لا يمرّ بالموجِّه أصلًا) | ٠٫٧٥ | ٠٫٨٥ | **+١٣٪** |
| `/health` | ٦٦٫٣٢ | ٥٨٫٤٨ | **−١٢٪** |
| `/series` | ٨٫٥٠ | ٧٫٥٩ | −١١٪ |

التشتّتُ **±١٣٪**، وهو أوسعُ من أكثر فروق «قبل/بعد».
**فلا فرقَ يُقاس في مسارات القراءة.**

### د · وما الفرقُ الحقيقيّ؟

**استعلامٌ واحدٌ زائد** على أحدَ عشرَ مسارًا تُفحص ملكيّتُه — مقيسٌ
بـ`Connection.set_trace_callback` لا مقدَّر:

```
authz.load → استعلامٌ واحد: SELECT user_id … WHERE id=?   (مفتاحٌ رئيس)

GET  /app/projects        → ١ استعلام   (بلا تغيير)
GET  /app/limits          → ٠ استعلام   (بلا تغيير)
POST /app/projects/update → ٥ استعلامات (منها واحدٌ لتحميل المورِد)
POST /app/projects/delete → ١ استعلام حين يُمنع (كما كان)
```

**وهذا ثمنُ قراءةِ المالك بدل تصديقه.** والطلبُ الممنوع لم يزدد شيئًا.

### هـ · معيارُ القبول `/card ≤ ٢٫٥ م.ث` — بدقّة

- بطاقةُ **الآية** (وهي المقيسةُ في الخطّة بـ٢٫١): **٢٫٠٥** ✔
- بطاقةُ **الحديث**: ٢٫٥٧ — و**كانت ٢٫٥٣ قبل التفكيك**. أي أن المعيار
  كان متجاوَزًا قبل P1.1 بهامشٍ داخل الضجيج، وفرقُ التفكيك ٠٫٠٤ م.ث.
  يُقال ولا يُخفى.

---

## ٩ · تغييراتُ قاعدة البيانات

**لا شيء.** لا هجرةَ جديدة، ولا عمودَ، ولا فهرس. `004` آخرُ ما هو مطبَّق،
و`003` كما هي: **جاهزةٌ وغيرُ مطبَّقةٍ على الإنتاج**.

---

## ١٠ · اتّجاهُ الاعتماد

```
HTTP        app.py · api.py · worker.py
             ↓
تطبيق       routing · app_routes · content_routes · web · ratelimit
             ↓
نطاق        authz · cards · verify · projects · billing · jobs · auth
             ↓
بنيةٌ تحتية  store (sqlite) · settings · migrate · القرص
```

**فُحص آليًّا بتحليل شجرة الاستيراد**: لا وحدةَ تحت `falah/` تستورد `app`
ولا `api`.

وكان `falah/projects.py` و`falah/agent.py` يستوردان `api` — أي أن طبقةَ
النطاق تعتمد على طبقة الويب. نزل بناءُ البطاقة إلى `falah/cards.py`
فانقطع ذلك، و`api.py` يعيد تصديرَ الأسماء (`api.quran_card` وأخواتها)
فلم يُكسر مستوردٌ قائمٌ في `render.py` ولا `build.py` ولا `audit.py` ولا
حزم الاختبار.

**وما بقي مائلًا، صريحًا:** `falah/jobs.py` يستورد `carousel` و`video`،
و`falah/content_routes.py` يستورد `carousel`. وهذه وحداتُ **تصيير** في
جذر المشروع لا وحداتُ ويب — فليس فيها صعودٌ إلى HTTP — لكنّ موضعَها
ينبغي أن يكون تحت `falah/`. **لم يُنقل**: خارج ما طُلب. قُيِّد
`B20` في `P1_BACKLOG.md`.

---

## ١١ · التوثيق

- `P1_ARCHITECTURE.md`: خريطةٌ جديدة بالطبقات · **دورةُ حياةِ الطلب** ·
  **تدفّقُ الإذن** · **مسؤوليةُ كلِّ وحدة** (ومعها عمودُ «لا تفعل») ·
  **اتّجاهُ الاعتماد** · الأداءُ قبل/بعد مع فقرة الضجيج · د١ و د٢ مشطوبان.
- `P1_BACKLOG.md`: `B20`.
- `API.md`: **لم يتغيّر** — وهذا هو المقصود.

---

## ١٢ · بوّابة الجودة

```
✓ ثغرات الاعتماديات    ✓ ruff        ✓ mypy (٦٠ ملفًّا)   ✓ الأسرار
✓ عقد الـAPI           ✓ القاعدة     ✓ بروفة الهجرات      ✓ الوحدة
✓ الشامل عبر الشبكة    ✓ الواجهة     ✓ الكسر المتعمَّد     ✓ التسريب
✓ حدود المستخدم        ✓ طبقة الإذن  ✓ إعداد الحاوية
! بناء صورة الحاوية    BLOCKED — عفريت docker غير مشتغل

PASS 15 · FAIL 0 · BLOCKED 1        QUALITY_GATE = PASS
```

وأُضيفت بوّابةُ `authz` إلى `Makefile` و`gate.py` و`.github/workflows/ci.yml`.

---

## ١٣ · حالةُ الحواجز — لم يتغيّر منها شيء

| الحاجز | الحال |
|---|---|
| **بناءُ صورة الحاوية** | `UNVERIFIED` — لا عفريت docker في هذه البيئة |
| **GitHub Actions** | `LOCAL VERIFIED / REMOTE UNVERIFIED` — لا مستودعَ بعيد |
| **الهجرة 003** | `READY · NOT APPLIED TO PRODUCTION` |

**ولا واحدٌ منها يُحسب نجاحًا.** والإطلاقُ ما زال موقوفًا حتى تُغلق
`External Verification`.

---

## ١٤ · ما ظهر ولم يُنفَّذ

| # | ما هو | لماذا تُرك |
|---|---|---|
| B20 | `carousel.py` · `video.py` · `render.py` في الجذر | خارج ما طُلب — §١٠ |
| — | `GET /app/<مسارٍ لا وجود له>` بلا جلسةٍ يردّ ٤٠١ لا ٤٠٤ | **سلوكُ اليوم نفسُه**، حُفظ عمدًا بقاعدةٍ عامّة مُعلَنة. تغييرُه إلى ٤٠٤ تحسينٌ صغيرٌ ظاهرُ الأثر — **قرارُك** |
| — | CSRF غيرُ مُعلَنٍ لكل مسار | لأنه خاصّةُ **طريقةٍ** لا مسار: كلُّ POST يشترطه في طبقة HTTP كما كان. إعلانُه في كل سطرٍ ضجيجٌ لا معلومة |

---

## ١٥ · خطّةُ الرجوع

المرحلةُ commit واحد (`370777d`)، ولا هجرةَ فيها ولا تغييرَ عقد.

```
git revert 370777d
```

يعيد الحالَ تمامًا. ولا أثرَ في القاعدة ولا في الصادرات ولا في العملاء.

---

## ١٦ · التالي — بانتظار قرارِك

**P1.1 انتهت. P1.2 لم تبدأ ولن تبدأ إلا بأمرك.**

وحين تأمر، هذا ما تجده P1.2 مهيّأً لها:

- `authz.roles_for(user_id)` — الموضعُ الوحيد الذي يُضاف فيه قراءةُ
  الأدوار من القاعدة.
- `authz.PERMISSIONS` — سبعُ صلاحياتٍ مسمّاة مكتوبةٌ سلفًا:
  `manage_users` · `manage_roles` · `view_audit_log` ·
  `manage_system_settings` · `manage_billing` · `manage_content` ·
  `manage_exports`.
- `authz.ROLES` — اليوم دورٌ واحد؛ يُضاف `moderator` و`admin` و
  `super_admin` بحزمِ صلاحياتٍ، ولا يتغيّر نداءُ `can` في موضعٍ واحد.
- `NEEDS(perm)` — الشرطُ على الصلاحية لا على اسم الدور، فلا
  `if role == "admin"` في مكان.

**ولم يُكتب من هذا سطرُ تنفيذٍ واحد.** المكانُ محجوزٌ فقط.
