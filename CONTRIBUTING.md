# المساهمة في فَلاح

## القاعدةُ الأولى — ولا تُخفَّف

**لا يُولَّد نصٌّ شرعيٌّ ولا يُعدَّل بذكاءٍ اصطناعيّ.** لا آية، ولا حديث، ولا
حكمٌ عليه، ولا تخريج، ولا تفسير، ولا نسبةٌ إلى عالِم.

كلُّ نصٍّ يُنقل من مصدرٍ مسجَّلٍ في جدول `sources`. وما لا يُمكن التحقّقُ منه
**يُحجَب مع ذكر السبب** — ولا يُستبدَل بشيء.

من خالف هذا رُدَّت مساهمتُه مهما جادت شيفرتُها.

## قبل الدفع

```bash
make gate          # ٢٢ بوّابة
cd falah_app && flutter analyze --fatal-infos && flutter test
```

**والبوّابةُ لا تُخفَّف لتمرّ مساهمة.** إن أسقطها تغييرُك فالتغييرُ يُصلَح،
لا الفاحص.

## ما لا يُقبل بحال

- حذفُ اختبارٍ صار مزعجًا
- `# noqa` أو `// ignore:` بلا سببٍ مكتوبٍ في السطر نفسِه
- إضعافُ إعدادٍ إنتاجيٍّ ليمرّ اختبار
- سرٌّ حرفيٌّ ولو اختباريًّا — يُشتقّ بـ`hashlib` كما في الملفّات القائمة
- `PASS` لما لم يُشغَّل
- TODO في شيفرةٍ تُعلَن منتهية

## الثوابتُ الأمنيّة

`invariants.py` يحمل ١٦ ثابتًا **مسمّاةً بأسمائها لا بأعدادٍ دنيا**.
تغييرُ حدِّ ملكيّةٍ يُسقط الثابتَ — وهذا مقصود: يُحدَّث بالاسم فيظهر في
الفرق ولا يمرّ صامتًا.

## أسلوبُ الشيفرة

- بايثون: مكتبةٌ قياسيّةٌ ما أمكن. لا تُضاف حزمةٌ إلا حين تعجز بايثون.
- Dart: معماريّةٌ نظيفةٌ بطبقات. `sealed` للأخطاء والنتائج.
- التعليقاتُ تشرح **لماذا** لا **ماذا**. والعيبُ الذي أوجب سطرًا يُكتب معه.

## الهجرات

الهادمةُ لا تجري على الإنتاج إلا بـ`make db-safe-migrate`. والحارسُ داخل
`falah/migrate.py` نفسِه — لا يتجاوزه متغيّرُ بيئة.
