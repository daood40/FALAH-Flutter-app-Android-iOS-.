# P1.2 — تقريرٌ نهائيّ

```
P1.2 STATUS: PASS  (مع حواجزَ خارجيةٍ مفتوحةٍ من P0 — لم يُغلقها P1.2 ولم يدَّعِ)

Tests:              PASS   ١٬٠١٣ → ١٬١٤٨ (+١٣٥) · صفرُ حذف
Audit:              PASS   ١٢٤ بندًا — بلا تغيير
Security:           PASS   critical=٠ · high=٠ · pip-audit=٠ ثغرة
Isolation:          PASS   ٧٤ بندًا — بلا تعديلِ حرفٍ في الحزمة
RBAC:               PASS   ١٢٥ بندًا جديدة — مصفوفةُ ١٩٠ خانة
Authentication:     PASS   مُراجَعةٌ بندًا بندًا + حدٌّ جديدٌ لكل عنوان
Audit Log:          PASS   يُلحق · لا يُعدَّل · لا يُحذف · لا سرَّ فيه
Migration:          PASS   ٠٠٥ إضافيّةٌ آمنة · قاعدةٌ جديدةٌ ومهاجَرةٌ تتقاربان
Performance:        PASS   صفرُ استعلامٍ زائدٍ للإذن · لا N+1
Leak checks:        PASS   ٥٤ بندًا
Failure injection:  PASS   ٩٥ + ٧ حقنٍ جديدة — والنتيجةُ DENY في كلِّها

Docker:             BLOCKED / UNVERIFIED   — عفريت docker غير مشتغل
CI:                 LOCAL VERIFIED / REMOTE UNVERIFIED — لا مستودعَ بعيد
Migration 003:      READY · NOT APPLIED TO PRODUCTION
External verification: NOT DONE — آبل · جوجل · SMTP · اختبارُ اختراق ·
                       مراجعةٌ بشرية · فحصُ صورة الحاوية
```

**commit: `ba22d0d`** (وقبله `bf3dd12` تقريرُ P1.1، و`370777d` شيفرتُها).
**P1.3 لم تبدأ.**

---

# ١ · القرار المعماريّ — وما يمنع الفوضى بالضبط

الأمرُ قال: لا نريد مجرّد إضافة `role`، ولا `if role == "admin"` متناثرة.
والحلُّ لم يكن اسمًا أفضل، بل **شقّ الشرط الواحد إلى بوّابتين**:

```
can(subject, action, resource)
   ├─ ① صلاحية:  هل يملك دورُ الفاعل «<مورِد>.<فعل>»؟
   │              سؤالٌ عن الفاعل وحده — لا يعرف أيَّ مورِدٍ بعينه.
   └─ ② نطاق:    وهل هذا المورِدُ بعينه في نطاقه؟
                  PUBLIC · AUTHENTICATED · SELF · OWNER · ANY
```

**ولماذا اثنتان لا واحدة؟** لأن خلطهما هو أصلُ فوضى `if role == "admin"`
نفسها. «يملك صلاحيةَ تعديل مشروع» شيء، و«هذا المشروع له» شيءٌ آخر. من
خلطهما اضطُرّ أن يكتب استثناءَ الإداريّ في كل معالِج. ومن فصلهما كتب:
الإداريُّ يملك صلاحيةً أوسع ونطاقُه `ANY` — **سطرٌ في جدول، لا شرطٌ في
مئة دالّة**.

والفصلُ يظهر في اختبارٍ واحد يقيس الفرق:

```
✓ المشرفُ أعلى من المستخدم تسلسلًا                  ← ② يسمح
✓ **ولا يملك صلاحيةَ تغييرِ دورٍ ولا حالة**          ← ① يمنع
```

لو خُلطتا لَما أمكن التعبيرُ عن «أعلى منه ولا يملك تغييره».

## واسمُ الصلاحية هو الزوجُ نفسُه

`project.update` **هي بالضبط** `(resource="project", action="update")`.
وفائدتُه أن **عمليةً بلا صلاحيةٍ صارت مستحيلة**: الصلاحيةُ ليست شيئًا
يُكتب إلى جانب العملية، بل هي اسمُها.

```
٣٨ صلاحيةً  ⟷  ٣٨ زوجًا في POLICY      تقابلٌ تامّ في الاتجاهين
أزواجٌ بلا صلاحية: []      صلاحياتٌ بلا زوج: []
صلاحيةٌ لا يبلغها دور: []  دورٌ يحمل صلاحيةً خارج الكتالوج: []
```

**والكتالوجُ مستخرَجٌ من النظام لا مفترَض**: مشتقٌّ من جدول
`falah/routing.py` — ٤٣ مسارًا، ٣٨ زوجًا مميّزًا. ولم أضع
`system.manage` لأنني لم أجد عمليةً قائمةً تحتاجها؛ ولا صلاحيةَ تُخترع
لعملٍ لا وجود له.

---

# ٢ · الأدوار

| الدور | صلاحيات | ما يزيده |
|---|---:|---|
| `anonymous` | ٨ | ما يسبق الجلسة: تسجيلٌ · دخولٌ · خروجٌ · استعادةٌ · محتوًى عامّ |
| `user` | ٣١ | كلُّ ما يملكه صاحبُ حساب |
| `moderator` | ٣٤ | `user.list` · `user.read` · `audit.list` |
| `admin` | ٣٧ | `user.update` · `user_role.read` · `subscription.grant` |
| `super_admin` | ٣٨ | `user_role.update` — **تغييرُ الأدوار وحده** |

```
✓ anonymous ⊂ user ⊂ moderator ⊂ admin ⊂ super_admin   (ضمٌّ لا نسخ)
✓ ولا نجمةَ في أيّ حزمة — لا admin.* ولا wildcard
✓ وتغييرُ الأدوار مقصورٌ على الأعلى وحده
```

**`super_admin` ليس تجاوزًا سحريًّا.** ما لا يتجاوزه — ومُختبَرٌ حيًّا:

| لا يتجاوز | الدليل |
|---|---|
| **ملكيةَ موارد الناس** | `✓ ولا super_admin يقرأ مشروعَ مستخدم — الإدارةُ ليست اطّلاعًا` · `✓ ولا ينزّل صادرتَه` |
| **حارسَ التسلسل** | `✓ يُمنع: super_admin → نفسه` |
| CSRF · حدَّ الحجم · التحقّقَ من المدخل · حدَّ المعدّل | تمرّ عليه كما على غيره |
| سجلَّ التدقيق | كلُّ فعلٍ له مسجَّلٌ باسمه |

وهذا مقصود: الإدارةُ تُدير الحسابات والأدوار والفوترة، **لا تقرأ محتوى
الناس**. ومن أراد ذلك غدًا فليُنشئ صلاحيةً باسمها ونطاقًا `ANY` ويوثّقها
ويختبرها — قرارٌ صريح، لا أثرٌ جانبيّ.

## ولا اسمَ دورٍ في شرط — مُثبَتٌ آليًّا

```
✓ لا فحصَ باسم دورٍ في المشروع كلِّه — ولا `if role == admin`
```

يمسح ٦٢ ملفًّا بستّ صيغٍ لكلِّ دور، **بعد إسقاط التعليقات والنصوص
بـ`tokenize`** — لولاه لسقط الفحصُ على تعليقٍ يقول «ولا `if role ==
admin` هنا»، وهو نفيٌ لا إثبات. و`falah/authz.py` و`falah/roles.py`
مستثنيان: هما موضعُ **تعريف** الأدوار لا فحصِها.

---

# ٣ · حارسُ التسلسل — ثلاثُ قواعدَ في `authz` لا في المعالِجات

| # | القاعدة | ما تمنعه |
|---|---|---|
| ق١ | `actor.id == target.id ⇒ DENY` | ترقيةَ الذات — **ولا استثناءَ لأحد**، ويمنع كذلك أن يُسقط آخرُ `super_admin` نفسَه فيُقفل النظام |
| ق٢ | `perms(الدور المطلوب) ⊄ perms(الفاعل) ⇒ DENY` | أن يصنع أحدٌ دورًا أقوى منه |
| ق٣ | `perms(دور الهدف) ⊄ perms(الفاعل) ⇒ DENY` | أن يُدار نظيرٌ أو مَن فوقك |

## ثمانيةُ مساراتِ تصعيدٍ — كلُّها ٤٠٣ حيًّا

```
✓ يُمنع: user → moderator          ✓ يُمنع: moderator → admin
✓ يُمنع: user → admin              ✓ يُمنع: moderator → super_admin
✓ يُمنع: user → super_admin        ✓ يُمنع: admin → super_admin
✓ يُمنع: admin → admin (نظير)      ✓ يُمنع: super_admin → نفسه
✓ والإداريُّ لا يُخفّض نفسَه كذلك
```

**والردُّ عند المنع رسالةٌ واحدة** لكل الأسباب — لا «لا تملك هذا الدور»
ولا «هذا الحساب أعلى منك»، فذلك يرسم للمهاجم خريطةَ الأدوار. والسببُ
الدقيق يُكتب في سجلّ التدقيق حيث يُقرأ ولا يُسرَّب.

## الإسنادُ الجماعيّ وحقنُ الدور

| الطريق | النتيجة |
|---|---|
| `role` في `/app/profile` | `✓ لا يُصدَّق` — الدورُ يبقى `user` |
| `role` في `/app/register` | `✓ لا يُصدَّق` |
| مفاتيحُ JSON مكرَّرة `{"role":"user","role":"super_admin"}` | `✓ لا تفتح بابًا` |
| `status` · `id` · `user_id` في الجسم | تُتجاهَل |
| مسارٌ بديل (`/app/admin/users/status` من مستخدم) | `✓ ٤٠٣` |
| **تسابُق: ٢٠ طلبًا متزامنًا لترقية الذات** | `✓ كلُّها ٤٠٣ · والدورُ لم يتغيّر` |

والحارسُ البنيويّ وراء ذلك: **لا `**body` يُمرَّر في موضعٍ واحد**.
`update_profile` تأخذ حقلين مسمّيَين لا قاموسًا.

---

# ٤ · المصادقة

## ما كان قائمًا ويجتاز البنود — رُوجع بندًا بندًا

| البند | الحال |
|---|---|
| تجزئةُ كلمة المرور | **PBKDF2-SHA256 · ٢٤٠٬٠٠٠ دورة · ملحٌ ١٦ بايت لكل حساب** — خوارزميةُ كلماتِ مرورٍ مخصّصة. لا `sha256(pw)` ولا MD5 ولا نصٌّ ظاهر. من المكتبة القياسية — **لا تعميةَ مخترَعة** |
| المقارنة | `hmac.compare_digest` — زمنٌ ثابت |
| عددُ الدورات | يُحفظ مع كل حساب فيُرفع مستقبلًا بلا كسرِ ما مضى |
| الجلسة | رمزٌ ٣٢ بايت · **يُخزَّن مجزَّأً sha256** · انتهاءٌ · حذفٌ عند الانتهاء |
| الكعكة | HttpOnly · SameSite=Strict · Secure (يُشترط في الإنتاج) · **ولا شيءَ في `localStorage`** |
| الإبطال | تغييرُ الكلمة واستعادتُها يحذفان **كلَّ** جلسات الحساب |
| رموزُ الاستعادة | مجزَّأة · مرّةً واحدة · تنتهي (ساعة · يومان) |

**فلم أغيّر آلية المصادقة.** النظامُ ويبٌّ بكعكةٍ آمنة، وهي الأنسب
لبنيته؛ وتغييرُها إلى Bearer بلا موجِبٍ مخاطرةٌ بلا مقابل.

## ما أُضيف

| الإضافة | لماذا |
|---|---|
| **حدُّ دخولٍ لكل عنوان** (٣٠/ربع ساعة) | الحدُّ القائم منذ P0 **لكل بريد** (٨/ربع ساعة). ومن يجرّب ألفَ بريدٍ بكلمةٍ واحدة (password spraying) **لا يبلغ حدَّ أيٍّ منها أبدًا**. فأُضيف حدٌّ ثانٍ لكل عنوان — **إلى جانبه لا بدلًا منه**، والاثنان يسدّان الوجهين |
| `login.success` · `login.failure` · `logout` في السجلّ | بعنوانٍ ووكيلٍ ونتيجة. **ولا يُسجَّل البريدُ المحاوَل به** — تسجيلُه يبني قائمةَ عناوينَ في جدولٍ يقرؤه المشرف |
| إيقافُ الحساب يُنهي جلساتِه | `✓ الموقوفُ لا تُقبل جلستُه · ✓ ولا يدخل من جديد` |
| تغييرُ الدور يُنهي جلساتِ الهدف | وإلا بقي مَن خُفِّض يعمل بصلاحياتٍ نُزعت منه حتى تنتهي كعكتُه بعد شهر |

## تعدادُ الحسابات — راجعتُه ولم أغيّره صمتًا

| المسار | يكشف؟ | القرار |
|---|---|---|
| `/app/login` | **لا** | ردٌّ واحد، والاشتقاقُ يُحسب في الحالين فلا يفرّق الزمن |
| `/app/password/forgot` | **لا** | ردٌّ واحد سواءٌ وُجد أو لا |
| `password/reset` · `verify/confirm` | **لا** | «رمزٌ منتهٍ أو مستعمَل» في كل الحالات |
| **`/app/register`** | **نعم** | «هذا البريد مسجَّل من قبل» — **بقي، ولم يُغيَّر** |

**ولماذا بقي؟** ثلاثةُ أسباب، وكلُّها من أمرك أنت:

1. تغييرُه يغيّر **جسمَ ردٍّ ورمزَه على مسارٍ عامّ** — أي كسرُ عقدٍ غيرُ
   مخطَّط، وأمرُك يقول عندها: **STOP**.
2. البديلُ الصحيح (قبولٌ صامت + رسالةُ بريدٍ تقول «لك حسابٌ بالفعل»)
   **يحتاج SMTP مُتحقَّقًا منه** — وهو `UNVERIFIED` في خطّ الأساس. بناؤه
   على قناةٍ لم تُجرَّب يستبدل تسريبًا صغيرًا بحسابٍ لا يُنشأ أصلًا.
3. الحدُّ قائم: **٢٠ تسجيلًا/ساعة لكل عنوان**، فالتعدادُ الواسع محدود.

**قُيِّد `B21` في `P1_BACKLOG.md`. قرارُك.**

---

# ٥ · سجلّ التدقيق

## الكتالوج مغلق — ٢٢ حدثًا

```
login.success · login.failure · logout
account.created · account.updated · account.deleted
password.changed · password.reset
role.changed · user.suspended · user.reactivated
subscription.granted · subscription.canceled · subscription.store_event
project.created · project.deleted
export.created · export.failed · export.completed
security.denied · audit.read
```

**حدثٌ خارجَه يرفع `ValueError`** — فلا ينمو السجلُّ بلا حساب ولا يصير
ضجيجًا لا يُقرأ. و`security.denied` خصوصًا هو ما يُظهر المسحَ بالأرقام
المتسلسلة: عشرون سطرًا من عنوانٍ واحدٍ في دقيقة تقول ما لا يقوله ردٌّ واحد.

## الحقول

`id · at · request_id · actor_id · actor_role · action · resource_type ·
resource_id · result · ip · user_agent · metadata`

- `actor_role` هو الدورُ **لحظةَ الفعل** لا الآن.
- `request_id` معرّفٌ لكل طلب — **الحدُّ الأدنى وحده**. لا سجلٌّ مهيكل،
  ولا مقاييس، ولا ترويسةُ ردٍّ جديدة (فذلك تغييرُ عقد). **P1.3 لم تبدأ.**
- `✓ وكلُّ سطرٍ جاء عبر الشبكة يحمل رقمَ طلبٍ وعنوانًا`
- `✓ وما جاء من سطر الأوامر يُميَّز بفاعله `system:cli` ولا يُخلط`

## لا سرَّ فيه — مصفاةٌ في الشيفرة، تُفتَّش بقنّاصة

المصفاة تُسقط **بالاسم** (`pass|pwd|secret|token|cookie|session|auth|
api_key|credential|salt|hash|signature|receipt|payload|private`)
و**بالشكل** (قيمةٌ ≥٢٤ محرفًا من محارف الرموز)، وتُسقط العمقَ والحمولاتِ
الخام. والإسقاطُ **حذفٌ تامّ** لا نجومٌ تحكي طولَ السرّ.

والاختبارُ يحقن قنّاصةً معروفةً في مسارات الأسرار كلِّها ثم يفتّش:

```
✓ المصفاة تُسقط كلَّ ما يشبه سرًّا بالاسم
✓ وتُسقط ما يشبه رمزًا بالشكل ولو كان مفتاحُه بريئًا
✓ ولا تُبقي عمقًا ولا حمولةً خام
✓ وتُبقي ما ليس سرًّا (plan · count · ok)
✓ لا أثرَ للسرّ في أيّ عمودٍ من الجدول كلِّه
✓ ولا كعكةَ ولا ترويسةَ وثيقة
```

## يُلحق ولا يُعدَّل — في القاعدة نفسها

```
✓ القاعدةُ نفسُها ترفض: UPDATE audit_logs SET action='x'…
✓ القاعدةُ نفسُها ترفض: UPDATE audit_logs SET actor_id=99 WHERE id=1…
✓ القاعدةُ نفسُها ترفض: DELETE FROM audit_logs…
✓ القاعدةُ نفسُها ترفض: DELETE FROM audit_logs WHERE id=1…
✓ ولا سطرَ ضاع
✓ user لا يقرأ السجلّ · ✓ مجهول لا يقرأ السجلّ · ✓ ولا يُكتب فيه عبر الشبكة
```

مُشغِّلان (`triggers`) يُجهضان بـ`RAISE(ABORT)` — **لا في التطبيق وحده**.
ولا مسارَ يعدّل أو يحذف، لا للمستخدم ولا للإداريّ ولا لـ`super_admin`.
والتصحيحُ حدثٌ جديد.

**وحذفُ الحساب لا يمحو أثرَه:** `✓ الحسابُ حُذف · ✓ وسجلُّ تدقيقه بقي
كاملًا`. لا مفتاحَ أجنبيًّا على `actor_id`، و`audit_logs` ليس في جداول
الحذف.

### وحدُّ هذه المنعة — يُقال ولا يُخفى

من يملك ملفَّ `app.db` على القرص يملك أن يُسقط المُشغِّل ويكتب ما شاء.
الحمايةُ **من التطبيق ومن مستعمليه، لا من مالك الخادم**. وكشفُ العبث
بوصولٍ خام يحتاج سلسلةَ تجزئةٍ أو سجلًّا خارج الجهاز — **لم يُطلب في
P1.2**، وقُيِّد `B22`.

---

# ٦ · قاعدة البيانات

## الهجرة `005_roles_and_audit` — إضافيّةٌ آمنة

```
ALTER TABLE users ADD COLUMN role TEXT NOT NULL DEFAULT 'user';
ALTER TABLE users ADD COLUMN role_changed_at INTEGER;
CREATE INDEX ix_users_role …
CREATE TABLE audit_logs(…) + ٣ فهارس + مُشغِّلان
```

**لا `DROP` ولا نقلَ بياناتٍ ولا إعادةَ بناءِ جدول.** ولا تمسّ `003`
الهادمة: تبقى `READY · NOT APPLIED`.

## البيانات القائمة — لم أفترض قاعدةً فارغة

```
✓ قاعدةٌ مهاجَرةٌ وقاعدةٌ جديدة تنتهيان إلى الكائنات نفسها  (٤٣ كائنًا)
✓ وأعمدةُ users متطابقةٌ نوعًا وافتراضيًّا
✓ **ولا مستخدمَ قائمٌ صار إداريًّا بالهجرة**   (٠ · ٠)
✓ والافتراضيُّ في العمود نفسه هو أقلُّ الأدوار
✓ وسجلُّ التدقيق ومُشغِّلاه موجودان في المسارين
✓ والهجرة ٠٠٥ مصنَّفةٌ آمنة — إضافةٌ محضة بلا DROP ولا نقلِ بيانات
```

## عطبٌ وقع فعلًا وأُصلح — يُقال لا يُطوى

أوّلَ تشغيلٍ للبوّابة **سقط الخادمُ والعامل معًا**:

```
sqlite3.OperationalError: no such column: role
  falah/store.py:258 in init → c.executescript(SCHEMA)
```

السبب: وضعتُ `CREATE INDEX ix_users_role ON users(role)` في `SCHEMA`،
و`SCHEMA` تُنفَّذ **قبل** الهجرات على القواعد القائمة — فالفهرسُ يسبق
العمود. **وهو الدرسُ نفسُه الذي علّمته الهجرةُ ٠٠٤** في P0-2، ونسيتُه.

العلاج: الفهرسُ يملكه `falah/migrate.py` وحده، كما فهارسُ الطابور.
وأُضيف **اختبارٌ دائم** يقارن المخطَّطين — فلا يتكرّر الدرسُ ثالثةً.

## أوّلُ `super_admin`

**ليس عبر الشبكة ولا بمفتاحٍ في ترويسة:**

```
python3 -m falah.roles grant <البريد> super_admin --reason "..."
```

من يملك تشغيلَ هذا يملك القرصَ والقاعدةَ أصلًا، فليس توسيعًا لسطح
الهجوم. **والفرقُ أن الفعلَ يترك أثرًا باسمه** في `audit_logs` بفاعلٍ
`system:cli` وسببٍ مكتوب — بدل مفتاحٍ لا يقول مَن استعمله.

---

# ٧ · تغييراتُ العقد — ٦٠ → ٦٦ مسارًا

| التغيير | النوع |
|---|---|
| `/app/admin/users` · `/app/admin/users/{id}` · `/app/admin/roles` · `/app/admin/audit` (GET) | **إضافيّ** |
| `/app/admin/users/role` · `/app/admin/users/status` (POST) | **إضافيّ** |
| `/app/me` يضيف `user.role` | **إضافيّ** — قراءةٌ فقط، ولا يُقبل من العميل |
| **`X-FALAH-ADMIN` أُلغي** | **كاسرٌ — مقصودٌ ومطلوب** |

## عن الكسر الوحيد — بصراحة

**من كان يستعمل `X-FALAH-ADMIN` اليوم يتوقّف غدًا.** وهذا ما طلبتَه
حرفيًّا: «لا أريد Shared Key كبديل للحسابات».

- المسارُ البديل: `python3 -m falah.roles grant <بريد> super_admin`.
- `FALAH_ADMIN_KEY` في البيئة **لا يمنع الإقلاع ولا يمنح شيئًا**، ويُطبع
  تحذيرٌ صريحٌ في الإنتاج يقول ذلك ويدلّ على البديل.
- `✓ ولا أثرَ له في طبقة الإذن` — فحصٌ آليّ.
- مذكورٌ في وثيقة التصميم قبل التنفيذ، وفي `API.md` بعده، وهنا.

**ولا شيءَ غير هذا.** بقيّةُ المسارات الستّين بردودها ورموزها وسلوك
أخطائها كما هي — والعقدُ يحرس ذلك (`api_contract.py --check` = PASS).

---

# ٨ · الاختبارات — ١٬٠١٣ → ١٬١٤٨

| الحزمة | قبل | بعد |
|---|---:|---:|
| `tests.py` | ٥٣٠ | ٥٣٨ |
| `audit.py` | ١٢٤ | ١٢٤ |
| `ui_audit.py` | ٥٣ | ٥٣ |
| `failure_test.py` | ٩٥ | ٩٥ |
| `leak_test.py` | ٥٤ | ٥٤ |
| `isolation_test.py` | ٧٤ | ٧٤ |
| `authz_test.py` | ٨٣ | ٨٥ |
| **`rbac_test.py`** | — | **١٢٥** |
| **المجموع** | **١٬٠١٣** | **١٬١٤٨** |

**صفرُ حذف.**

## المصفوفة — لا خمسةُ أمثلةٍ منتقاة

```
✓ المصفوفة كاملةً (٣٨ زوجًا × ٥ أدوار = ١٩٠ خانة)   ← ٠ خانةً مخالفة
✓ زوجٌ لا سياسةَ له يُمنع لكلِّ دورٍ — حتى super_admin
✓ وفعلٌ لا سياسةَ له على مورِدٍ قائم كذلك
```

والمصفوفةُ الحيّة كذلك: ٤ أدوارٍ × ٣ مساراتٍ إدارية = ١٢ خانةً عبر HTTP،
ثم ٣ مساراتٍ بلا جلسة.

## المصفوفة الأفقية

```
✓ أ يقرأ مشروعَه · ✓ وأ لا يبلغ مشروعَ غيره
✓ ب يقرأ مشروعَه · ✓ وب لا يبلغ مشروعَ غيره
✓ ولا moderator/admin/super_admin يقرأ مشروعَ مستخدم
✓ ولا ينزّل صادرتَه
```

## fail closed — حقنُ أعطالٍ حيّ

| العطب | النتيجة |
|---|---|
| **دورٌ مجهولٌ في القاعدة** (`wizard`) | `✓ يُمنع من كلِّ شيءٍ` — ستّةُ مسارات، كلُّها ٤٠٣. `✓ والعامُّ يبقى عامًّا له` |
| **سجلٌّ متعذّرُ الكتابة** | `✓ تغييرُ الدور يسقط ولا يقع صامتًا` (٥٠٠ والدورُ لم يتغيّر) · `✓ ولا يُسرَّب سببُ العطب` · `✓ وبعودة السجلّ يعمل كما ينبغي` |
| كعكةٌ مختلَقة · كعكةٌ فارغة | `✓ ٤٠١ لا ٥٠٠` |
| دورٌ فارغٌ أو `None` | `✓ صفرُ صلاحيات` |

**والقاعدةُ في الشيفرة لا في النيّة**: `perms_of` تعيد `frozenset()`
لدورٍ لا تعرفه — لا صلاحياتِ مستخدم.

## اختباراتٌ غُيِّرت (لا حُذفت)

| البند | كان | صار | أقوى؟ |
|---|---|---|---|
| «إنتاجٌ بمفتاح إدارةٍ قصير يسقط» | يفحص طولَ مفتاحٍ **أُلغي** | «المفتاحُ المشترك أُلغي: وجودُه لا يمنع الإقلاع ولا يمنح دورًا» + «ولا أثرَ له في طبقة الإذن» | ✔ بندان بدل واحد، ويفحصان ما يهمّ |
| «حدُّ المعدّل يشمل…» | ٥ أنواع | ٦ أنواع + بندٌ يثبت أن حدَّ العنوان **إلى جانب** حدِّ البريد | ✔ |
| «والمشرفُ لا يدير أحدًا» | توقُّعٌ خاطئ | بندان يفصلان التسلسلَ عن الصلاحية | ✔ يقيس الفصل نفسَه |

---

# ٩ · الأمن — إعادةُ تشغيلٍ كاملة

| الفحص | النتيجة |
|---|---|
| XSS · حقنُ CSS (`safe_color`) | ✓ `tests.py` |
| SSRF (مخطَّطُ التلاوة وحجمُها) | ✓ `tests.py` |
| الخروجُ من المسار · الترميزُ المزدوج · الوصلاتُ الرمزية | ✓ `isolation_test.py` — ١٧ مسارًا |
| حقنُ SQL | ✓ استعلاماتٌ مُعامَلة — بما فيها `audit.listing` الجديدة |
| IDOR | ✓ `authz_test.py` + `rbac_test.py` |
| عزلُ المشاريع والمهامّ والصادرات | ✓ ٧٤ بندًا **بلا تعديلِ حرفٍ في الحزمة** |
| الحصّة | ✓ `failure_test.py` — ٨ سيناريوهات |
| حدُّ المعدّل | ✓ + النوعُ الجديد |
| تسريبُ الأخطاء | ✓ ٥٤ بندًا |
| حدُّ حجم الطلب (٤١٣) | ✓ (وانظر `B23` أدناه) |
| سلامةُ العمليات الفرعية والعامل | ✓ لم تُمَسّ |
| `security_scan.py` · `pip-audit` | ✓ ٠ critical · ٠ high · ٠ ثغرة |

**ولم يُفقد شيءٌ من حماية P0/P1.1.**

---

# ١٠ · الأداء

## استعلاماتُ الإذن — صفرٌ زائد

```
بناءُ الفاعل والصلاحيات:            ٠ استعلام   (الدورُ جاء مع الجلسة أصلًا)
وفحصُ الصلاحيات الـ٣٨ كلِّها:        ٠ استعلام   ← لا N+1
```

الدورُ عمودٌ في صفّ المستخدم الذي تقرؤه الجلسة أصلًا (`SELECT u.*`)،
والصلاحياتُ تُحسب من `ROLES` في الذاكرة. **فبوّابةُ الصلاحية لم تكلّف
استعلامًا واحدًا.**

| الطلب | P1.1 | P1.2 | الفرق |
|---|---:|---:|---|
| `GET /app/projects` | ١ | ١ | = |
| `GET /app/limits` | ٠ | ٠ | = |
| `POST /app/projects/update` | ٥ | ٥ | = |
| `POST /app/projects/delete` (ممنوع) | ١ | ٤ | **+٣** ← كتابةُ `security.denied` |

**والزيادةُ الوحيدة ثمنُ تسجيل المنع**، وتقع على الطلب المرفوض وحده.

## استعلاماتُ المحتوى — لم تتغيّر

```
/health ١٢ · /card آية ٦ · /card حديث ٥ · بحثان ١+١
/chapters ٢ · /templates ٢ · /topics ٤٩ · /sources ١     قبل = بعد
```

## الزمن

| المسار | قبل | بعد |
|---|---:|---:|
| `/app/config` · `/app/me` | ٠٫٩٢ · ١٫٤٦ | **٠٫٧٢ · ١٫٢٨** |
| `/app/projects` · `/app/jobs` · `/app/exports` | ١٫٦١ · ١٫٦٧ · ١٫٥٥ | **١٫٣٦ · ١٫٣٨ · ١٫٢٩** |
| `/card` (آية · حديث) | ١٫٧٥ · ٢٫١٧ | ٢٫٠٩ · ٢٫٤٧ |
| `POST /app/export` | ٤٫٧٢ | ٤٫٦٦ |

مساراتُ التطبيق **أسرعُ ١١–٢٢٪**، ومساراتُ المحتوى مختلطةٌ (+١٩٪ إلى
−١٤٪) — **وكلُّه داخل الضجيج**: تشغيلان متتاليان للشيفرة نفسها اختلفا
في P1.1 بـ±١٣٪، وقُيس ذلك ولم يُفترض.

**الحكمُ الصادق: لا فرقَ يُقاس في الزمن.** والفرقُ الحقيقيُّ في عدد
الاستعلامات — وهو صفرٌ للإذن، و+٣ للمنع المسجَّل.

---

# ١١ · الملفّات

## جديدة
`falah/audit.py` (١٣٩) · `falah/roles.py` (٨٦) · `rbac_test.py` (٥٥٨) ·
`docs/P1.2_BASELINE.md` · `docs/P1.2_AUTH_RBAC_DESIGN.md`

## مُعدَّلة
`falah/authz.py` (٣٢٠→٤٩٧ · البوّابتان والكتالوج والحارس) ·
`falah/routing.py` (٦ مساراتٍ إدارية + تسجيلُ المنع) ·
`falah/app_routes.py` (٦ معالِجاتٍ إدارية + ربطُ الأحداث) ·
`falah/store.py` · `falah/migrate.py` (٠٠٥) · `falah/auth.py` (`role` في
`dict_user`) · `falah/web.py` (`request_id`) · `falah/settings.py`
(حذفُ `ADMIN_KEY`) · `falah/ratelimit.py` (حدُّ الدخول) · `app.py` ·
`api_contract.py` · `API.md` · `tests.py` · `authz_test.py` · `bench.py` ·
`Makefile` · `gate.py` · `.github/workflows/ci.yml` ·
`P1_ARCHITECTURE.md` · `P1_BACKLOG.md`

## لم تُمَسّ
`falah/verify.py` · `falah/cards.py` · `falah/content_routes.py` ·
`falah/jobs.py` · `falah/projects.py` · `falah/billing.py` ·
`render.py` · `carousel.py` · `video.py` · `worker.py` · `build.py` ·
`api.py` · `isolation_test.py` · `leak_test.py` · `failure_test.py` ·
`audit.py` · `ui_audit.py` · `falah-app.html`

**وحزمُ P0 الأربع لم يُمَسّ فيها حرفٌ واحد** — وهو الدليلُ على أن
الانحدار لم يقع.

---

# ١٢ · ما بقي مفتوحًا — بلا تجميل

| # | البند | الحال |
|---|---|---|
| — | **بناءُ صورة الحاوية** | `BLOCKED / UNVERIFIED` — لا عفريت docker |
| — | **GitHub Actions** | `LOCAL VERIFIED / REMOTE UNVERIFIED` |
| — | **الهجرة 003** | `READY · NOT APPLIED TO PRODUCTION` |
| — | **آبل · جوجل · SMTP** | `UNVERIFIED` |
| — | **اختبارُ اختراق · مراجعةٌ بشرية · فحصُ صورة الحاوية** | `NOT DONE` |
| B21 | تعدادُ الحسابات عند التسجيل | **قرارُك** — §٤ |
| B22 | سلسلةُ تجزئةٍ لسجلّ التدقيق | خارج ما طُلب — §٥ |
| B23 | **اختبارُ ٤١٣ متقطّع** | سقط مرّةً ومرّ في تاليه. السبب: الخادم يردّ ويغلق قبل أن يفرغ العميلُ من إرسال ١٫١ م.ب، فيرى قطعًا لا ردًّا. **سلوكٌ سابقٌ لـP1.2** (قرارُ P0-2: لا يُستنزف جسمٌ ضخم). علاجُه استنزافٌ محدود أو `Expect: 100-continue` — قرارُ بروتوكول |
| B20 | `carousel.py`/`video.py` في الجذر | من P1.1 |

**ولا واحدٌ منها يُحسب PASS.**

---

# ١٣ · قرارٌ خالفتُ فيه ظاهرَ الأمر — يُذكر لا يُخفى

الأمرُ ذكر `user.manage` و`role.manage` أمثلةً، وقال: «لا تفترض أن هذه
القائمة نهائية. افحص النظام الحالي أوّلًا واستخرج permission catalog
مطابقًا للواقع».

ففعلتُ ذلك، وخرجتُ بأسماءَ أدقّ: `user.list` · `user.read` ·
`user.update` · `user_role.read` · `user_role.update` · `audit.list` ·
`subscription.grant`. والسببُ أن هذه تحفظ **التقابل التامّ** بين الصلاحية
والزوج `(مورِد، فعل)` — وهو ما يجعل «عمليةً بلا صلاحية» مستحيلةً بنيويًّا
لا محفوظةً بانتباه. و`manage` اسمٌ يجمع القراءةَ والكتابة في كلمة، وكان
سيكسر ذلك.

وكذلك **لم أُنشئ `system.manage`**: لا عمليةَ قائمةً تحتاجها اليوم.

---

# ١٤ · التالي

**P1.2 انتهت. P1.3 لم تبدأ ولن تبدأ إلا بأمرك.**

وما أُخذ من P1.3 هو `request_id` وحده، بحدوده المكتوبة في §٥: معرّفٌ لكل
طلبٍ يُكتب في سطر التدقيق. **ولا سجلٌّ مهيكل، ولا مقاييس، ولا تتبّعٌ
موزَّع، ولا ترويسةُ ردٍّ جديدة.**
