# memory/BUGS.md   (المفتوحة فقط)

لا عطبَ برمجيٌّ مفتوحٌ معلومٌ في الخادم أو العميل.

وما يلي **ليس عطبًا** وإنّما نقصُ بيئةٍ أو إذنٍ — لئلّا يُعاد تشخيصُه:

## ENV-01 — `tests.py` و`QUALITY_GATE` حمراوان في CI · ليس عطبًا
السبب: يحتاجان قاعدةَ محتوًى ١٦٥ م.ب لا تدخل git (`raw/` و`falah.db` خارجها،
والأنبوبُ يرفض ملفًّا فوق ٢ م.ب).
**لا يُصلَح** بتعطيل اختبارٍ ولا بتخطٍّ صامتٍ ولا بحذف البوّابة. الحلُّ الوحيد
المقبول: إتاحةُ قاعدة المحتوى للأنبوب.
أمّا وظيفةُ `flutter` الحمراء فتلك **عطبٌ حقيقيّ** يُحقَّق فيه.

## ENV-02 — `authz_test` · `rbac_test` · `leak_test` تتعثّر محلّيًّا · ليس عطبًا
مرحلتاها الساكنتان تنجحان (٩٩ فحصًا · صفر ساقط)، والمرحلةُ الحيّة تحتاج قاعدةَ
المحتوى. وفي CI تنجح على خادمٍ حيّ.
