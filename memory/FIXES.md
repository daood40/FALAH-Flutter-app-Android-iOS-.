# memory/FIXES.md   (الأحدث في الأعلى)

## 2026-09-09 — BUG-001 رقمٌ خارجَ نطاق SQLite يُخرج ٥٠٠
العَرَض: `GET /card?surah=<40 خانة>` يردّ `500 خطأ داخلي` لأيِّ زائرٍ بلا جلسة.
السبب الجذري: أعدادُ بايثون بلا سقف، فـ`int("9"*40)` تنجح ثم تفيض عند ربطها
بـSQLite. و`OverflowError` ليست من `ValueError`، فتسقط إلى المعالج العامّ.
الحل: أُضيفت إلى معالجَي الطلب الفاسد. الملف: `app.py:464` **و**`api.py:69`
· commit: cf63fbb
الوقاية: **معالجا أخطاءٍ اثنان بنفس المنطق** — أُصلح `app.py` أوّلًا ولم يتغيّر
شيء، لأنّ المسار الحيَّ يمرّ بـ`api.py`. فكلُّ تعديلٍ على معالجة الأخطاء يُطبَّق
عليهما معًا، **ويُعاد التحقّق على خادمٍ حيّ** لا باستنتاج. راجع
`qa/rounds/2026-09-09-card-api.md`.

## 2026-09-09 — فاحصُ الحقوق كان يعِد بأكثرَ ممّا يفعل
العَرَض: لا شيءَ ظاهر — البطاقةُ تُصدَّر وكلُّ الفواحص خضراء.
السبب الجذري: الفاحصُ «حالة ترخيص كلّ مصدر مسجَّلة» يقرأ `ctx["license_status"]`
وهو ترخيصُ المصدر الأساسيِّ وحدَه. فتفسيرٌ أو ترجمةٌ أو شرحٌ من مصدرٍ آخرَ
يُطبع على البطاقة ولا يُسأل عن حقّه — وهي مواضعُ الخطر أصلًا.
الحل: `contributors` في البطاقة، والفاحصُ يقرؤها ويُسمّي الناقص.
الملف: `falah/cards.py` · `falah/verify.py` · commit: 9031b46
الوقاية: الثابت `EVERY_CONTRIBUTING_SOURCE_IS_CHECKED` (80e93c5).

## 2026-09-09 — الحالةُ الافتراضيّة للتراخيص كانت غيرَ آمنة
العَرَض: لا شيءَ ظاهر.
السبب الجذري: المصادرُ التسعةُ تُبنى `enabled=1` وحالُ ترخيصها جميعًا «يحتاج
مراجعة» أو «يحتاج إذنًا». فالحزمةُ تُشحن بمادّةٍ لم يُحسم حقُّ عرضها.
الحل: `LICENCE_CLEARED` — والمُطفأُ هو الأصل. الملف: `build.py` · commit: 2aab461
الوقاية: الثابت `LICENCE_CLEARED_IS_EXPLICIT` — مثبَّتٌ بالاسم لا بالعدد.

## 2026-09-09 — إطفاءُ المصدر كان وهمًا
العَرَض: يُطفأ مصدرٌ محفوظُ الحقوق فيختفي من قائمة الاختيار — **ويبقى نصُّه
يخرج على البطاقة**.
السبب الجذري: `content_routes.py` يشترط `enabled=1` حين يعرض القائمة،
و`cards.py` لا يشترطه حين يبني البطاقة. ومعه `"source_registered": True`
مكتوبةٌ ثابتةً لبطاقة الموسوعة فيمرّ فاحصُ «المصدر مُدرج» لها دائمًا.
الحل: `s.enabled=1` في الوصلات الثلاث، وقراءةُ الحال بدل تثبيتها.
الملف: `falah/cards.py` · commit: 015a4f2
الوقاية: الثابت `DISABLED_SOURCE_NEVER_SERVES_TEXT` — شكلٌ لا سلوك، فيسقط
لحظةَ تُكتب وصلةٌ جديدةٌ بلا شرط.

## 2026-09-09 — عمى فحص الأنواع في موضعين
السبب الجذري: إعادةُ ربط `ar` في `build.py` (نصٌّ ثم قاموس) تُعمي mypy عن كتلةٍ
كاملة · و`CMDS` في `dbsafe.py` بأنواعِ رجوعٍ مختلفةٍ تُستنتج `object`.
الحل: اسمٌ خاصٌّ بالكتلة (`enc_ar`) · نوعٌ مكتوبٌ صراحةً. commit: abd7d49
الوقاية: البوّابةُ نفسُها — كانت 2 PASS فصارت 6. **بلا `# type: ignore`.**
