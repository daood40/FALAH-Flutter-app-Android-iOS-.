# اعتماديات فلاح — أربعٌ لا خامسة، وكلٌّ لها سببٌ مكتوب.
#
# القاعدة: لا تُضاف مكتبةٌ إلا حين يعجز عنها ما في بايثون نفسها. طبقة
# المحتوى كلّها (البناء والفحص والبحث والقواعد والخادم) تعمل بالمكتبة
# القياسية وحدها — هذه الأربع للتصيير والتحقّق من المتاجر فقط.
#
# الإصدارات مثبَّتةٌ لا مفتوحة: `>=` يعني أن بناء اليوم غير بناء الغد،
# وأن عطبًا يظهر في الإنتاج ولا يظهر عندك. الترقية قرارٌ يُختبر لا حدثٌ يقع.
#
#   pip install -r requirements.txt && playwright install chromium

# تصيير البطاقات في متصفّحٍ حقيقيّ — لا محاكاةَ للخطوط العربية ولا للتشكيل
playwright==1.56.0

# التحقّق من إيصالات المتاجر: توقيع ES256 لآبل، وسلسلة شهادات x5c،
# وRS256 لحساب خدمة جوجل. لا يُشتقّ حقٌّ من إيصالٍ لم يثبت توقيعه.
cryptography==46.0.7

# ffmpeg مجموعًا في عجلة — يُجنّب اعتمادًا على ما هو مثبَّتٌ في النظام
imageio_ffmpeg==0.6.0

# فحص الصور المصدَّرة في audit.py (الأبعاد والقنوات) — للفحص لا للإنتاج
pillow==12.2.0

# ─────────── أدوات التطوير (اختيارية) ───────────
# pip install ruff==0.14.6 mypy==1.13.0
#
# ─────────── ثغرات الاعتماديات ───────────
# لا يوجد `pip-audit` في هذه البيئة، فلم يُفحَص أيٌّ من هذه الأربع ضدّ
# قواعد الثغرات المعروفة. هذا **نقصٌ مذكورٌ لا مُغطّى**: أضِف إلى خطّ
# التكامل خطوةً `pip install pip-audit && pip-audit -r requirements.txt`
# حين تتاح الشبكة له.
