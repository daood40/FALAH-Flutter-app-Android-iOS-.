# P1 — الخطّة الرئيسة

**الحالة: `PLANNING ONLY` — لا شيفرةَ لـP1.1 حتى تُراجَع هذه الخطّة.**

> **RELEASE BLOCKERS** — تبقى ظاهرةً في كل تقرير، ولا تُعدّ PASS:
> ① `docker build` لم يُنفَّذ · ② GitHub Actions لم يدُر ·
> ③ الهجرة ٠٠٣ لم تُطبَّق على الإنتاج.
> **تطويرُ P1 مسموح · النشرُ إلى الإنتاج ممنوع حتى تُغلق الثلاثة.**

---

# المبدأ الحاكم

P1 ليست «إضافةَ ميزات». هي **ثلاثةُ أشياء بهذا الترتيب**:

1. **جعلُ الأمان بنيويًّا لا تذكُّريًّا.** اليوم كلُّ مسارٍ جديد يجب أن
   يتذكّر كاتبُه فحصَ الملكية والحصّة والحدّ. غدًا يجب أن يكون النسيانُ
   **مستحيلًا**، لا مكروهًا.
2. **أن نرى ما يجري.** لا مراقبةَ اليوم. ميزةٌ لا تُرى لا تُدار.
3. **ثم — وبعدهما فقط — ما يطلبه المستخدم.**

من بنى ميزةً قبل ①و② بنى على أساسٍ يتّسع خرقُه مع كل إضافة.

---

# المراحل

| | المرحلة | لماذا الآن | يعتمد على |
|---|---|---|---|
| **P1.1** | التوجيه المُصرَّح — الأمان بنيويًّا | كلُّ ما بعدها يُبنى عليها | — |
| **P1.2** | الأدوار وسجلُّ التدقيق | المفتاحُ المشترك لا يقول مَن فعل | P1.1 |
| **P1.3** | المراقبة المهيكلة | لنعرف قبل الشكوى | P1.1 |
| **P1.4** | CSP · HSTS · ترويسات | ثغراتٌ مفتوحة مذكورةٌ منذ التدقيق | P1.1 |
| **P1.5** | الحذف المنطقيّ والاسترجاع | استرجاعُ خطأِ النقرة | P1.2 |
| **P1.6** | كنسُ الصادرات وحياةُ الملفّات | التخزينُ يتراكم بلا حدّ | P1.3 |
| **P1.7** | لوحةُ الإدارة والمراجعة | مراجعةُ العلماء تحتاج مكانًا | P1.2 · P1.3 |
| **P1.8** | i18n — فصلُ النصّ عن الشيفرة | التوسّعُ لغويًّا | P1.1 |

**ولا تبدأ مرحلةٌ قبل أن تجتاز سابقتُها `make gate` كاملةً.**

---

# P1.1 · التوجيه المُصرَّح

### Goal
أن يصير فحصُ **المصادقة والملكية والحصّة وحدِّ المعدّل والتحقّق من المدخل**
جزءًا من *تعريف* المسار لا سطورًا يتذكّرها كاتبُه.

### Requirements
- جدولُ مساراتٍ يُصرَّح فيه لكل مسار: الطريقة · الصلاحية · المالك · الحصّة ·
  الحدّ · مخطَّطُ المدخل.
- الموزِّع يطبّق المُصرَّح **قبل** استدعاء المعالج.
- مسارٌ بلا تصريحٍ **لا يُسجَّل** — الافتراضيّ الرفضُ لا القبول.
- `api.do_GET` (٣٠٨ سطرًا) و`app.app_post` (١٩٨) تصيران جدولًا + معالجاتٍ صغيرة.

### Architecture
```
ROUTES = {
  "/app/export": Route(method="POST", auth=SESSION, csrf=True,
                       owns="project", quota=("cards", n_items),
                       rate="export", schema={"project": int},
                       handler=export),
  …
}
dispatch(): يطابق → يطبّق المُصرَّح → يستدعي المعالج
```
**ما لا يتغيّر:** المنطقُ داخل كل معالج ينتقل **كما هو**. لا إعادةَ كتابة.

### Database changes
**لا شيء.**

### API changes
**لا شيء ظاهرًا.** المسارات والردود ورموزُ الحالة كما هي — والعقدُ
(`api_contract.py --check`) يحرس ذلك: أيُّ انحرافٍ يُسقط البوّابة.

### Security impact
**موجَبٌ كبير، وهو الغرض.** الافتراضيّ يصير الرفض. ويُضاف اختبارٌ يمشي
على جدول المسارات كلِّه ويتحقّق أن **كلَّ** مسار كتابةٍ يُصرِّح بمالكٍ
وحصّةٍ أو يُعلن صراحةً أنه لا يحتاجهما.

### Tests
- المسارُ بلا تصريحٍ لا يُسجَّل (اختبارُ وحدة).
- لكل مسارٍ في الجدول: بلا جلسة → ٤٠١ · جلسةُ غيره → ٤٠٠ · بلا CSRF → ٤٠٣.
- **`isolation_test.py` يمرّ بلا تعديلٍ حرف** — وهو الدليل الحاسم.
- ٩٢٩ بندًا تمرّ كما هي.

### Acceptance criteria
- `api.do_GET` و`app.app_post` كلٌّ منهما **دون ٦٠ سطرًا**.
- عقدُ الـAPI بلا انحراف.
- ٩٢٩ + الجديد، وصفرُ حذف.
- زمنُ `/card` لا يزيد عن ٢٫٥ م.ث (قياسٌ قبل وبعد).

### Rollback plan
مرحلةٌ في commit واحد. `git revert` يعيد الحال. ولا هجرةَ فيها ولا تغييرَ
عقد، فالرجوع بلا أثر.

---

# P1.2 · الأدوار وسجلُّ التدقيق

### Goal
أن يُعرف **مَن** فعل، وأن تُمنح الصلاحيةُ بقدرها.

### Requirements
- `role` على `users`: `user | moderator | admin`.
- الإداريّ يدخل بحسابه ودورِه لا بمفتاحٍ مشترك.
- `FALAH_ADMIN_KEY` يبقى **مسارًا للطوارئ** ويُسجَّل استعمالُه بوضوح.
- سجلُّ تدقيقٍ لكل فعلٍ حسّاس: مَن · ماذا · متى · على مَن · النتيجة.

### Database changes
- هجرة `005_roles_and_audit` — **آمنة**: `ALTER TABLE users ADD COLUMN role`
  بافتراضيّ `'user'`، وجدولُ `audit_log` جديد.
- لا حذفَ ولا إعادةَ بناء. `make migrate-check` قبلها.

### API changes
- `/app/admin/*` جديدةٌ، تشترط `role='admin'` **في الخادم**.
- `/app/me` يضيف `role` (قراءةٌ فقط — لا يُغيَّر من العميل أبدًا).

### Security impact
- **خطِرٌ إن أُخطئ**: ترقيةُ دورٍ من العميل تعني استيلاءً كاملًا.
  الحارس: الدورُ يُقرأ من القاعدة في كل طلب، ولا يُقبل من جسمِ طلبٍ ولا
  كعكةٍ ولا ترويسة. واختبارٌ صريح: مستخدمٌ يحاول ترقية نفسه بكل طريق.

### Tests
- `user` → كلُّ `/app/admin/*` = ٤٠٣.
- محاولةُ ترقيةِ النفس عبر `profile` و`register` وجسمِ الطلب = تُرفض.
- كلُّ فعلٍ إداريّ يترك سطرًا في `audit_log` باسم فاعله.
- **إضافةٌ لـ`isolation_test.py`**: `moderator` لا يبلغ ما لا يخصّه.

### Acceptance criteria
هجرةٌ آمنةٌ مجرَّبةٌ على نسخةٍ واقعية · صفرُ تغييرٍ في سلوك المستخدم العاديّ ·
`isolation_test.py` يمرّ.

### Rollback plan
الهجرةُ إضافيّة: العمودُ يبقى ولا يضرّ. الرجوعُ بالشيفرة وحدها.

---

# P1.3 · المراقبة المهيكلة

### Goal
أن يُعرف ما يجري **قبل** أن يشتكي أحد.

### Requirements
- سجلٌّ مهيكل (JSON سطرًا سطرًا): وقت · مستوًى · حدث · مسار · مدّة ·
  `job_id` · `user_id` **رقمًا لا بريدًا**.
- زمنُ كل مهمّة، وسببُ كل فشل، وطولُ الانتظار في الطابور.
- `/metrics` بصيغةٍ نصّية بسيطة (طابور · مهامّ · أخطاء · مدد).

### Database changes
**لا شيء.** المقاييس تُشتقّ من `jobs` و`events` القائمين.

### Security impact
**خطرُ تسريبٍ جديد.** كلُّ حقلٍ يُسجَّل يجب أن يمرّ على قاعدة `leak_test.py`.
لا كلماتِ مرور، ولا رموزَ جلسة، ولا بريدًا، ولا نصَّ محتوى المستخدم.
و`/metrics` **يشترط دورَ الإدارة** — لا يُفتح للعامّة.

### Tests
- **توسيعُ `leak_test.py`**: يُفتَّش السجلُّ المهيكل عن كل ما يُمنع.
- كلُّ حقلٍ حسّاسٍ يُحقن ثم يُبحث عنه في السجلّ.
- `/metrics` بلا دورٍ = ٤٠٣.

### Acceptance criteria
`leak_test.py` موسَّعٌ ويمرّ · لا زيادةَ تُذكر في زمن الطلب (قياسٌ قبل وبعد).

---

# P1.4 · CSP · HSTS · ترويسات

### Goal
إغلاقُ ما بقي مفتوحًا من ترويسات الأمان.

### Requirements
- `Content-Security-Policy` صارمة. والواجهةُ اليوم فيها JS مضمَّن —
  **يُنقل إلى ملفّ أو يُوقَّع بـnonce**، ولا يُرخى CSP ليُقبل المضمَّن.
- `Strict-Transport-Security` من التطبيق أيضًا لا من Caddy وحده.
- `Permissions-Policy` يغلق ما لا يُستعمل.

### Security impact
**موجَبٌ ثمَّ خطِر**: CSP خاطئةٌ تكسر الواجهة صامتةً. الحارس:
`ui_audit.py` يفشل إن لم تعمل الواجهة، ويُضاف فحصُ **أخطاء الطرفية**
(console) لا الشكلِ وحده.

### Tests
- الترويسات حاضرةٌ في كل ردّ.
- `ui_audit.py` يمرّ **بصفر خطأِ طرفية** — وهذا هو الفحص الحقيقيّ.
- محاولةُ حقنٍ مضمَّن تُمنع.

### Rollback plan
CSP في متغيّر بيئة. الإطفاءُ فوريٌّ بلا نشرِ شيفرة.

---

# P1.5 · الحذف المنطقيّ والاسترجاع

### Goal
أن يُسترجَع خطأُ النقرة.

### Requirements
- `deleted_at` على `projects` و`project_items` و`exports`.
- سلّةٌ زمنيّة (٣٠ يومًا) ثم حذفٌ نهائيّ.
- **`users` مستثنًى**: حذفُ الحساب يمحو فعلًا — وعدٌ قطعناه في الواجهة
  ولا يُنقض.

### Database changes
هجرة `006_soft_delete` — **آمنة** (أعمدةٌ وفهارس). والخطر الحقيقيّ في
الشيفرة لا الهجرة: كلُّ استعلامٍ قائم يجب أن يستثني المحذوف، **وأيُّ
استعلامٍ يُنسى يصير تسريبًا لمشروعٍ محذوف**.

### Security impact
اختبارٌ صريح: مشروعٌ محذوفٌ لا يظهر في قائمة، ولا يُقرأ برقمه، ولا
تُنزَّل صادراتُه، ولا يُصدَّر.

### Tests
إضافةٌ لـ`isolation_test.py`: المحذوفُ كالمعدوم في كل طريق.

---

# P1.6 · كنسُ الصادرات

### Goal
ألّا يتراكم التخزين بلا حدّ.

### Requirements
- عمرٌ افتراضيّ للصادرات (٩٠ يومًا) قابلٌ للضبط.
- تحذيرٌ قبل الكنس لا حذفٌ مفاجئ.
- الكنسُ في العامل لا في الطلب.

### Security impact
**خطرُ حذفٍ خاطئ.** الحارس: الكنسُ محدودٌ بـ`exports/<uid>/` بـ`realpath`
— القاعدةُ نفسها التي أُصلح بها حذفُ الحساب. واختبارٌ يزرع ملفًّا خارجه
ويتحقّق أنه يبقى.

---

# P1.7 · لوحةُ الإدارة والمراجعة

### Goal
مكانٌ لمراجعة العلماء ولمتابعة الطابور والمستخدمين.

### Requirements
- طابورُ المراجعة (٢٤٬٤٠٦ مدخلًا محجوبًا) بواجهةٍ تُقرأ وتُوقَّع.
- حالةُ الطابور · المستخدمون · الاشتراكات · سجلُّ التدقيق.
- **قراءةٌ وقرار، لا تعديلُ نصّ.** ولا يُرفع الحجبُ الآليّ بتوقيع —
  القاعدةُ من `review_pack.py` تبقى كما هي.

### Security impact
لوحةُ الإدارة أخطرُ سطحٍ في المشروع. تشترط `role='admin'` **في الخادم**،
وكلُّ فعلٍ فيها في `audit_log`، وكلُّ مدخلٍ يُفحص عدائيًّا.

---

# P1.8 · i18n

### Goal
فصلُ النصّ عن الشيفرة.

### Requirements
- كلُّ رسالةٍ تصير مفتاحًا في `locales/ar.json`.
- العربيةُ تبقى الأصل والافتراضيّ.
- **نصوصُ القرآن والحديث ليست ترجمةً ولا تُمسّ** — هي محتوًى مقفول.

### Tests
- لا رسالةٌ عربيةٌ مكتوبةٌ في شيفرة الإنتاج (فحصٌ ساكن).
- كلُّ مفتاحٍ يُنادى موجودٌ في الملفّ.
- `ui_audit.py` يمرّ بالعربية كما هو.

---

# القواعد النافذة في كل مرحلة

### Definition of Done
```
Implementation ✓   Tests ✓   Security ✓   API contract ✓
UI ✓ (عند الحاجة)  Documentation ✓   Migration ✓ (عند الحاجة)
Performance ✓ (قياسٌ قبل وبعد)   Regression ✓   make gate ✓
```

### قاعدةُ الإيقاف
عند اكتشاف: ثغرةٍ أمنية حرجة · خطرِ فقدِ بيانات · وصولٍ عابرٍ بين
المستخدمين · فسادِ حصّةٍ أو فوترة · فسادِ طابور · تخطّي مصادقة —
**يُوقَف العملُ الحاليّ فورًا**، ويُصلَح، ويُضاف اختبارُ انحدار، ثم تُعاد
البوّابة كاملة.

### الهجرات
مرقَّمة · مجرَّبةٌ على قاعدةٍ جديدة **وعلى نسخةٍ واقعية** · مقارنةُ شكل ·
فحصُ تكامل · اختباراتُ انحدار · **NO DATA LOSS** · ولا هادمةَ تلقائيًّا.

### المسارات الجديدة
مصادقة · تخويل · تحقّقُ مدخل · حدُّ معدّلٍ إن كانت مكلفة · فحصُ ملكية ·
أخطاءٌ آمنة · عقدٌ محدَّث · اختبارات. **ولا شيءَ منها يُعتمد على الواجهة.**

### التصيير
كلُّ ما يُصيّر يمرّ بالطابور. **لا عودةَ إلى دورة الطلب.** ويُحفظ:
التفرّد · حجزُ الحصّة · سياسةُ الإعادة · آلةُ الحالات · المهل · حدودُ
الموارد · عزلُ العامل.

### القياس
`MEASURE → CHANGE → MEASURE`. لا تحسينَ بالحدس. والخطُّ الأساس في
`P1_ARCHITECTURE.md §٣`.

### النطاق
فكرةٌ تظهر أثناء التنفيذ → `P1_BACKLOG.md` ثم يُواصَل العمل الحاليّ.

### Git
مرحلةٌ = commit مستقلّ: `P1.1: …` · `P1.2: …`

---

# ما لا تفعله P1

- ❌ لا Postgres — SQLite يكفي، ولا يُهجَر بلا قياسٍ يوجبه.
- ❌ لا إعادةَ كتابةٍ لـ`verify.py` ولا لبناء القاعدة.
- ❌ لا إخراجَ للتصيير من الطابور.
- ❌ لا إطارَ ويب — المكتبةُ القياسية كافية ومُختبَرة.
- ❌ لا ميزةٍ مستخدمٍ جديدة قبل P1.4.

---

# المطلوب منك قبل P1.1

1. **الموافقةُ على الترتيب** — أم تقدّم شيئًا؟
2. **P1.1 خصوصًا**: هي تفكيكٌ بنيويّ لملفَّين كبيرين. لن تُغيَّر مساراتٌ
   ولا ردود، والعقدُ يحرس ذلك — لكنها أوسعُ تغييرٍ منذ P0-1. أتأذن؟
3. **الأدوار (P1.2)**: أتريد إداريًّا بحسابٍ ودور، أم يبقى المفتاح المشترك؟
