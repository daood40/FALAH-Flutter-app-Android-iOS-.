# P1 — دفتر المؤجَّل

> كلُّ فكرةٍ تظهر أثناء التنفيذ تُكتب هنا **ولا تُنفَّذ**. الغرضُ ألّا
> يتّسع النطاق بالتدريج حتى لا يُعرف أين انتهت المرحلة.
>
> ولا يُنقل بندٌ من هنا إلى مرحلةٍ إلا بقرارٍ صريح منك.

---

## من مرحلة التخطيط — قِيست ولم تُنفَّذ

| # | البند | الأثر المقيس | لماذا أُجِّل |
|---|---|---|---|
| B1 | **خطوط Plex: امتدادٌ يكذب** — ملفّاتُ TrueType باسم `.woff2`، تُقدَّم `font/woff2` ويُعلَن `format('woff2')` | ٦٧٦ ك.ب بدل ~٢٠٠ · كروميوم يقبلها بالشمّ (قِسناه) ومتصفّحٌ أشدُّ يرفضها | تجميلٌ وأداء، لا أمان. يُصلح بتحويلٍ حقيقيّ إلى WOFF2 أو بتصحيح الامتداد والنوع |
| B2 | **`falah-agent.html` = ١٫١٤ م.ب** — سطرٌ واحد فيه ٧٤٥ ك.ب من JSON مضمَّن | صفحةُ العرض العامّة ثقيلة | العملُ بلا اتصال مقصودٌ في أصله. البديل: تقسيمٌ كسول — قرارُ منتج |
| B3 | **`/health` = ٦٢ م.ث** — يعدّ صفوف القاعدة كلَّها | أبطأُ مسارٍ بعشرين ضعفًا | `/healthz` و`/readyz` حلّا الحاجةَ التشغيلية. يبقى `/health` للعرض |
| B4 | **العامل لم يُقَس بأكثر من واحد** | `--scale worker=3` مكتوبٌ غيرُ مجرَّب | يحتاج بيئةَ docker — RELEASE BLOCKER |
| B5 | **SQLite تحت حملٍ مستدام** | مجهولٌ لا مطمئنٌّ له | يحتاج ساعةَ حملٍ متصلة |
| B6 | **قاعدة المحتوى في الصورة** — ٢٥٧ م.ب في كل نشرة | نشرةٌ ثقيلة | حجمٌ خارجيّ بدل النسخ — قرارُ نشر |
| B7 | **آبل وجوجل لم يُجرَّبا على متجرٍ حقيقيّ** | التحقّق مُختبَرٌ بسلسلةٍ مولَّدة | يحتاج حسابَي مطوّر |
| B8 | **SMTP لم يُجرَّب بخادمٍ حقيقيّ** | استعادةُ كلمة المرور وتأكيدُ البريد | يحتاج خادمَ بريد |
| B9 | **نسخٌ خارج الجهاز** | النسخُ على القرص نفسه | قرارُ بنيةٍ تحتية |
| B10 | **بيئة staging** | لا بيئةَ بين التطوير والإنتاج | يحتاج خادمًا |

---

## أفكارُ منتجٍ ظهرت ولم تُطلَب

| # | الفكرة | لماذا هنا لا في الخطّة |
|---|---|---|
| B11 | مفاتيحُ السند والعلامة المكرّرة في الواجهة | سؤالٌ مفتوحٌ منذ P0-1: يضيفان سؤالين إلى تدفّقٍ ثابتٍ صمّمتَه بنفسك — قرارُك أنت |
| B12 | جدولةُ النشر إلى المنصّات | ميزةٌ كبيرة لم تُطلب في P1 |
| B13 | قوالبُ يصنعها المستخدم | يحتاج تصميمًا ومساحةَ تخزين |
| B14 | تصديرٌ جماعيّ (bulk) | موجودٌ في ميزات الخطط ولم يُبنَ |
| B15 | واجهةُ برمجةٍ للمشتركين (`api: true` في خطّة «دار») | معلَنٌ في الخطط وغيرُ مبنيّ — **دَينُ وعدٍ لا دَينُ شيفرة** |

---

## دَينٌ تقنيٌّ معروفٌ ومقبولٌ الآن

| # | البند | لماذا يُقبل |
|---|---|---|
| B16 | `mypy` لا يشمل حزم الفحص | تتلاعب بالأنواع عمدًا لتختبر الحدود |
| B17 | `ruff`: ٧٢٩ بلاغَ أسلوبٍ مُسقَطة | أسلوبُ المشروع مطّردٌ في ١٢ ألف سطر، والإسقاطُ مكتوبٌ بسببه |
| B18 | لا اختبارَ اختراقٍ ولا مراجعةٌ بشرية | خارج ما يفعله كود |
| B19 | لا فحصَ لصورة الحاوية (trivy) | يحتاج docker — RELEASE BLOCKER |
| B21 | **تعدادُ الحسابات عند التسجيل** | **DEFERRED** — انظر البطاقة أسفل الجدول |
| B22 | **سلسلةُ تجزئةٍ لسجلّ التدقيق** | المُشغِّلان يمنعان التعديل من التطبيق ومن مستعمليه، لا من مالك القرص. الكشفُ عن عبثٍ بوصولٍ خام يحتاج سلسلةَ تجزئةٍ أو سجلًّا خارج الجهاز — لم يُطلب في P1.2 |
| B23 | **٤١٣: الخادمُ لا يستنزف الجسمَ المرفوض** | **KNOWN LIMITATION** — انظر البطاقة أسفل الجدول |
| B20 | **`carousel.py` و`video.py` و`render.py` في الجذر** — يستوردها `falah/jobs.py` و`falah/content_routes.py` | ليس صعودًا إلى HTTP (فهي تصييرٌ لا ويب)، لكنّ موضعَها تحت `falah/` أصحّ. ظهر في P1.1 ولم يُنفَّذ: خارج ما طُلب |

---

---

## B21 — تعدادُ الحسابات عند التسجيل

```
B21    = DEFERRED
Reason = SMTP / account recovery infrastructure not verified
Risk   = account enumeration  (registration endpoint only)
Owner  = P1.x authentication hardening
Status = behaviour UNCHANGED in P1.2 — and NOT claimed to be fixed
```

**ما هو بالضبط:** `POST /app/register` ببريدٍ مسجَّلٍ يردّ «هذا البريد
مسجَّل من قبل». فمن جرّب بريدًا عرف أله حسابٌ عندنا أم لا.

**وما ليس كذلك — فُحص ولم يُترك ظنًّا:** `/app/login` و
`/app/password/forgot` و`password/reset` و`verify/confirm` **لا تكشف**:
ردٌّ واحدٌ في كل الحالات، والاشتقاقُ يُحسب حتى لبريدٍ غير مسجَّل فلا يفرّق
الزمن.

**ولماذا أُجِّل ولم يُصلَح في P1.2:**

1. البديلُ الصحيح — قبولٌ صامتٌ ثم رسالةُ بريدٍ تقول «لك حسابٌ بالفعل» —
   **يحتاج SMTP مُتحقَّقًا منه**، وهو `UNVERIFIED` في خطّ الأساس. بناؤه
   على قناةٍ لم تُجرَّب يستبدل تسريبًا صغيرًا **بحسابٍ لا يُنشأ أصلًا**.
2. تغييرُه يغيّر جسمَ ردٍّ ورمزَه على مسارٍ عامّ — كسرُ عقدٍ غيرُ مخطَّط.

**وما يخفّفه اليوم:** حدُّ **٢٠ تسجيلًا في الساعة لكل عنوان**، فالتعدادُ
الواسع محدودٌ لا مفتوح. **وهذا تخفيفٌ لا حلّ.**

**وما يمنع نسيانَ هذا القرار:** ثابتٌ في `invariants.py` يفحص أن السلوك
لم يتغيّر صمتًا وأن هذه البطاقة قائمة. فإن غيّر أحدٌ السلوكَ غدًا سقطت
البوّابةُ حتى تُحدَّث البطاقة.

---

## B23 — ٤١٣ والفرق بين دلالة HTTP وسلوك النقل

```
B23    = KNOWN LIMITATION  (transport, not HTTP semantics)
Owner  = P1.x protocol hardening
Status = behaviour UNCHANGED in P1.2
```

**السؤال الذي طُرح:** أيردّ الخادمُ ٤١٣ فعلًا حسب العقد، أم يغلق الاتصال
قبل أن يُرسل العميلُ الجسمَ كاملًا؟

**الجواب مقيسٌ بمقبسٍ خام، لا مستنتَج:**

| الطبقة | الحكم | الدليل |
|---|---|---|
| **دلالةُ HTTP** | **صحيحة** | ٤١٣ تُرسَل دائمًا بجسمها الموثَّق (`limit_bytes` · `got_bytes`) وبترويسة `Connection: close`. ثابتةٌ عند ١ م.ب+١٠٠ وعند ١٠ م.ب، وبدفعات ٦٤ ك.ب و٤ ك.ب. **٢٠ من ٢٠** |
| **سلوكُ النقل** | **محدود** | الخادم يردّ بعد قراءة الترويسات ولا يستنزف بقيّةَ الجسم، ثم يغلق. **مقيس: أُرسل ٦٥٬٥٣٦ من ١٬١٠٠٬٠٢٦ بايت قبل وصول الردّ** |

**والأثر على العميل:** من يقرأ ما وصله — ولو بعد خطأ كتابة — **يرى ٤١٣
دائمًا**. ومن يعتبر خطأَ الكتابة نهائيًّا فلا يقرأ (وهذا ما يفعله
`urllib`) قد يرى «تعذّر الاتصال» عن خادمٍ ردَّ كما ينبغي: **قِيس ١٠ من
٢٠ في تشغيلٍ واحد**.

**والثابتُ المفروض:** العميلُ يرى **إمّا ٤١٣ صحيحةً وإمّا خطأَ نقل — ولا
رمزَ حالةٍ خاطئًا أبدًا**. مفحوصٌ بعشرين محاولةً في `isolation_test.py`.

**ولماذا لم يُغيَّر في P1.2:** قرارُ P0-2 كان ألّا يُستنزف جسمٌ ضخمٌ
مرفوض (وهو حمايةٌ من الإغراق). وعلاجُه الصحيح استنزافٌ محدود أو
`Expect: 100-continue` — **قرارُ بروتوكولٍ لا تنظيفُ اختبار**.

**وما لم يُفعل:** لم يُخفَّف الاختبار، ولم يُغيَّر السلوك ليخضرَّ فحص.
بل فُصلت الطبقتان في `isolation_test.py` فصار المقيسُ **أدقَّ وأشدّ**:
سبعةُ بنودٍ بدل بندين، وثباتٌ في أربعة تشغيلاتٍ متتالية.


## كيف يُنقل بندٌ من هنا

1. يُذكر في تقرير `P1.x STATUS` بوصفه ظهر.
2. تقول: **انقله**.
3. يُكتب له وصفٌ كاملٌ بحقول المرحلة (Goal · Requirements · … · Rollback).
4. يدخل في مرحلةٍ مرقَّمة.

**ولا يُنفَّذ بندٌ من هنا في أثناء مرحلةٍ أخرى** — ولو بدا صغيرًا.
