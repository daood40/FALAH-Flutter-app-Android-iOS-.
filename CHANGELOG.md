# سِجلُّ التغييرات

يتبع [Keep a Changelog](https://keepachangelog.com/ar/1.1.0/).
والإصدارُ يُرقَّم [دلاليًّا](https://semver.org/lang/ar/).

## [غير مُصدَر]

### أُضيف
- عميلُ Flutter كاملًا: تصفّحُ القرآن والحديث · المصادر · الوكيل ·
  الجدولة · النشر · المشاريع · التصدير · الحساب · الخطط
- مسارات النشر (`/app/publish/*`) وصلاحيّاتُها
- الجدولةُ في الخادم بمناطقَ زمنيّةٍ صحيحةٍ عبر التوقيت الصيفيّ
- المراقبة: سجلٌّ مبنيٌّ ومقاييسُ موسومةٌ خلف صلاحيّة
- `/privacy` و`/terms` صفحتين عامّتين تُصيَّران من Markdown واحد
- خدمةُ نسخٍ احتياطيٍّ يوميّة مع فحصِ صحّةٍ يكشف خدمةً «تعمل» ولا تنسخ
- `verify_image.py` — الصورةُ تتحقّق من نفسها وقت البناء
- `certs/` — بناءٌ خلف وسيطٍ يعترض TLS، بلا تخفيفِ تحقّق

### أُصلح
- **حذفُ الحساب كان يُبقي رمزَ بوتِ تلغرام مغلَّفًا** وإيصالاتٍ واشتراكاتٍ
  وجدولةً ورموزًا وحصصًا. صار ١٦ جدولًا بالاسم بلا `except`
- **نسخةُ الإصدار كانت توقَّع بمفتاح التصحيح.** صار مستحيلًا
- **`docker build` كان يسقط**: الصورةُ تشغّل اختباراتِ المستودع داخلَها
- **صلاحيّةُ الإنترنت غائبةٌ عن نسخة الإصدار** — تُفتح ولا تصل الخادم
- **عقدُ المحوِّل**: `checks_now` و`card.title` و`state`
- `ApiClient` كان يرفض المصفوفةَ في جذر الردّ
- `JobConflict` على الجدولة · «مرّةً واحدة» كانت تتكرّر
- ٩ اختباراتٍ حيّةٍ كانت تتخطّى نفسَها ويُطبع «All tests passed»

### الأمان
- ١٦ ثابتًا أمنيًّا مسمًّى يُفحص في كلِّ بناء
- إذنٌ ببوّابتين · المالكُ من القاعدة لا من الطلب · «ليس لك» ≡ «غير موجود»
- الأسرارُ **تُحذف** من السجلّ حذفًا لا تُنجَّم
- سجلُّ تدقيقٍ لا يُعدَّل (مشغِّلاتٌ في القاعدة)
