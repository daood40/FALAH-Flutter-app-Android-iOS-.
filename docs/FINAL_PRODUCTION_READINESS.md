# FINAL PRODUCTION READINESS REPORT — FINAL

**فَلاح** · ٢٠٢٦-٠٩-٠٦ · الالتزام `d47524a`

```
CODE COMPLETE            ✅
BUILD COMPLETE           ✅   (Android · و iOS = UNVERIFIED بلا macOS)
TEST COMPLETE            ✅   FAIL = 0 · SKIPPED = 0
DOCUMENTATION COMPLETE   ✅
DEPLOYMENT READY         ✅   (الشيفرةُ جاهزة · التشغيلُ ينتظر خادمًا)
OWNER CREDENTIALS PENDING ⏳
```

> **القاعدةُ الحاكمة:** لا يُكتب PASS لما لم يُشغَّل. وكلُّ رقمٍ أدناه من
> تشغيلٍ فعليٍّ في هذه الجلسة.

---

## ١ · Executive Summary

**البوّابة: `PASS 22 · FAIL 0 · BLOCKED 0`.**

كلُّ ما يُصلَح برمجيًّا أُصلح. والباقي **حصرًا** ما يحتاج حسابك أو مفتاحك أو
خادمك أو جهاز macOS — لا نقصَ في الشيفرة.

وأهمُّ ما حدث بعد التقرير السابق أنّ ثلاثةَ عيوبٍ **حقيقيّةٍ** ظهرت، وكلُّها
من صنفٍ واحد: **شيءٌ يبدو تامًّا وليس كذلك.**

1. حزمةُ إصدارٍ اسمُها `app-release.aab` وهي موقَّعةٌ بمفتاح تصحيح.
2. حذفُ حسابٍ ينجح ظاهرًا ويُبقي **رمزَ بوتِ تلغرام** لمن ظنّ أنه انصرف.
3. صورةٌ تقلع سليمةً وتردّ **٤٠٤** على `/privacy` — أي أنّ مراجعةَ المتجر
   كانت سترفض التطبيق بعد كلِّ هذا العمل.

ولم يظهر أيٌّ منها بقراءة شيفرة. ظهرت بالتشغيل: بناءٌ فعليّ، واختبارٌ يقرأ
جداولَ القاعدة، ودخانُ حاويةٍ حقيقيّة.

---

## ٢ · ما بُني فعليًّا

### الخادم — ٣٣ وحدةً في `falah/`

مصادقةٌ وجلساتٌ بكعكةٍ HttpOnly · إذنٌ ببوّابتين (٤٨ صلاحيّةً ↔ ٤٨ سياسة) ·
سجلُّ تدقيقٍ لا يُعدَّل · قرآنٌ وحديثٌ بفحصٍ ٢٥/٢٥ وإسنادٍ موثَّق · تصييرُ
بطاقاتٍ ومقاطع · طابورٌ وعاملٌ مستقلّ · حقوقٌ وحصصٌ وخطط · **الجدولة** ·
**النشر** · **المراقبة** · **الوثائقُ القانونيّةُ صفحاتٍ عامّة** · هجراتٌ
٠٠١–٠٠٧ بحارسٍ للهادمة.

### عميل Flutter — ٤٠ ملفَّ Dart · ١٢ شاشة

| الشاشة | الحال |
|---|---|
| بداية · دخول · تسجيل | كانت |
| رئيسيّة · مشروع · خطط · ملفٌّ شخصيّ · إعدادات | كانت |
| **تصفّحُ القرآن والحديث** | **بُنيت الآن** |
| **المصادرُ وحالُ تراخيصها** | **بُنيت الآن** |
| **الوكيل** | **بُنيت الآن** |
| **الجدولة** | **بُنيت الآن** |
| **النشر** | **بُنيت الآن** |

بنيةٌ: `sealed Failure` (١١ نوعًا) · `sealed Result<T>` · dio بمهلٍ وإلغاءٍ
وتصنيفٍ لكلِّ رمزِ حالة · جرّةُ كعكاتٍ على القرص · GoRouter بحرّاسٍ حقيقيّين ·
Riverpod · سمتان · RTL.

### البنية

`Dockerfile` (+`certs/` للشبكات التي تعترض TLS) · `docker-compose` بخمس
خدماتٍ فيها **النسخُ الاحتياطيُّ خدمةً** · `Caddyfile` · خطُّ تكاملٍ من ٥
وظائف · بوّابةٌ من ٢٢ خطوة · `verify_image.py`.

---

## ٣ · ما أُصلح بعد التقرير السابق

| # | العيب | كيف ظهر |
|---|---|---|
| ١ | **حزمةُ إنتاجٍ موقَّعةٌ بمفتاح تصحيح.** الوسمُ `-debugsigned` كان صدقًا ولا يكفي: يُقرأ لمن نظر، ومن لم ينظر رفع | مراجعةُ ما بنيناه |
| ٢ | **حذفُ الحساب يُبقي `publish_accounts` وفيه رمزُ تلغرام مغلَّفًا**، وإيصالاتٍ واشتراكاتٍ وجدولةً ورموزًا وحصصًا وبريدًا في مفاتيح الكبح. السببُ `try/except pass` على قائمةٍ من خمسةِ جداول | كتابةُ سياسة الخصوصيّة أوجبت قراءةَ ما يُحذف فعلًا |
| ٣ | **`/privacy` و`/terms` ٤٠٤ في الصورة** — `.dockerignore` يستبعد `docs/` | **دخانُ الحاوية** |
| ٤ | **النشرُ وحدةٌ بلا مسارات** — مبنيٌّ ومُختبَرٌ ولا شيءَ يبلغه | جردُ `routing.py` |
| ٥ | **`ApiClient` يرفض المصفوفةَ في جذر الردّ** — كان سيُفرغ كلَّ شاشةِ تصفّح | بناءُ طبقة البيانات |
| ٦ | شكلُ الوكيل: `progress` كائنٌ لا عدد، و`question` كائنٌ فيه `id`/`why`/`options` | ردٌّ حيّ |
| ٧ | مسارُ الفصل بلا `fields=` فلم يصل المعرِّفُ إلى الحارس | تجربةٌ حيّة (٤٠٠) |
| ٨ | `/tmp` بمسارٍ ثابت (S108) | `ruff` |
| ٩ | `COPY deploy/` كان سيُدخل `.env` في طبقةٍ تُقرأ بـ`docker history` | مراجعةُ إصلاحٍ آخر |

**ولم يُحذف اختبارٌ، ولم يُضعَّف فاحص، ولم يُستثنَ ملفّ.**

وأمسك **ثابتٌ أمنيٌّ** و**عقدُ الـAPI** و**كتالوجُ الصلاحيّات** تغييراتِنا
فأسقطوا البناءَ حتى سُجّلت بالاسم — وهذا عملُهم.

---

## ٤ · ما اختُبر · النتائجُ الفعليّة

### الخادم — ١٤٦٦ فحصًا · صفرٌ ساقط

| المجموعة | العدد |
|---|---:|
| `tests.py` وحدةٌ وتكامل | ٦١٨ |
| `rbac_test.py` أدوارٌ وتدقيق | ١٤٦ |
| `failure_test.py` كسرٌ متعمَّد | ٩٦ |
| `authz_test.py` طبقةُ الإذن | ٨٦ |
| `isolation_test.py` حدودُ المستخدم | ٨٢ |
| `leak_test.py` تسريبُ الأخطاء | ٥٥ |
| `sched_test.py` الجدولة | ٥٢ |
| `audit.py` عبر الشبكة | ٤٩ |
| `obs_test.py` المراقبة | ٤٧ |
| `pub_test.py` النشر | ٣٩ |
| `cors_test.py` حدودُ الأصل | ٣٦ |
| `invariants.py` ثوابتُ مسمّاة | ١٦ |
| `dbsafe.py` نسخٌ واسترجاع | ١٢ |
| `verify_image.py` تحقُّقُ الصورة | ١١ |
| `ui_audit.py` واجهةٌ في متصفّح | ✅ |

### العميل — ٧٧ اختبارًا · **٠ متخطًّى** (كانت ٤٤)

```
flutter analyze --fatal-infos        → No issues found!
dart format --set-exit-if-changed    → 0 changed
flutter test --dart-define=FALAH_API=… → All tests passed!
حارسُ التخطّي (JSON)                  → اختبارات: 77 · متخطًّى: 0
```

### رحلةُ المستخدم — ٢٥ خطوةً على خادمٍ حيّ

زائرٌ ← تسجيلٌ ← **نجاةُ الجلسة من إغلاق التطبيق** ← حقوقٌ ← بحثُ قرآنٍ ←
بحثُ حديثٍ بحكمه ← مصادرُ ← مشروعٌ ← إضافةٌ بالمرجع ← **٢٥/٢٥** ← رفضُ
المفقود ← رفضُ المكرَّر ← تصديرٌ يكتمل ← وكيلٌ يسأل ← إجابةٌ تتقدّم ←
إجاباتٌ ناقصةٌ تُرفض ← جدولةٌ بمنطقةٍ زمنيّة ← تعليقٌ ← حذفٌ ← منصّاتٌ
بحالتها ← رفضُ غيرِ المنفَّذ ← حذفُ مشروعٍ ← منعُ الإداريّ ← خروجٌ ← ٤٠١ ←
تصفّحٌ عامٌّ يبقى متاحًا.

**كلُّها نجحت.**

### الحاوية

```
docker build            → PASS من المستودع بلا تعديل
verify_image.py         → 11 فحصًا: 6236 آية · 114 سورة · 37537 حديثًا
                          · فهرسُ بحثٍ مبنيّ · 9 مصادر · يقلع · /privacy 200
docker run              → healthz live · readyz ready · privacy 200 · terms 200
healthcheck             → healthy
الحجم                    → 2.92GB
```

### النسخُ والاسترجاع — ١٢ فحصًا

بصمةٌ تطابق · `integrity_check` · `foreign_key_check` · **الأعدادُ جدولًا
جدولًا** · **والتطبيقُ أقلع على القاعدة المسترجَعة وخدم طلبًا فعليًّا**.

---

## ٥ · Flutter

**✅ PASS.** ٤٠ ملفًّا · ١٢ شاشة · معماريّةٌ نظيفةٌ بطبقات · تحليلٌ نظيفٌ
بـ`--fatal-infos` · ٧٧ اختبارًا بلا تخطٍّ · RTL · سمتان.

**والناقصُ عمدًا:** التخزينُ دون اتّصال (لا drift/isar) — قرارُ منتَجٍ لا
عيبُ تنفيذ، ويُذكر MISSING لا UNVERIFIED.

## ٦ · Android

**✅ PASS.** يُبنى ويعمل ويُثبَّت. `minSdk 24` · `targetSdk 36` ·
صلاحيّةُ الإنترنت **مُتحقَّقٌ منها في الحزمة نفسِها**.

## ٧ · APK

```
falah_app/build/app/outputs/flutter-apk/app-staging-release.apk
54.4 م.ب · com.falah.app.staging · 1.0.0-staging · «فَلاح (تجريبيّ)»
```

**✅ جاهزٌ للتجربة على جهازك.** 🚫 ولا يُرفع للمتجر — معرِّفُه مختلفٌ عمدًا،
فلا يمكن رفعُه مكانَ الإنتاج ولو أُريد.

## ٨ · AAB

**⏳ ينتظر مفتاحَك — ولا يُبنى بدونه.**
`flutter build appbundle --flavor production` **يفشل صراحةً** بلا مفتاح
رفعٍ برسالةٍ تقول ما ينقص وأين يُقرأ عنه.

**ومُتحقَّقٌ من المسارين:** بلا مفتاحٍ فشل كما ينبغي؛ وبمفتاحٍ مؤقّتٍ وُلّد في
`/tmp` وأُتلف فورًا **بُنيت AAB موقَّعةٌ فعلًا** (فُحص وجودُ
`META-INF/*.RSA` — البناءُ ينجح بحزمةٍ غيرِ موقَّعة).

## ٩ · Android Signing

**✅ READY FOR OWNER CREDENTIALS** — لا `TODO`.

| | التوقيع | المعرِّف |
|---|---|---|
| production + مفتاح | مفتاحُ الرفع | `com.falah.app` |
| production بلا مفتاح | **يفشل البناء** | — |
| staging | مفتاحُ تصحيح | `com.falah.app.staging` |

خطُّ التكامل يبني التجربةَ دائمًا، والمتجرَ عند وجود الأسرار، ويمحو المفتاحَ
`if: always()`. والتوثيق: `RELEASE_SIGNING.md` — بما فيه التوليدُ من هاتفٍ
بلا حاسوب.

## ١٠ · iOS

```
IOS PROJECT = READY · IOS BUILD = UNVERIFIED
السبب = macOS + Xcode + اعتماداتُ توقيع
```

أُعدّ: المعرِّفُ `com.falah.app` (كان `com.falah.falahApp`) · الاسمُ `فَلاح` ·
`ar` لغةً · إعلانُ التعمية · ATS تمنع غيرَ المشفَّر · اتّجاهٌ رأسيّ.
**ولم يُبنَ أرشيفٌ ولم يعمل على جهاز.** والأيقونةُ تنتظر أصلًا منك.

## ١١ · Backend — ✅ PASS

١٤٦٦ فحصًا · صفرٌ ساقط · ١٦ ثابتًا أمنيًّا · تصنيفٌ صريحٌ لكلِّ خطأ · لا
تسريبَ أثرِ نداءٍ ولا مسارِ ملفّ.

## ١٢ · Database — ✅ PASS · ⏳ الهجرة ٠٠٣ للإنتاج

٦٢٣٦ آية · ١١٤ سورة · ٣٧٥٣٧ حديثًا · ٩ مصادرَ موثَّقة. الهجرةُ ٠٠٣ هادمة،
ولا تُطبَّق إلا بـ`make db-safe-migrate` — والحارسُ داخل `migrate.py` نفسِه.

## ١٣ · Security — ✅ PASS فيما اختُبر

بوّابتان للإذن · **المالكُ من القاعدة لا من الطلب** · «ليس لك» ≡ «غير موجود» ·
سجلٌّ لا يُعدَّل · كعكةٌ HttpOnly+Secure+SameSite · حدودُ الأصل · **الأسرارُ
تُحذف من السجلّ حذفًا لا تُنجَّم** · لا سرَّ في صورةٍ ولا حزمةٍ ولا مستودع ·
`pip-audit` صفرُ ثغرات · **حذفُ الحساب لا يُبقي صفًّا في ١٣ جدولًا**.

⏳ لم يُجرَ: اختبارُ اختراقٍ · فحصُ صورة · مراجعةٌ بشريّة.

## ١٤ · Islamic Content Integrity — ✅ PASS

٢٥ فحصًا لكلِّ عنصر · المحجوبُ يُعرض **مع سببه** لا يُخفى · الحكمُ من المصدر
ولا يُملأ فراغُه · التصديرُ ممنوعٌ على مشروعٍ فيه محجوبٌ أو منجرف ·
`/sources` تعرض حالَ كلِّ ترخيص.

## ١٥ · SOURCE_LOCK — ✅ PASS · مُتحقَّقٌ منه في الشيفرة

العميلُ يرسل **المرجعَ** (`{surah, ayah}` أو `{book, no}`) والخادمُ يجلب
النصَّ من قاعدةٍ مفحوصة. **ولا يصل متنٌ من العميل إلى بطاقةٍ بحال.**

## ١٦ · AI — ✅ PASS · **لا نموذجَ لغويًّا**

«الوكيل» سلسلةُ أسئلةٍ محدَّدةٍ سلفًا تنتهي باستعلامٍ من قاعدةٍ محلّيّة.

**واختبارٌ يفحص `falah/agent.py` فلا يجد فيه `urllib` ولا `requests` ولا
`httpx` ولا `socket`** — فالنفيُ محروسٌ لا مكتوبٌ في وثيقة. ولا يخرج شيءٌ
من محتوى المستخدم إلى أيِّ مزوّد.

## ١٧–١٩ · Content Creation · Rendering · Export — ✅ PASS

إنشاءٌ يدويٌّ وبالوكيل · تصييرٌ بكروميوم وffmpeg · طابورٌ ومتابعةٌ بتراجعٍ
أُسّيٍّ وسقفٍ خمسِ دقائقَ وإلغاءٍ حقيقيّ · **مُتحقَّقٌ منه بمهمّةٍ اكتملت**.

## ٢٠ · Scheduling — ✅ PASS

٥٢ فحصًا + واجهة. المنطقةُ تُحسب محلّيًّا ثم تُحوَّل · لا إطلاقَ مرّتين
(قيدُ `UNIQUE` في القاعدة لا قفلٌ في التطبيق) · «مرّةً واحدة» لا تتكرّر.

## ٢١ · Publishing — ✅ PASS بنيةً · ⚠️ منصّةٌ واحدةٌ منفَّذة

| تلغرام | `ready` — اعتمادٌ يُصدره المستخدم من BotFather |
| إنستغرام · يوتيوب · تيك توك | `not_implemented` — تحتاج مراجعةَ منصّةٍ واعتماداتِ تطبيق |

**وهذا ليس نقصَ تنفيذٍ بل صدقَ إعلان.** ولا يُقبل ربطُ حسابٍ لمنصّةٍ غيرِ
منفَّذة: حسابٌ يبدو مربوطًا ولا ينشر أسوأُ من رفضٍ صريح.

## ٢٢ · Monitoring — ✅ PASS داخليًّا

سجلٌّ مبنيٌّ بمعرِّف طلب · مقاييسُ موسومةٌ بمسارٍ مضبوطِ التعدّد · عدٌّ ومجموعٌ
وأقصى لا قائمةٌ تنمو · `/app/admin/metrics` خلف صلاحيّة (٤٠٣ مُختبَر).
⏳ لا تنبيهَ خارجيّ — يُضاف عند النشر.

## ٢٣ · Backup / Restore — ✅ PASS

خدمةٌ تُقلع مع الباقي وتنسخ يوميًّا ٣:٠٠، **ولها فحصُ صحّةٍ يصير
`unhealthy` إن مرّت ٤٨ ساعةً بلا نسخة**. والاسترجاعُ مُتحقَّقٌ منه بإقلاع
التطبيق على النسخة وخدمةِ طلب.

## ٢٤ · Privacy / Terms — ✅ PASS

`docs/PRIVACY_POLICY.md` و`docs/TERMS_OF_SERVICE.md` **مكتوبتان من الشيفرة
لا من قالب**: كلُّ حقلٍ مذكورٍ عمودٌ قائم، وكلُّ نفيٍ مفحوصٌ باختبار.
و`/privacy` و`/terms` صفحتان عامّتان تُصيَّران من الـMarkdown نفسِه — لا
نسختان تفترقان. **مُتحقَّقٌ منهما داخل الحاوية (٢٠٠).**

⏳ ٦ حقولٍ `[يُملأ]` تنتظرك — ولم تُخترع أسماءٌ ولا بريدٌ مكانها.

## ٢٥ · CI/CD — ✅ الشيفرةُ كاملة · ⏳ لم تُشغَّل (لا مستودع)

٥ وظائف: static · tests · build · flutter · quality-gate، والبوّابة تشترطها
كلَّها بالاسم. وفيها **حارسٌ يُسقط البناءَ إن تخطّى اختبارٌ واحد** — لأنّ
«All tests passed» على اختباراتٍ لم تجرِ خضرةٌ كاذبة.

## ٢٦ · Documentation — ✅ PASS

`README` · `API.md` · `PERMISSIONS` · `PRIVACY_POLICY` · `TERMS_OF_SERVICE` ·
`DEPLOYMENT` · `RELEASE_SIGNING` · `ARTIFACTS` · `IOS_READINESS` ·
`LAUNCH_CHECKLIST` · `USER_GUIDE` · `CONTRIBUTING` · `CHANGELOG` ·
`MASTER_AUDIT` · `MASTER_CHECKLIST` · `PRODUCTION_STANDARD`.

## ٢٧ · Deployment Code — ✅ PASS

```
DEPLOYMENT CODE       = PASS
PRODUCTION DEPLOYMENT = BLOCKER: OWNER SERVER/DOMAIN REQUIRED
```

٥ خدماتٍ · فحوصُ حياةٍ وجاهزيّةٍ منفصلةُ المعنى · HTTPS تلقائيّ · نسخٌ
مجدوَل · `.env.example` كامل · تعليماتُ الإقلاع والتراجع والتوسيع.

---

## البوّابات الثلاثون

| # | البوّابة | الحال | الدليل |
|---|---|---|---|
| ٠١ | Architecture | ✅ PASS | طبقاتٌ · اتّجاهُ اعتمادٍ ينزل · `MASTER_AUDIT` |
| ٠٢ | Flutter | ✅ PASS | ٤٠ ملفًّا · ١٢ شاشة · analyze نظيف |
| ٠٣ | UI/UX | ✅ PASS | ٤ حالاتٍ لكلِّ شاشة · RTL · سمتان |
| ٠٤ | Backend | ✅ PASS | ١٤٦٦ فحصًا |
| ٠٥ | Database | ✅ PASS | سلامةٌ + بروفةُ هجرات |
| ٠٦ | Authentication | ✅ PASS | دورةُ الجلسة على خادمٍ حيّ |
| ٠٧ | Security | ✅ PASS¹ | ١٦ ثابتًا + حذفٌ لا يُبقي صفًّا |
| ٠٨ | **Islamic Content** | ✅ PASS | ٢٥/٢٥ · حجبٌ بسبب · إسنادٌ مرئيّ |
| ٠٩ | AI Agent | ✅ PASS | شاشةٌ + اختبارُ «لا نداءَ شبكيّ» |
| ١٠ | Content Creation | ✅ PASS | يدويٌّ + وكيل، مُختبَران حيًّا |
| ١١ | Templates | ✅ PASS | في القاعدة لا في العميل |
| ١٢ | Export / Rendering | ✅ PASS | مهمّةٌ اكتملت فعلًا |
| ١٣ | Scheduling | ✅ PASS | ٥٢ فحصًا + واجهة |
| ١٤ | Publishing | ✅ PASS² | ٣٩ فحصًا + مساراتٌ + واجهة |
| ١٥ | Subscription | ⏳ UNVERIFIED | المنطقُ مُختبَر · **لا إيصالَ حقيقيّ** |
| ١٦ | Quotas | ✅ PASS | حجزٌ عند الوضع |
| ١٧ | Storage | ✅ PASS | حجومٌ تبقى بعد التحديث |
| ١٨ | Performance | ⏳ UNVERIFIED | خطُّ أساسٍ مقيس · **لا اختبارَ حِمل** |
| ١٩ | Offline / Errors | ⚠️ PARTIAL | الأخطاءُ ✅ · **التخزينُ دون اتّصال MISSING** |
| ٢٠ | Accessibility | ⏳ UNVERIFIED | **لم يُدقَّق** — يحتاج أجهزةً وقارئَ شاشة |
| ٢١ | Testing | ✅ PASS | ١٤٦٦ + ٧٧ · ٠ متخطًّى |
| ٢٢ | Android | ✅ PASS | APK مبنيٌّ ومفحوص |
| ٢٣ | iOS | ⏳ UNVERIFIED | مشروعٌ READY · **بناءٌ يحتاج macOS** |
| ٢٤ | CI/CD | ✅ PASS³ | ٥ وظائف · **لم تُشغَّل: لا مستودع** |
| ٢٥ | Documentation | ✅ PASS | ١٦ وثيقة |
| ٢٦ | Deployment | ✅ PASS³ | شيفرةٌ كاملة · **التشغيلُ يحتاج خادمًا** |
| ٢٧ | Backup / Restore | ✅ PASS | ١٢ فحصًا · إقلاعٌ على المسترجَعة |
| ٢٨ | Monitoring | ✅ PASS | ٤٧ فحصًا |
| ٢٩ | Privacy | ✅ PASS | وثيقتان + صفحتان ٢٠٠ في الحاوية |
| ٣٠ | Production Readiness | ⏳ OWNER PENDING | البنودُ ١٥ · ١٨ · ٢٠ · ٢٣ + الاعتمادات |

**✅ ٢٤ · ⏳ ٥ · ⚠️ ١ · ❌ ٠**

¹ ما اختُبر يمرّ؛ اختبارُ الاختراق والفحصُ والمراجعةُ البشريّة خارجيّة.
² البنيةُ والمسارات والواجهةُ تامّة؛ وثلاثُ منصّاتٍ معلَنةٌ غيرَ منفَّذةٍ **بصدق**.
³ الشيفرةُ كاملةٌ ومُتحقَّقٌ منها؛ والتشغيلُ يحتاج مستودعًا/خادمًا.

---

## الاختبارات

```
Total Tests   1543   (خادم 1466 · عميل 77)
Passed        1543
Failed           0
Skipped          0
FAIL = 0
```

**ولا اختبارَ متخطًّى، ولا واحد.** ٩ اختباراتٍ كانت تتخطّى نفسَها بلا خادمٍ
ويُطبع «All tests passed» — فصار الأنبوبُ يُشغّل خادمًا حقيقيًّا ويُسقط
البناءَ إن تخطّى واحد.

---

## EXTERNAL BLOCKERS ONLY

**ما تحتاجه أنت — ولا شيءَ غيره:**

| # | ما | لماذا لا يمكن هنا |
|---|---|---|
| ١ | **مستودع GitHub** | الرمزُ المتاح هنا مقصورٌ على مستودعاتٍ معيّنة (403) |
| ٢ | **مفتاحُ رفع Android** | من ملكه نشر باسمك، ومن أضاعه لم يعد يُحدِّث تطبيقه أبدًا — **ولا يُولَّد في محادثة** |
| ٣ | **حساب Google Play** | ٢٥$ · ليبيا مدعومةٌ للمطوّر وللتاجر بالدولار |
| ٤ | **حساب Apple Developer** | ٩٩$/سنة |
| ٥ | **macOS + Xcode** | بناءُ iOS وتوقيعُه · أو خدمةُ بناءٍ سحابيّة |
| ٦ | **خادم** | ٤ ج.ب ذاكرةً (كروميوم + ffmpeg) |
| ٧ | **نطاق** | الشهادةُ التلقائيّةُ تحتاج DNS يشير إليه |
| ٨ | **مزوّد SMTP** | التحقّقُ من البريد |
| ٩ | **اعتمادات المتاجر** | مقصورةً وقابلةً للسحب — **ولا كلمةَ مرورِ حسابٍ ولا رمزَ تحقّقٍ ثنائيّ إليّ أبدًا** |
| ١٠ | **مراجعةُ المتجر** | خارجةٌ عنّا |
| ١١ | **الحقولُ الستّة `[يُملأ]`** | اسمُك وبريدُك ونطاقُك واختصاصُك القضائيّ |
| ١٢ | **رخصةُ المحتوى** ⚠ | التفسيرُ الميسّر محفوظٌ لمجمّع الملك فهد — إذنٌ خطّيّ أو بديل |
| ١٣ | **مراجعةٌ بشريّةٌ للبطاقات** | نزاهةُ النصّ الشرعيّ لا يُصادَق عليها آليًّا وحدَها |
| ١٤ | **اختبارُ اختراقٍ وفحصُ صورة** | خدمةٌ خارجيّة |

**الترتيبُ التنفيذيُّ كاملًا:** [`LAUNCH_CHECKLIST.md`](LAUNCH_CHECKLIST.md)

---

## كلمةٌ أخيرة

الوعدُ الذي بقي محفوظًا في كلِّ سطرٍ من هذا العمل: **النصُّ الشرعيُّ لا
يُولَّد ولا يُعدَّل.** يُنقل من مصدرٍ مذكور، أو يُحجَب مع ذكر السبب.

والوعدُ الثاني أنّ هذا التقرير لا يقول PASS لما لم يُشغَّل. فالبنودُ الخمسةُ
المعلَّقةُ معلَّقةٌ بأسمائها، ولن تصير خضراءَ حتى تُشغَّل فعلًا.

**وما بقي من العمل ليس شيفرةً — هو مفاتيحُ وحساباتٌ وخادمٌ وقراراتٌ لا
يملكها إلا صاحبُ المشروع.**
