# P1.2 — تقريرُ الإغلاق

```
P1.2 CLOSURE STATUS: PASS

B21:                  DEFERRED · موثَّقٌ بالصيغة المطلوبة · وثابتٌ يمنع نسيانَه
B23:                  RESOLVED AS DIAGNOSIS — دلالةُ HTTP صحيحة · النقلُ محدودٌ ومكتوب
Bootstrap:            PASS — عشرةُ حرّاسٍ مُختبَرةٌ بتشغيلٍ حقيقيّ (٢٦ بندًا)
Migration 005:        PASS — تكافؤُ مخطَّط · لا NULL ولا مجهول · **صفرُ إداريٍّ عرَضيّ**
Permission catalog:   PASS — ٣٨ ⟷ ٣٨ · لا ميتة · لا حسّاسَ بلا تسجيل · مولَّدٌ من الشيفرة
Security invariants:  PASS — ١٦ ثابتًا مسمّى في بوّابةٍ حرجةٍ مستقلّة
Regression:           PASS — ١٬١٤٨ → ١٬١٨٣ (+٣٥) · صفرُ حذف
Gate:                 PASS 17 · FAIL 0 · BLOCKED 1  (بناءُ الصورة — لا عفريت docker)
```

**commit `0bb9ad6`.** ولم تُضَف ميزةٌ واحدة. **P1.3 لم تبدأ.**

---

# ١ · B23 — الجواب، لا التخفيف

سألتَ سؤالًا دقيقًا: **أيردّ الخادم ٤١٣ حسب العقد، أم يغلق قبل أن يُرسل
العميلُ الجسمَ كاملًا؟**

فحصتُه بمقبسٍ خام — لا بـ`urllib` الذي كان يقيس — أكتب الجسمَ على دفعاتٍ
وأقرأ بينها، فأعرف **متى** يصل الردُّ و**بعد كم بايتًا**.

## الجواب

| الطبقة | الحكم | القياس |
|---|---|---|
| **دلالةُ HTTP** | **صحيحة** | `HTTP/1.1 413` بجسمها الموثَّق (`limit_bytes` · `got_bytes`) وبترويسة `Connection: close`. **٢٠ من ٢٠** |
| **سلوكُ النقل** | **محدود** | الخادم يردّ بعد قراءة الترويسات ولا يستنزف بقيّةَ الجسم ثم يغلق. **أُرسل ٦٥٬٥٣٦ من ١٬١٠٠٬٠٢٦ بايت قبل وصول الردّ** |

وثابتةٌ في كل صورة: عند ١ م.ب + ١٠٠ بايت، وعند ١٠ م.ب (عشرةُ أضعاف
الحدّ)، وبدفعات ٦٤ ك.ب و٤ ك.ب. **٤١٣ في كلِّها.**

## فأين كان السقوطُ المتقطّع؟

**في القياس لا في الخادم.** `urllib` يعامل خطأَ الكتابة **خطأً نهائيًّا**
فلا يقرأ الردَّ الذي وصل فعلًا إلى مخزنه. فيقول «تعذّر الاتصال» عن خادمٍ
ردَّ كما ينبغي.

```
عميلٌ يكتب ثم يقرأ رغم خطأ الكتابة (مقبسٌ خام):   ٢٠ × 413
urllib نفسه، الحمولةُ نفسُها:                     ١٠ × 413 · ١٠ × URLError
```

**وهذا فرقٌ بين «الخادمُ مخطئ» و«هذا العميلُ لا يقرأ»** — ولا يجوز الخلط
بينهما، ولا وصفُ الثاني بأنه سقوطُ عقد.

## ما فُعل — وما لم يُفعل

**لم يُخفَّف اختبار، ولم يُغيَّر سلوكٌ ليخضرَّ فحص.**

فُصلت الطبقتان في `isolation_test.py` بدالّةِ `oversized()` تقيس الاثنين
معًا وتفرّق بينهما. فصار المقيسُ **أدقَّ وأشدّ**:

| | قبل | بعد |
|---|---|---|
| بنودُ حدّ الجسم | ٢ | **٧** |
| الحجومُ المفحوصة | ١ | ٣ |
| الدفعاتُ المفحوصة | ١ | ٢ |
| ترويسةُ الإغلاق | لا تُفحص | **تُفحص** |
| «لا يستنزف الجسم» | لا يُقاس | **يُقاس بالبايت** |
| الثبات | ساقطٌ متقطّع | **٤ تشغيلاتٍ متتالية بلا سقوط** |

والثابتُ المفروض على العميل — وهو ما يهمّ فعلًا:

```
✓ عميلٌ لا يقرأ عند خطأ الكتابة يرى إمّا ٤١٣ وإمّا قطعَ نقل
  — ولا رمزَ حالةٍ خاطئًا أبدًا      (عشرون محاولة)
```

## والحدُّ مكتوبٌ بطاقةً

`P1_BACKLOG.md` → `B23 = KNOWN LIMITATION (transport, not HTTP semantics)`
بجدول القياس كاملًا، وسببِ بقائه (قرارُ P0-2: لا يُستنزف جسمٌ ضخمٌ
مرفوض — وهو حمايةٌ من الإغراق)، وعلاجِه الصحيح (استنزافٌ محدود أو
`Expect: 100-continue`) — **قرارُ بروتوكولٍ لا تنظيفُ اختبار**.

وثابتُ بوّابةٍ `B23_413_SEMANTICS_DOCUMENTED` يمنع طيَّ البطاقة أو دمجَ
الطبقتين ثانيةً.

---

# ٢ · B21 — مؤجَّلٌ ومكتوبٌ، لا مُدَّعًى حلُّه

```
B21    = DEFERRED
Reason = SMTP / account recovery infrastructure not verified
Risk   = account enumeration  (registration endpoint only)
Owner  = P1.x authentication hardening
Status = behaviour UNCHANGED in P1.2 — and NOT claimed to be fixed
```

**السلوك لم يُمَسّ.** `/app/register` ببريدٍ مسجَّلٍ ما زال يقول «هذا
البريد مسجَّل من قبل».

**وحدودُ الخطر مفحوصةٌ لا مظنونة:** `/app/login` و`password/forgot` و
`password/reset` و`verify/confirm` **لا تكشف** — ردٌّ واحدٌ في كل الحالات،
والاشتقاقُ يُحسب حتى لبريدٍ غير مسجَّل فلا يفرّق الزمن.

**وما يخفّفه:** ٢٠ تسجيلًا/ساعة لكل عنوان. **وهذا تخفيفٌ لا حلّ** —
مكتوبٌ هكذا في البطاقة.

**وما يمنع نسيانَه:** ثابتٌ `B21_ENUMERATION_DEFERRED_DOCUMENTED` يفحص
ثلاثةَ أشياء معًا: أن البطاقةَ قائمةٌ بحقولها الأربعة، وأن التسجيل ما زال
يكشف (فلو غُيّر صمتًا سقطت البوّابة حتى تُحدَّث البطاقة)، وأن الدخولَ
والاستعادةَ ما زالا لا يكشفان.

---

# ٣ · نطاقُ الإدارة — عقدٌ أمنيٌّ دائم

```
super_admin  ≠  read every user's project
admin        ≠  content visibility
```

ثُبِّت ثابتًا اسمُه `SUPER_ADMIN_DOES_NOT_BYPASS_RESOURCE_SCOPE`، ويفحص
ثلاثةَ أشياء:

1. كلُّ زوجٍ على مورِدٍ من موارد المحتوى (`project` · `project_item` ·
   `job` · `export_file` · `export`) نطاقُه `OWNER` أو `AUTH` — **ولا
   شيءَ أوسع**.
2. `moderator` و`admin` و`super_admin` كلُّهم يُمنعون فعلًا من مورِدٍ
   مالكُه غيرهم — يُنادى `can()` ويُتحقَّق من القرار.
3. **ولا دورَ إداريٍّ يحمل صلاحيةَ محتوًى لا يحملها المستخدم العاديّ**:
   `perms(super_admin) − perms(user)` لا يقع فيها شيءٌ من موارد المحتوى.

وأُضيف `OWNER_SCOPE_ENFORCED` يثبّت **الأزواجَ العشرة بالاسم** لا بعددٍ
أدنى:

```
project.read · project.update · project.delete · project.render
project_item.create · project_item.update · project_item.delete
job.read · job.cancel · export_file.download
```

تضييقُ هذه المجموعة يعني مورِدًا **خرج** من حراسة الملكية، وتوسيعُها يعني
نطاقًا تغيّر. **وكلاهما يجب أن يظهر في الفرق لا أن يمرّ صامتًا.**

## وكيف يُوسَّع مستقبلًا — إن أردتَ

الطريقُ واحد، ومكتوبٌ في `P1_ARCHITECTURE.md`:

1. صلاحيةٌ **جديدةٌ باسمها** (`project.read_any` مثلًا) بنطاقٍ صريح.
2. تحديثُ الثابتِ نفسِه في `invariants.py`.
3. توثيقٌ واختبارٌ وتسجيلُ كلِّ استعمال.

**لا توسيعَ نطاقٍ قائم، ولا استثناءَ في معالِج.**

---

# ٤ · أمرُ الإقلاع — عشرةُ حرّاسٍ مُختبَرة

يُختبر **بتشغيلٍ حقيقيّ** (`subprocess`) لا باستدعاءِ دالّة: الوسائطُ
والرموزُ الراجعة والمخرَجُ كلُّها جزءٌ من عقده مع المشغّل.

| المطلوب | الحال | الدليل |
|---|---|---|
| لا يعمل عبر HTTP | ✔ | لا مسارَ ولا معالِجَ يستورده — مفحوصٌ في المصدر |
| لا يقبل كلمةً ولا رمزًا في الوسائط | ✔ | لا وسيطَ اسمُه `password`/`token`/`secret`/`key`. **والوسائطُ تظهر في `ps` وفي تاريخ الصدفة، فما فيها مكشوف** |
| لا يطبع أسرارًا | ✔ | فُتِّش مخرَجُه عن `pw_hash` · `pw_salt` · `token_hash` · ورمزِ جلسةٍ حقيقيّ |
| يسجّل حدثَ تدقيق | ✔ | `role.changed` بفاعل `system:cli` |
| **يوضح من قام بالعملية** | ✔ | `os_user` · `host` · `via` في `metadata` |
| يرفض مستخدمًا غير موجود | ✔ | رمزٌ ≠ ٠ ورسالةٌ مفهومة |
| يرفض التباسَ الاختيار | ✔ | يشترط صفًّا **واحدًا بالضبط**، ويطبّع البريد (فراغًا وحالةَ حرف) |
| لا يتخطّى حالةَ الهجرات | ✔ | يرفض وهجرةٌ **آمنة** معلّقة. والهادمةُ المؤجَّلة عمدًا (٠٠٣) لا تمنعه — وإلا صار الحارسُ سببًا في تعطيل الإدارة |
| يرفض دورًا غير معروف | ✔ | في `argparse` وفي الشيفرة معًا، **قبل أن يمسّ القاعدة** |
| **idempotent** | ✔ | المنحةُ نفسُها ثانيةً: لا تغيير، **ولا حدثَ ثانٍ في السجلّ** |
| **آمنٌ تحت التزامن** | ✔ | `BEGIN IMMEDIATE` — القراءةُ والتغييرُ والتسجيل صفقةٌ واحدة |

## الاختبارات الصريحة التي طلبتَها

```
✓ يمنح الدورَ الأعلى                                  (grant user super_admin)
✓ ويكتب حدثَ تدقيقٍ واحدًا
✓ **idempotent**: المنحةُ نفسُها ثانيةً لا تغيّر شيئًا   (grant same again)
✓ ولا تكتب حدثًا ثانيًا — السجلُّ يحكي ما جرى لا ما طُلب
✓ يرفض حسابًا لا وجود له برمزٍ غير صفر                 (grant nonexistent)
✓ ويرفض دورًا غير معروفٍ قبل أن يمسّ القاعدة            (grant unknown role)
✓ ثمانيةُ تشغيلاتٍ متزامنةٍ بالدور نفسه — كلُّها تنجح بلا تعارض  (concurrent)
✓ والنتيجةُ دورٌ واحدٌ صحيح
✓ ولا تُكتب ثمانيةُ أحداثٍ لتغييرٍ واحد
✓ والقاعدةُ سليمةٌ بعد التزامن                          (integrity_check = ok)
```

**٢٦ بندًا** في `rbac_test.py`.

---

# ٥ · الهجرة ٠٠٥ — تحقّقٌ موسَّع

## تكافؤُ المخطَّط

```
✓ قاعدةٌ مهاجَرةٌ وقاعدةٌ جديدة تنتهيان إلى الكائنات نفسها   (٤٣ كائنًا)
✓ وأعمدةُ users متطابقةٌ نوعًا وافتراضيًّا
✓ وسجلُّ التدقيق ومُشغِّلاه موجودان في المسارين
```

## البيانات القائمة — على قاعدةٍ فيها ٢٥ حسابًا

```
✓ قاعدةٌ فيها ٢٥ حسابًا قائمًا تُهاجَر بلا فقدِ صفّ
✓ ولا صفَّ بدورٍ NULL                          (٠)
✓ ولا بدورٍ فارغ                               (٠)
✓ ولا دورَ خارج ما تعرفه الشيفرة               ({'user'})
✓ **ولا حسابَ صار مشرفًا ولا إداريًّا ولا أعلى**  (٠ حسابًا)
✓ والكلُّ على أقلِّ الأدوار
✓ والعمودُ NOT NULL فلا يُكتب فيه فراغٌ لاحقًا
✓ وحالةُ الحسابات لم تُمَسّ — الموقوفُ يبقى موقوفًا  (٤ من ٢٥)
✓ والهجرة ٠٠٥ مصنَّفةٌ آمنة — إضافةٌ محضة بلا DROP ولا نقلِ بيانات
```

## والهجرة ٠٠٣

```
⛔ 003 jobs_state_guard  (هادمة)   ← READY · NOT APPLIED TO PRODUCTION
```

**لم تُمَسّ، ولم تُطبَّق، ولن تُطبَّق إلا بـ`make db-safe-migrate` ثم
قرارٍ يدويٍّ منك.**

---

# ٦ · الثوابت الأمنية — ١٦ ثابتًا في بوّابةٍ حرجة

`python3 invariants.py` — بوّابةٌ مستقلّة في `make gate` وفي CI.

**ولماذا ملفٌّ مستقلٌّ وحزمُ الاختبار موجودة؟** لأن الحزمَ تختبر
**السلوك**، وهذه تُثبّت **الشكل**. اختبارُ سلوكٍ يقول «هذا الطلبُ مُنع»؛
وثابتُ شكلٍ يقول «**لا يمكن** أن يُسمح، لأن البنيةَ لا تحتمله». الأولُ
يمرّ اليوم وقد يسقط غدًا بمسارٍ جديد؛ **والثاني يسقط لحظةَ يُكتب المسارُ
الجديد، لا لحظةَ يُستغَلّ.**

| الثابت | مطلوبٌ منك؟ |
|---|---|
| `AUTHZ_DEFAULT_DENY` | ✔ |
| `UNKNOWN_PERMISSION_DENY` | ✔ |
| `UNKNOWN_ROLE_DENY` | ✔ |
| `UNAUTHENTICATED_DENY` | ✔ |
| `OWNER_SCOPE_ENFORCED` | ✔ |
| `SUPER_ADMIN_DOES_NOT_BYPASS_RESOURCE_SCOPE` | ✔ |
| `ROLE_ESCALATION_GUARDED` | ✔ |
| `AUDIT_APPEND_ONLY` | ✔ |
| `AUDIT_NO_SECRETS` | ✔ |
| `AUDIT_UNAUTHORIZED_ACCESS_DENIED` | ✔ |
| **`NEW_OPERATION_WITHOUT_PERMISSION`** | ✔ **وهو الأهمّ** |
| `NO_ROLE_NAME_IN_CONDITION` | أُضيف |
| `AUDIT_CATALOGUE_CLOSED` | أُضيف |
| `PERMISSION_CATALOGUE_DOCUMENTED` | أُضيف |
| `B21_ENUMERATION_DEFERRED_DOCUMENTED` | أُضيف |
| `B23_413_SEMANTICS_DOCUMENTED` | أُضيف |

## `NEW_OPERATION_WITHOUT_PERMISSION` — ماذا يفحص بالضبط

```
٤٤ مسارًا · ٣٨ صلاحية
بلا سياسة=[]   بلا صلاحية=[]   فعلٌ خارج القائمة=[]   مسارٌ بلا إعلان=[]
صلاحيةٌ ميتة=[]   زوجٌ يتيم=[]   صلاحيةٌ لا يبلغها دور=[]
```

سبعةُ شروطٍ في ثابتٍ واحد. **مسارٌ يُضاف غدًا بلا صلاحيةٍ يُسقط
البوّابة** — هذا هو المقصود بـsecure by construction، لا انتباهُ مراجعٍ
بعد ستّة أشهر.

---

# ٧ · كتالوج الصلاحيات

`docs/PERMISSIONS.md` — **مولَّدٌ من الشيفرة** بـ`python3 perms_report.py`،
ولا يُحرَّر باليد. ويسقط في البوّابة إن تقادم.

الأعمدةُ التي طلبتَها: **الصلاحية · المورِد · الفعل · النطاق · أدنى دورٍ
يملكها · أتُسجَّل؟ · حدُّ المعدّل · المسار**.

## نموذجٌ من الجدول

| الصلاحية | المورِد | الفعل | النطاق | أدنى دورٍ | تُسجَّل؟ | حدُّ معدّل | المسار |
|---|---|---|---|---|---|---|---|
| `project.render` | project | render | `OWNER` | `user` | ✔ `export.created` … | `export`/`video` (داخل المعالِج) | `POST /app/export` · `POST /app/video` |
| `user_role.update` | user_role | update | `ANY` | `super_admin` | ✔ `role.changed` | — | `POST /app/admin/users/role` |
| `audit.list` | audit | list | `AUTH` | `moderator` | ✔ `audit.read` | — | `GET /app/admin/audit` |
| `session.create` | session | create | `PUBLIC` | `anonymous` | ✔ `login.success` · `login.failure` | `login` (قبليّ · لكل عنوان) | `POST /app/login` |

**«أدنى دورٍ يملكها»** يكفي عن سرد الأدوار كلِّها: التسلسلُ محسوبٌ بالضمّ،
فما يملكه الأدنى يملكه من فوقه.

**وعمودُ التدقيق دقيقٌ لكلِّ عمليةٍ بعينها لا لكلِّ مورِد** — قولُ
«المشاريعُ تُسجَّل» يوهم أن سردَها يُسجَّل، وهو لا يُسجَّل ولا ينبغي. وما
لا حدثَ له يُكتب «—» صراحةً.

## ما يفحصه آليًّا

```
✓ لا صلاحيةَ ميتة                 (كلُّ صلاحيةٍ لها مسارٌ أو قاعدةٌ عامّة)
✓ ولا عمليةَ بلا صلاحية            (٣٨ ⟷ ٣٨ تقابلًا تامًّا)
✓ ولا صلاحيةَ لا يبلغها دور
✓ ولا عملٌ حسّاسٌ بلا تسجيل        (تغييرُ دورٍ · إيقافُ حساب · حذفُه ·
                                    منحةٌ · دخولٌ · قراءةُ السجلّ)
```

---

# ٨ · الانحدار الكامل

| الحزمة | قبل الإغلاق | بعده | الفرق |
|---|---:|---:|---|
| `tests.py` | ٥٣٨ | **٥٤٦** | +٨ (الهجرة ٠٠٥ الموسَّعة) |
| `audit.py` | ١٢٤ | ١٢٤ | — |
| `ui_audit.py` | ٥٣ | ٥٣ | — |
| `failure_test.py` | ٩٥ | ٩٥ | — |
| `leak_test.py` | ٥٤ | ٥٤ | — |
| `isolation_test.py` | ٧٤ | **٨١** | +٧ (B23) |
| `authz_test.py` | ٨٥ | ٨٥ | — |
| `rbac_test.py` | ١٢٥ | **١٤٥** | +٢٠ (أمرُ الإقلاع) |
| **المجموع** | **١٬١٤٨** | **١٬١٨٣** | **+٣٥** |
| `invariants.py` | — | **١٦ ثابتًا** | جديد |

**صفرُ حذف.**

## البوّابة

```
✓ ثغرات الاعتماديات  ✓ ruff  ✓ mypy  ✓ الأسرار والإعداد  ✓ عقد الـAPI
✓ سلامة القاعدة  ✓ بروفة الهجرات  ✓ الوحدة  ✓ الشامل  ✓ الواجهة
✓ الكسر المتعمَّد  ✓ التسريب  ✓ حدود المستخدم  ✓ طبقة الإذن
✓ الأدوار والتدقيق  ✓ **الثوابت الأمنية**  ✓ إعداد الحاوية
! بناء صورة الحاوية   BLOCKED — عفريت docker غير مشتغل

PASS 17 · FAIL 0 · BLOCKED 1        QUALITY_GATE = PASS
```

## عطبٌ وقع في الإغلاق نفسه — يُقال لا يُطوى

أسقط `security_scan.py` البوّابةَ على قنّاصةِ اختبارٍ كتبتُها حرفيًّا في
`rbac_test.py`:

```
✗ [high] rbac_test.py:35  سرٌّ مكتوبٌ في الشيفرة
```

**والفاحصُ كان محقًّا** — قيمةٌ عالية العشوائية مُسنَدةٌ إلى اسمٍ يشبه
السرّ. والصوابُ ألّا يُضعَّف الفاحصُ ولا يُستثنى الملفّ، بل **ألّا يُكتب
في الشيفرة ما يشبه سرًّا أصلًا**: صارت القنّاصةُ تُشتقّ
(`sha256(b"falah-rbac-canary")`) لا تُكتب. ثابتةٌ بين التشغيلات فيبقى
الفحصُ حاسمًا، وليست سرًّا مكتوبًا.

---

# ٩ · ما بقي مفتوحًا — بلا تجميل

| البند | الحال |
|---|---|
| **بناءُ صورة الحاوية** | `BLOCKED / UNVERIFIED` — لا عفريت docker |
| **GitHub Actions** | `LOCAL VERIFIED / REMOTE UNVERIFIED` |
| **الهجرة 003** | `READY · NOT APPLIED TO PRODUCTION` |
| **آبل · جوجل · SMTP** | `UNVERIFIED` |
| **اختبارُ اختراق · مراجعةٌ بشرية · فحصُ صورة الحاوية** | `NOT DONE` |
| **B21** | `DEFERRED` — موثَّقٌ ومحروسٌ بثابت |
| **B22** سلسلةُ تجزئةٍ للسجلّ | مؤجَّل — الحمايةُ من مالك القرص خارج ما طُلب |
| **B23** | `KNOWN LIMITATION` — مُشخَّصٌ ومكتوبٌ ومحروسٌ بثابت |
| **B20** `carousel.py`/`video.py` في الجذر | من P1.1 |

**ولا واحدٌ منها يُحسب PASS، ولم يُغلقه هذا الإغلاق ولم يدَّعِ.**

---

# ١٠ · التالي

**P1.2 مغلقة. P1.3 لم تبدأ — ولا تخطيطُها.**

أمرُك الأخير كان: «أغلق P1.2 أوّلًا، ثم أعطني تقرير الإغلاق، ثم ننتقل
إلى P1.3 Planning» و**GO FOR P1.2 CLOSURE ONLY**. فوقفتُ هنا.

وحين تأذن، خطّةُ P1.3 تبدأ بوثيقتين لا بشيفرة:
`docs/P1.3_BASELINE.md` و`docs/P1.3_OBSERVABILITY_DESIGN.md`، بمعمارية
ونموذجِ بيانات ومدّةِ حفظٍ وخصوصيةٍ وكلفةِ أداءٍ وضبطِ تعدُّدٍ وسلوكٍ عند
العطب.

وما أُخذ من P1.3 في P1.2 هو `request_id` وحده — ولم يُبنَ عليه شيء.
