# توجيهات المستودع — فَلاح

> لم يكن في المستودع ملفُّ `CLAUDE.md` من قبل، فأُنشئ الآن. والقسمُ الأوّل
> إحالةٌ إلى ما هو مكتوبٌ ومحكَّمٌ أصلًا في `docs/`، لا قواعدُ جديدة.

## قبل أيّ سطر

اقرأ `docs/HANDOFF.md` كاملًا، ثم `docs/TRANSFER_LOG.md`. فيهما قواعدُ
المشروع وحالتُه وعيوبٌ كلّف اكتشافُها ساعات.

وأشدُّها لزومًا:

- **`SOURCE_LOCK`** — لا يُولَّد نصٌّ شرعيٌّ ولا يُعدَّل ولا يُعاد صوغُه
  بذكاءٍ اصطناعيّ: لا آية، ولا حديث، ولا حكمٌ عليه، ولا تخريج، ولا تفسير،
  ولا نسبةٌ إلى عالِم. كلُّ نصٍّ يُنقل من مصدرٍ مسجَّلٍ في جدول `sources`،
  وما لا يُتحقَّق منه يُحجَب مع ذكر السبب.
- **لا خضرةَ كاذبة** — لا `PASS` لما لم يُشغَّل. الحالاتُ المسموحة:
  PASS · PARTIAL · BLOCKED · FAIL · MISSING · UNVERIFIED · BLOCKER.
- **لا تحذف اختبارًا صار مزعجًا** ولا تُضعِف فاحصًا ليمرَّ تغييرُك، ولا
  تُضعِف إعداداتِ الإنتاج لتنجح الاختبارات.
- **الهجرة ٠٠٣ هادمة** — لا تُطبَّق على الإنتاج إلا بـ`make db-safe-migrate`.
- **مفتاحُ التصحيح لا يعود إلى نكهة الإنتاج** بأيّ ذريعة.
- لا `# noqa` ولا `// ignore:` بلا سببٍ مكتوبٍ في السطر نفسه.
- لا أسرارَ في المستودع ولا في `environment:` داخل compose — ملفّاتُ
  بيئةٍ بصلاحيّة `600` أو `env_file` فقط.

## مهارات Flutter (‎.claude/skills/flutter-*‎)

- المستودع فيه 34 مهارة Flutter مثبّتة. افتح المهارة المناسبة **قبل** كتابة الجزء
  الذي تخصّه، لا بعده. الفهرس والترتيب في `flutter-app-blueprint`.
- عند طلب ميزة كاملة أو تطبيق كامل: اتبع `flutter-autobuild` — خطة في
  `docs/SPEC.md` ثم تنفيذ المراحل بالترتيب، بلا توقف للسؤال بين المراحل،
  وبلا `TODO` ولا شاشة فارغة ولا زر لا يعمل.
- عند التعارض: توجيهات هذا المستودع أعلاه ومستندات `docs/` **تسبق** أي افتراض
  في المهارات (المكدّس التقني، الألوان، التسمية، سير العمل) — المهارات مرجع
  تقني لا مصدر قرار.
- ثوابت لا تُخالَف: كل نص ظاهر من ملف الترجمة، كل شاشة لها الحالات الأربع
  (تحميل/فراغ/خطأ/بيانات)، `EdgeInsetsDirectional` لا `left/right`،
  لا مفاتيح سرية في التطبيق، الصلاحيات تُفرض على السيرفر لا في الواجهة.
