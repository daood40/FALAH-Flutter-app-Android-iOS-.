{
  "name": "falah-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "قشرة فلاح الأصلية لأندرويد وiOS — نفس الواجهة، بميزات الجهاز",
  "scripts": {
    "sync": "node prepare.js && npx cap sync",
    "android": "npm run sync && npx cap open android",
    "ios": "npm run sync && npx cap open ios",
    "build:aab": "npm run sync && cd android && ./gradlew bundleRelease",
    "build:apk": "npm run sync && cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/filesystem": "^7.0.0",
    "@capacitor/ios": "^7.0.0",
    "@capacitor/share": "^7.0.0",
    "@capacitor/splash-screen": "^7.0.0",
    "@capacitor/status-bar": "^7.0.0"
  },
  "devDependencies": { "@capacitor/cli": "^7.0.0" }
}
