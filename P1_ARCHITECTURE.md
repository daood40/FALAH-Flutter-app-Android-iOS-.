# P1 — معمارية فَلاح كما هي

> كلُّ رقمٍ هنا **مقيسٌ في هذه الجلسة**، لا مقدَّر. وما لم يُقَس يُقال إنه
> لم يُقَس. والغرضُ من الوثيقة أن يُبنى P1 على ما هو قائمٌ فعلًا، لا على
> ما يُظنّ أنه قائم.

---

# ١ · الخريطة

```
                    ┌─────────────────────────────────┐
   المتصفّح  ──────▶│  Caddy — شهادة · ضغط · XFF      │
   القشرة           └────────────────┬────────────────┘
                                     │
   ══════════ HTTP ═══════════════════▼═══════════════════════════════
                    ┌─────────────────────────────────┐
                    │  app.py (٤٤٠ س) · api.py (٨٢ س) │
                    │  مقبسٌ · كعكةٌ · CSRF · ترويسات   │
                    │  وترجمةُ الأخطاء إلى رموزِ حالة   │
                    └────────────────┬────────────────┘
   ══════════ تطبيق ═════════════════▼═══════════════════════════════
        ┌───────────────────┐   ┌───────────────────────────┐
        │ falah/routing.py  │   │ falah/content_routes.py   │
        │ جدولُ المسارات     │   │ جدولُ القراءة العامّة      │
        │ + دورةُ الحياة     │   └─────────────┬─────────────┘
        └─────────┬─────────┘                 │
                  │ يسأل                      │
        ┌─────────▼─────────┐   ┌─────────────▼─────────────┐
        │  falah/authz.py   │   │  falah/app_routes.py      │
        │ can(subject,      │   │  معالِجٌ لكلِّ مسار         │
        │     action,       │   │  بلا فحصِ إذنٍ فيه          │
        │     resource)     │   └─────────────┬─────────────┘
        └─────────┬─────────┘                 │
   ══════════ نطاق ═══════════════════════════▼═══════════════════════
     ┌──────────────┐ ┌────────────┐ ┌──────────┐ ┌────────────────┐
     │ falah/cards  │ │ projects   │ │ billing  │ │ jobs (الطابور) │
     │ بناءُ البطاقة │ │ auth · ref │ │ الحصّة    │ │ آلةُ الحالات    │
     └──────┬───────┘ └─────┬──────┘ └────┬─────┘ └───────┬────────┘
            │               │             │               │ يسحب
            ▼               │             │        ┌──────▼─────────┐
     ┌──────────────┐       │             │        │   worker.py    │
     │ falah/verify │◀──────┴─────────────┘        │ عمليةٌ مستقلّة  │
     │ ٢٥ فحصًا —   │◀─────────────────────────────│ Chromium+ffmpeg│
     │ سلطةُ الحكم   │            يعيد الفحص        └──────┬─────────┘
     └──────┬───────┘                                     │
   ══════════ بنيةٌ تحتية ══════════════════════════════════▼═════════
     ┌──────────────────┐  ┌──────────────┐  ┌────────────────────┐
     │ falah.db (قراءة) │  │ app.db       │  │ exports/<uid>/     │
     │ ٢٥٧ م.ب · ٣٢ جدول│  │ ١٥ جدولًا     │  │ حجمٌ مشترك          │
     └──────────────────┘  └──────────────┘  └────────────────────┘
```

**الاتّجاه ينزل ولا يصعد.** لا وحدةَ في «نطاق» تستورد وحدةً في «تطبيق» أو
«HTTP»، ولا وحدةَ في «بنيةٍ تحتية» تعرف شيئًا عن طلب. وكان `falah/projects.py`
و`falah/agent.py` و`render.py` تستورد `api` — أي أن النطاق يعتمد على الويب —
فنزل بناءُ البطاقة إلى `falah/cards.py` وانقطع ذلك.

## الحجم المقيس

| | |
|---|---|
| بايثون | **٥٩ ملفًّا · ١٣٬٧٧٣ سطرًا** |
| واجهات | `falah-app.html` ١٬٠٧٩ سطرًا (٥٥ ك.ب) · `falah-agent.html` ٨١٣ سطرًا (**١٫١٤ م.ب**) |
| اعتماديات | **٤**: playwright · cryptography · imageio_ffmpeg · pillow |
| قاعدة المحتوى | ٢٥٧ م.ب · ٣٢ جدولًا (منها ١٢ جدولَ FTS) |
| قاعدة التطبيق | ١٥ جدولًا · ١٬٢٥٤ صفًّا اليوم |
| التحقّق | **١٬١٤٨ بندًا** في ثماني حزم |

---

# ٢ · التدفّقات

## دورةُ حياةِ الطلب — واحدةٌ لكلّ مسار

كانت مكرَّرةً مرّتين (`app_get` و`app_post`) بكتلتَي `except` متشابهتين لا
متطابقتين. صارت واحدة، مكتوبةً في `falah/routing.py::dispatch`:

```
الطلب
 ↓ ① HTTP        app.py: مقبس · CSRF (X-FALAH + Origin) · جسمٌ ≤ ١ م.ب → ٤١٣
 ↓ ② توجيه       routing.resolve(method, path) → مسارٌ معلَن أو القاعدة العامّة
 ↓ ③ وثيقة       app.py: كعكةٌ → جلسةٌ → Subject (والأدوارُ تُشتقّ لا تُقرأ من طلب)
 ↓ ④ حدُّ معدّل   routing: القبليُّ فقط (تسجيلٌ · ملفٌّ · وكيل) — والباقي بعد القبول
 ↓ ⑤ تحميلُ مورِد routing.locate → authz.load: **مالكُه من القاعدة لا من الطلب**
 ↓ ⑥ إذن         authz.can(subject, action, resource) → منعٌ ⇒ ردٌّ من جدولٍ واحد
 ↓ ⑦ تحقّقُ حقول routing._fields بالترتيب المُعلَن (فيظهر الخطأُ الأوّل كما كان)
 ↓ ⑧ عمل         falah/app_routes.py: معالِجٌ واحد — بلا فحصِ إذنٍ فيه
 ↓ ⑨ نطاق        projects · billing · jobs · verify
 ↓ ⑩ ردّ         app.py: Response → JSON أو ملفّ · كعكةٌ · ترويسات
```

## تدفّقُ الإذن

```
                        رقمٌ جاء من العميل
                              │
                              ▼
              authz.load(c, "project", 7)
                              │  SELECT user_id FROM projects WHERE id=7
                              ▼
       Resource(type="project", id=7, owner_id=<من القاعدة>)
                              │
   Subject(user_id, roles) ───┼──▶  can(subject, action, resource)
                              │            │
                              │     POLICY[(type, action)]
                              │            │
        ┌─────────────────────┴────────────┴──────────────────┐
        │ PUBLIC · AUTHENTICATED · OWNER · SELF · NEEDS(perm)  │
        └─────────────────────┬────────────────────────────────┘
                              ▼
        Decision(allowed, reason ∈ {unauthenticated · not_found
                                    · forbidden · no_policy})
```

**ثلاثُ خصالٍ تحسم منعَ IDOR:**

1. **المالكُ يُقرأ لا يُصدَّق.** لا يُؤخذ رقمٌ من الطلب على أنه لصاحبه؛
   يُحمَّل المورِد ويُقرأ `user_id` الحقيقيّ ثم يُقارَن.
2. **«ليس لك» و«غيرُ موجود» ردٌّ واحد** — حرفًا بحرف. فلا تُعدّ الموارد
   بفروق الرسائل، ولا يُستدلّ على وجود مورِدِ غيرك برقمٍ مخمَّن.
3. **المنعُ أصل.** `POLICY` جدولٌ مغلق: زوجٌ (مورِد، فعل) ليس فيه يُمنع
   بسبب `no_policy`. ومسارٌ يُضاف غدًا بلا سياسةٍ يسقط في `authz_test.py`
   لا في الإنتاج.

**ولا شيءَ في `authz.py` يعرف اسمَ مسار.** القاعدةُ على «مشروعٌ يُعدَّل»
لا على `/app/projects/update` — فإعادةُ تسميةِ مسارٍ لا تمسّ حرفًا من الإذن،
وإضافةُ مسارٍ لمورِدٍ قائمٍ ترث سياستَه.

## طلبُ قراءة (بطاقة)

```
GET /card?kind=quran&surah=94&ayah=5
  → app.do_GET يطابق CONTENT_PATHS
  → proxy_content: Shim فوق api.H (بلا شبكة — استدعاءٌ داخل العملية)
  → api.do_GET (١٧ سطرًا) → content_routes.dispatch → card()
  → cards.quran_card: ٦ استعلامات
  → verify.py: ٢٥ فحصًا
  → JSON
```
**المقيس: ٢٫٠٥ م.ث.**

## طلبُ كتابة (تصدير)

```
POST /app/export
  → guard_csrf (X-FALAH + Origin) · body_json (سقفٌ ١ م.ب → ٤١٣)
  → routing.resolve → Route(project.render · owner_field=project · quota=cards)
  → Subject من الجلسة
  → _fields: int(b["project"])          ← ناقصٌ ⇒ «حقل ناقص» كما كان
  → authz.load(project) → can(…, "render", …)   ← الملكية، مركزيًّا
  → app_routes.export:
       open_project → إعادةُ فحصٍ لكل عنصر
       billing.check → require(ratio) → require(skin)
       live_for(idem_key) → مكرَّر؟ يُردّ بالقائمة
       rate_ok(export)                  ← بعد قبول العمل لا عند الطلب
       billing.consume  ← الحجز
       jobs.enqueue     ← الطابور
  → 202 {job}
```
**المقيس: ٦٫٢٥ م.ث** (وقبل التفكيك ٥٫٥٣ — الفرقُ استعلامٌ واحدٌ على مفتاحٍ
رئيس، وهو ثمنُ قراءةِ المالك بدل تصديقه).

## التصيير (العامل)

```
claim (BEGIN IMMEDIATE · سقفُ تزامنٍ ٤ · not_before)
  → perform: open_project ← **يُعاد الفحص وقت التصيير**
  → carousel.render_plan → render.build_html → Chromium
  → beat(progress) كل مرحلة
  → complete | fail(backoff) | reap(stale)
```
**المقيس: ٣٫٢ث للبطاقة · ٢٦ث للمقطع.**

## المصادقة والتخويل

| الطبقة | الآلية |
|---|---|
| كلمة المرور | PBKDF2-SHA256 · ٢٤٠ ألف دورة · ملحٌ لكل حساب |
| الجلسة | رمزٌ عشوائيّ **يُخزَّن مجزَّأً** · كعكة HttpOnly+SameSite=Strict+Secure |
| CSRF | ترويسة `X-FALAH: 1` + `Origin` المطابق |
| **الإذن** | `authz.can(subject, action, resource)` — **موضعٌ واحدٌ يُقرَّر فيه** |
| الملكية | `authz.load` يقرأ `user_id` من القاعدة · و`owned()` يبقى حائطًا ثانيًا |
| الرسائل | «ليس لك» = «لا وجود له» — فلا تُعدّ الموارد بفروق الرسائل |

**عن الحائط الثاني:** استعلاماتُ النطاق ما زالت مقيَّدةً بـ`AND user_id=?`.
ليس ذلك تكرارًا للسياسة — السياسةُ في `POLICY` وحدها — بل تنفيذٌ لا يُتخطّى
حتى لو أخطأ سطرٌ في الجدول. القرارُ واحد، والحائطان اثنان.

**والأدوار:** اليوم دورٌ واحد (`system_admin`) يمنحه `FALAH_ADMIN_KEY`، وهو
الدَّينُ الأمنيّ الباقي (د٣). لكنّ **شكلَه صار جاهزًا**: الشرطُ مكتوبٌ على
الصلاحية (`NEEDS("manage_billing")`) لا على اسم الدور، والأدوارُ تُشتقّ في
`authz.roles_for` وحدها. فP1.2 يضيف قراءةً من القاعدة هناك، ولا يمسّ معالِجًا
ولا مسارًا.

---

# ٣ · الأداء المقيس

**قبل التفكيك وبعده، بالأمر نفسه** (`python3 bench.py`، ٨٠ عيّنةً لكل مسار،
الوسيطُ لا المتوسّط):

| المسار | قبل | بعد | الفرق |
|---|---:|---:|---:|
| `/healthz` | ٠٫٧٢ | ٠٫٧٥ | +٤٪ |
| `/readyz` | ١٫٨٨ | ٢٫٠٣ | +٨٪ |
| `/card` (آية · ٦ استعلامات) | ٢٫٠٧ | ٢٫٠٥ | **−١٪** |
| `/card` (حديث · ٥ استعلامات) | ٢٫٥٣ | ٢٫٥٧ | +٢٪ |
| بحثُ القرآن · بحثُ الحديث | ١٫٧٧ · ٢٫٠٢ | ١٫٨٢ · ٢٫٠٨ | +٣٪ |
| `/chapters` · `/templates` · `/topics` | ٣٫٦٥ · ٢٫١٦ · ١٫٨١ | ٤٫٠٥ · ٢٫٣٠ · ١٫٨٥ | +٢–١١٪ |
| `/series?count=5` | ٧٫٩٣ | ٨٫٥٠ | +٧٪ |
| **`/health`** | ٦٤٫٩٥ | ٦٦٫٣٢ | +٢٪ |
| `/app/projects` · `/app/jobs` · `/app/exports` | ١٫٣٩ · ١٫٥١ · ١٫٣٥ | ١٫٥٢ · ١٫٦١ · ١٫٥٢ | +٧–١٣٪ |
| `POST /app/export` | ٥٫٥٣ | ٦٫٢٥ | +١٣٪ |
| `POST /app/video` | ٥٫١٠ | ٥٫٢٩ | +٤٪ |
| التصيير (عامل) | ٣٫٢ث · ٢٦ث | لم يُمَسّ | — |

**واستعلاماتُ القاعدة لم تتغيّر في مسارٍ واحد**: ١٢ · ٦ · ٥ · ١ · ١ · ٢ · ٢
· ٤٩ · ١ — قبل وبعد، عددًا بعدد.

## هل هذه فروقٌ أم ضجيج؟

النسبُ المئوية تُغري بأن يُقال «تباطأ». ولذلك شُغّل **تشغيلان متتاليان
للشيفرة نفسها** (`AFTER2` و`AFTER3`) لقياس تشتّت الجهاز:

| المسار | تشغيلٌ أوّل | تشغيلٌ ثانٍ | الفرق |
|---|---:|---:|---:|
| `/healthz` (لا يمرّ بالموجِّه أصلًا) | ٠٫٧٥ | ٠٫٨٥ | **+١٣٪** |
| `/health` | ٦٦٫٣٢ | ٥٨٫٤٨ | **−١٢٪** |
| `/templates` | ٢٫٣٠ | ٢٫٠٥ | −١١٪ |
| `/series` | ٨٫٥٠ | ٧٫٥٩ | −١١٪ |

التشتّتُ بين تشغيلين متطابقين **±١٣٪** — أي أوسعُ من أكثر فروق «قبل/بعد».
فالنتيجةُ الصادقة: **لا فرقَ يُقاس في مسارات القراءة.**

## وما الفرقُ الحقيقيّ إذن؟

**استعلامٌ واحدٌ زائد** على المسارات التي تُفحص ملكيّتُها — وهو مقيسٌ لا
مقدَّر (`Connection.set_trace_callback`):

```
authz.load → SELECT user_id FROM projects WHERE id=?     ← استعلامٌ واحد
                                                            على مفتاحٍ رئيس
GET  /app/projects        → ١ استعلام   (بلا تغيير)
GET  /app/limits          → ٠ استعلام   (بلا تغيير)
POST /app/projects/update → ٥ استعلامات (منها واحدٌ لتحميل المورِد)
POST /app/projects/delete → ١ استعلام حين يُمنع (كما كان: استعلامُ `owned`)
```

وهذا **ثمنُ قراءةِ المالك بدل تصديقه**، ويقع على أحدَ عشرَ مسارَ كتابةٍ
وقراءةٍ بمعرِّف. والطلبُ الممنوع لم يزدد شيئًا.

**خلاصة: طبقةُ القراءة سريعةٌ كما كانت.** الثقلُ كلُّه في التصيير، وقد خرج
من دورة الطلب في P0-1 ولم يُمَسّ هنا.

---

# ٣٫٥ · مسؤوليةُ كلِّ وحدة

قاعدةُ القراءة: **إن لم تستطع أن تقول ما لا تفعله الوحدة، فهي تفعل أكثرَ ممّا ينبغي.**

| الوحدة | تفعل | **لا تفعل** | أسطر |
|---|---|---|---:|
| `app.py` | مقبسٌ · كعكةٌ · CSRF · ترويسات · ترجمةُ الأخطاء · إقلاع | لا تقرّر إذنًا · لا تعرف مسارًا بعينه · لا تلمس نطاقًا | ٤٤٠ |
| `api.py` | يفتح اتصالًا وينادي جدولَ المحتوى ويكتب JSON | لا يبني بطاقةً · لا يحكم | ٨٢ |
| `falah/routing.py` | جدولُ المسارات · دورةُ الحياة · ترجمةُ المنع | لا تعرف HTTP · لا تنفّذ عملًا | ٢٥١ |
| `falah/authz.py` | `can(subject, action, resource)` · تحميلُ المالك | **لا تعرف مسارًا ولا رمزَ حالة ولا ترويسة** | ٣٢٠ |
| `falah/app_routes.py` | معالِجٌ لكلِّ مسارٍ كتابةً وقراءة | **لا تفحص إذنًا** · لا تكتب ترويسة | ٣٢٧ |
| `falah/content_routes.py` | معالِجُ قراءةٍ لكلِّ مسارٍ عامّ | لا تعرف جلسةً ولا مستخدمًا | ٣٧٢ |
| `falah/cards.py` | يبني البطاقة وسياقَها من القاعدة | **لا يحكم** — الحكمُ لـ`verify` | ٢٩١ |
| `falah/verify.py` | ٢٥ فحصًا — **سلطةُ الحكم الوحيدة** | لا تعرف طلبًا ولا مستخدمًا | ١٠٤ |
| `falah/web.py` | `Request` و`Response` | لا منطقَ فيها البتّة | ٦٩ |
| `falah/ratelimit.py` | جدولُ الحدود وفحصُها | لا تقرّر متى تُفحص — ذلك في الجدول | ٤٧ |
| `falah/settings.py` | قراءةُ البيئة في موضعٍ واحد | لا تقرّر شيئًا | ٣٤ |
| `falah/jobs.py` | الطابور وآلةُ حالاته | لا تنسخ منطقَ فحص — تفتح المشروعَ فيُعاد | ٤٦٠ |

## اتّجاهُ الاعتماد

```
   HTTP        app.py · api.py · worker.py
                     │  (يستوردون ما تحتهم فقط)
                     ▼
   تطبيق       routing · app_routes · content_routes · web · ratelimit
                     │
                     ▼
   نطاق        authz · cards · verify · projects · billing · jobs · auth
                     │
                     ▼
   بنيةٌ تحتية  store (sqlite) · settings · migrate · القرص
```

**ولا وحدةَ تحت `falah/` تستورد `app` ولا `api`** — فُحص آليًّا بتحليل
شجرة الاستيراد. وكان `falah/projects.py` و`falah/agent.py` يستوردان `api`،
فصارا يستوردان `falah/cards.py`.

**وما بقي مائلًا، صريحًا:** `falah/jobs.py` يستورد `carousel` و`video`،
و`falah/content_routes.py` يستورد `carousel`. وهذه وحداتُ تصييرٍ في جذر
المشروع لا وحداتُ ويب، فليس فيها صعودٌ إلى HTTP — لكنّ موضعَها ينبغي أن
يكون تحت `falah/` أيضًا. **لم يُنقل في P1.1** لأنه خارج ما طُلب، ومُقيَّدٌ
في `P1_BACKLOG.md` بندًا مستقلًّا يُنقل بقرارٍ صريح.

---

# ٤ · الدَّين التقنيّ — مرتَّبًا بالأثر

### ~~د١ · `api.do_GET` = ٣٠٨ سطرًا~~ — **أُغلق في P1.1**
صار ١٧ سطرًا: يقرأ المسار، ويفتح اتصالًا، وينادي `content_routes.dispatch`،
ويكتب الردّ. والمساراتُ العشرون جدولٌ يُقرأ في نظرة، وكلُّ معالِجٍ دالّةٌ
`(c, q) → (جسم، رمز)` تُنادى في اختبارٍ بلا خادم.

### ~~د٢ · `app.app_post` = ١٩٨ سطرًا~~ — **أُغلق في P1.1**
اختفت. صارت `app_request` (٣٠ سطرًا) تمرّ بالدورة المُعلَنة، والمعالِجاتُ
في `falah/app_routes.py`. والأثرُ الأمنيّ الذي كان أخطرَ من الطول — أن
كاتبَ مسارٍ جديد يجب أن **يتذكّر** فحصَ الملكية — زال بنيويًّا: المسارُ
يُعلن مورِدَه، والدورةُ تفحص، و`authz_test.py` يسقط إن أُضيف مسارٌ بلا
سياسة. النسيانُ لم يعد ممكنًا صامتًا.

### ~~د٣ · لا أدوار~~ — **أُغلق في P1.2**
`FALAH_ADMIN_KEY` **أُلغي**. صار الدورُ عمودًا على الحساب يُقرأ مع الجلسة،
والصلاحيةُ اسمُها `<مورِد>.<فعل>` (٣٨ صلاحيةً = ٣٨ زوجًا في `POLICY`،
تقابلًا تامًّا)، والأدوارُ أربعة (`user ⊂ moderator ⊂ admin ⊂ super_admin`)
حزمًا صريحةً بلا wildcard. وحارسُ التسلسل ثلاثُ قواعدَ في `authz` لا في
المعالِجات. و`audit_logs` يقول مَن فعل وعلى مَن وبأيّ نتيجة — يُلحق ولا
يُعدَّل (مُشغِّلان في القاعدة نفسها).

### د٤ · لا حذفٌ منطقيّ
حذفُ مشروعٍ نهائيّ. لا استرجاعَ لخطأِ نقرة.

### د٥ · العربية مكتوبةٌ في الشيفرة
كلُّ رسالةٍ للمستخدم نصٌّ عربيٌّ داخل الدوالّ. لا i18n. والتوسّعُ لغةً
ثانيةً يعني لمسَ كلِّ ملفّ.

### د٦ · `falah-agent.html` = ١٫١٤ م.ب
سطرٌ واحد فيه **٧٤٥ ك.ب** من JSON مضمَّن (`var DB = {…}`) — مادّةُ العرض
كلُّها في الصفحة لتعمل بلا اتصال. مقصودٌ في أصله، ثقيلٌ في أثره.

### د٧ · خطوط Plex: امتدادٌ يكذب
`fonts/plex*.woff2` **ملفّاتُ TrueType** لا WOFF2 (التوقيع `\x00\x01\x00\x00`
لا `wOF2`)، وتُقدَّم بنوع `font/woff2` ويُعلَن `format('woff2')` في CSS.
كروميوم يقبلها بالشمّ — **قِسناه، والخطُّ يُحمَّل فعلًا** — لكنّ متصفّحًا
أشدَّ يرفضها. و٦٧٦ ك.ب بدل ~٢٠٠ ك.ب لو كانت WOFF2 حقًّا.

### د٨ · لا مراقبةَ مهيكلة
`log_error` سطرٌ على stderr. لا مقاييس، ولا تتبّعَ أخطاء، ولا زمنَ عمليات.
بعد النشر لن نعرف أن شيئًا يتباطأ إلا من شكوى.

---

# ٥ · نقاط الاختناق — ما ينكسر أوّلًا حين يكبر الحمل

| النقطة | الحدُّ اليوم | ما يكشفه |
|---|---|---|
| **عاملٌ واحد** | ٣٫٢ث/بطاقة ⇒ ~١٬١٠٠ بطاقة/ساعة | طابورٌ يطول عند ٤٠ مشتركًا نشطًا |
| **SQLite كتابةً** | كاتبٌ واحد في اللحظة | لم يُقَس تحت حملٍ مستدام — **مجهولٌ لا مطمئنٌّ له** |
| **`/health`** | ٦٢ م.ث | يُستدعى من الواجهة؛ لو صار في فحصٍ دوريّ أثقل |
| **التخزين** | ٢٬٠٤٨ م.ب/مستخدم | لا كنسَ تلقائيًّا للصادرات القديمة |
| **الذاكرة** | Chromium لكل تصيير | ٤ متزامنة × ~٣٠٠ م.ب — لم يُقَس تحت ضغط |
| **`falah.db`** | ٢٥٧ م.ب في الصورة | كلُّ نشرةٍ تحمل القاعدة كاملة |

---

# ٦ · مخاطر التوسّع

1. **العامل الواحد** أوّلُ ما يختنق. الحلّ موجودٌ ومجرَّب: `--scale worker=3`.
   لم يُقَس بأكثر من واحد.
2. **SQLite** يكفي آلافًا لا ملايين. الانتقالُ إلى Postgres قرارٌ كبير
   **لا يُتّخذ إلا بقياسٍ يوجبه** — ولم يُقَس بعد.
3. **الصادرات تتراكم** بلا كنس. عشرة آلاف مستخدمٍ × ٢ ج.ب = عشرون تيرابايت.
4. **قاعدة المحتوى في الصورة** تجعل كلَّ نشرةٍ ٣٠٠ م.ب.

---

# ٧ · القرار الثلاثيّ

## ✅ ما لا يُمَسّ — SECURITY CONTRACT

هذه **عقدٌ مغلق**. أيُّ تغييرٍ يمسّها يُوقَف حتى يُراجَع صراحةً:

| البند | الحارس |
|---|---|
| `verify.py` سلطةَ الحكم الوحيدة | `tests.py` · لا نسخَ لمنطقها في الطابور |
| SOURCE_LOCK — لا نصَّ يُولَّد ولا يُعدَّل | ٢٥ فحصًا · بصمة sha256 |
| عزلُ المستخدمين والمشاريع والمهامّ والصادرات | `isolation_test.py` — ٧٤ بندًا |
| **قرارُ الإذن نفسه** — مالكٌ مقروء · منعٌ أصل · لا مسارَ بلا سياسة | `authz_test.py` — ٨٥ بندًا |
| **`super_admin` لا يبلغ مورِدَ غيره** — والإدارةُ ليست اطّلاعًا على المحتوى | `invariants.py` — `SUPER_ADMIN_DOES_NOT_BYPASS_RESOURCE_SCOPE` |
| **مسارٌ جديدٌ بلا صلاحية = بوّابةٌ ساقطة** | `invariants.py` — `NEW_OPERATION_WITHOUT_PERMISSION` |
| **الأدوارُ والتصعيدُ وسجلُّ التدقيق** — مصفوفةٌ كاملة · لا تصعيدَ ذاتٍ ولا أفقيّ · سجلٌّ يُلحق بلا أسرار | `rbac_test.py` — ١٢٥ بندًا |
| منعُ حقن CSS/XSS (`safe_color`) | `tests.py` — ٦ حمولاتٍ عدائية |
| منعُ الخروج من المسار (نقاطٌ وترميزٌ ووصلات) | `isolation_test.py` — ١٧ مسارًا |
| SSRF (مخطَّطُ التلاوة · حجمُها) | `tests.py` — ٥ مخطَّطات |
| سلامةُ الحصّة (لا خصمَ مضاعف ولا ضائع) | `failure_test.py` — ٨ سيناريوهات |
| التفرّد عند التزامن | `failure_test.py` — ١٠ ← ١٠٠ |
| آلةُ حالات المهمّة | `tests.py` — كلُّ انتقالٍ مشروعٍ وممنوع |
| حدُّ المعدّل (يعدّ العملَ لا الطلب) | `isolation_test.py` |
| منعُ تسريب الأخطاء وبصمةِ الخادم | `leak_test.py` — ٥٤ بندًا |
| حدُّ حجم الطلب (٤١٣) | `isolation_test.py` |
| حارسُ الهجرات الهادمة | `tests.py` · `dbsafe.py` |

**الخطُّ الأساس بعد إغلاق P1.2: ١٬١٨٣ بندًا** (٥٤٦ وحدة + ١٢٤ شامل
+ ٥٣ واجهة + ٩٥ كسر + ٥٤ تسريب + ٨١ عزل + ٨٥ إذن + ١٤٥ أدوارٌ وتدقيق)
**+ ١٦ ثابتًا أمنيًّا مسمّى**. لا ينزل.

> تصحيحٌ في العدّ: كان يُقال ٩٢٩، وكان يعدّ سطرَ الخلاصة في `tests.py`
> بندًا. العددُ الصحيح قبل P1.1 **٩٢٨**، وبعده ١٬٠١٣ (+٨٥).

## 🔧 ما يُحسَّن

| | لماذا |
|---|---|
| ~~تفكيكُ التوجيه (د١ · د٢)~~ | **تمّ في P1.1** — الفحصُ صار بنيويًّا |
| ~~الأدوار وسجلُّ التدقيق (د٣)~~ | **تمّ في P1.2** |
| المراقبة المهيكلة (د٨) | لنعرف قبل الشكوى |
| CSP · HSTS | ثغراتٌ مفتوحة |
| الحذفُ المنطقيّ (د٤) | استرجاعُ خطأِ النقرة |
| كنسُ الصادرات | التخزينُ يتراكم بلا حدّ |
| خطوط Plex (د٧) | امتدادٌ يكذب · ٤٧٦ ك.ب زائدة |

## 🔒 عقودٌ أمنيةٌ دائمة — لا تُوسَّع بأثرٍ جانبيّ

هذه **ثوابتُ بوّابة** في `invariants.py`، لكلٍّ منها اسمٌ يُقرأ في المخرَج.
وتوسيعُ أيٍّ منها ليس تعديلَ سطر: هو **قرارٌ يظهر في الفرق** لأن الثابتَ
نفسَه يجب أن يُحدَّث في الشيفرة.

| الثابت | ما يمنعه |
|---|---|
| `AUTHZ_DEFAULT_DENY` | زوجٌ (مورِد، فعل) بلا سياسةٍ يُمنع لكلِّ دور |
| `UNKNOWN_PERMISSION_DENY` | صلاحيةٌ خارج الكتالوج لا يملكها أحد |
| `UNKNOWN_ROLE_DENY` | دورٌ مجهولٌ ⇒ صفرُ صلاحيات (fail closed) |
| `UNAUTHENTICATED_DENY` | كلُّ نطاقٍ غير عامٍّ يشترط جلسة |
| `OWNER_SCOPE_ENFORCED` | مجموعةُ الأزواج ذاتِ نطاق `OWNER` **مثبَّتةٌ بالاسم** |
| **`SUPER_ADMIN_DOES_NOT_BYPASS_RESOURCE_SCOPE`** | **`super_admin` ≠ قراءةُ مشروعِ غيره · `admin` ≠ رؤيةُ المحتوى** |
| `ROLE_ESCALATION_GUARDED` | القواعدُ الثلاث كاملةً لكلِّ دورٍ في كلِّ هدف |
| **`NEW_OPERATION_WITHOUT_PERMISSION`** | **مسارٌ جديدٌ بلا صلاحيةٍ يُسقط البوّابة** |
| `NO_ROLE_NAME_IN_CONDITION` | `if role == "admin"` في أيّ ملفّ |
| `AUDIT_APPEND_ONLY` | تعديلُ سجلّ التدقيق أو حذفُه — من الشيفرة ومن القاعدة |
| `AUDIT_NO_SECRETS` | تسرُّبُ سرٍّ إلى السجلّ (يُفتَّش بقنّاصة) |
| `AUDIT_UNAUTHORIZED_ACCESS_DENIED` | قراءةَ السجلّ بلا صلاحيةٍ مسمّاة |
| `AUDIT_CATALOGUE_CLOSED` | حدثًا خارج الكتالوج |
| `PERMISSION_CATALOGUE_DOCUMENTED` | صلاحيةً ميتةً · عملًا حسّاسًا بلا تسجيل · كتالوجًا متقادمًا |
| `B21_ENUMERATION_DEFERRED_DOCUMENTED` | نسيانَ قرارِ التأجيل أو تغييرَه صمتًا |
| `B23_413_SEMANTICS_DOCUMENTED` | طيَّ الحدّ المعروف في ٤١٣ |

### عن `super_admin` خصوصًا

```
super_admin  ≠  read every user's project
admin        ≠  content visibility
```

الإدارةُ تُدير **الحسابات والأدوار والفوترة**، ولا تحصل تلقائيًّا على محتوى
المستخدمين. نطاقُ `project.read` و`job.read` و`export_file.download` هو
`OWNER` **لكلِّ دورٍ بلا استثناء**.

ومن أراد غدًا أن يقرأ إداريٌّ محتوى مستخدمٍ (لدعمٍ فنّيّ مثلًا) فالطريقُ
واحد: **صلاحيةٌ جديدةٌ باسمها** (`project.read_any` مثلًا) بنطاقٍ صريح،
وتحديثُ هذا الثابت، وتوثيقُه، واختبارُه، وتسجيلُ كلِّ استعمالٍ له. لا
توسيعَ نطاقٍ قائم، ولا استثناءَ في معالِج.

## ⛔ ما لا يُعاد كتابته

- **`falah.db` وبناؤها** — تعمل، ومقيسة، ولا شكوى منها.
- **`verify.py`** — أنضجُ ما في المشروع.
- **معمارية الطابور** — مجرَّبةٌ تحت الكسر المتعمَّد.
- **SQLite** — لا يُهجَر بلا قياسٍ يوجبه.
- **`api.py` منطقًا** — يُفكَّك توجيهُه لا يُعاد بناءُ حكمه.

---

# ٨ · التكاملات الخارجية

| | الحال |
|---|---|
| آبل (App Store Server API) | مكتوبٌ ومُختبَرٌ بسلسلةِ شهاداتٍ مولَّدة · **لم يُجرَّب على متجرٍ حقيقيّ** |
| جوجل (Play Developer API) | مكتوب · **لم يُجرَّب** |
| البريد (SMTP) | مكتوب · **لم يُجرَّب بخادمٍ حقيقيّ** |
| مصادر النصوص | تُنزَّل مرّةً وتُبنى · لا اتصالَ وقت الخدمة |
| التلاوات | تُجلب وقت التصيير — **مخطَّطٌ مشروطٌ وحجمٌ محدود** |
