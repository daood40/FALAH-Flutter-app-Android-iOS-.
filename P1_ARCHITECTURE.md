# P1 — معمارية فَلاح كما هي

> كلُّ رقمٍ هنا **مقيسٌ في هذه الجلسة**، لا مقدَّر. وما لم يُقَس يُقال إنه
> لم يُقَس. والغرضُ من الوثيقة أن يُبنى P1 على ما هو قائمٌ فعلًا، لا على
> ما يُظنّ أنه قائم.

---

# ١ · الخريطة

```
                    ┌─────────────────────────────────┐
   المتصفّح  ──────▶│  Caddy — شهادة · ضغط · XFF      │
   القشرة           └────────────────┬────────────────┘
                                     │
                    ┌────────────────▼────────────────┐
                    │  app.py  (Server · 128 backlog) │
                    │  جلسة · CSRF · حصّة · حدُّ معدّل │
                    └───┬─────────────────────────┬───┘
                        │ استدعاءٌ داخل العملية    │ يضع مهمّة
                        │ (بلا شبكة)               │
              ┌─────────▼────────┐        ┌────────▼─────────┐
              │     api.py       │        │  jobs (app.db)   │
              │  بطاقات · بحث    │        │  الطابور         │
              └─────────┬────────┘        └────────┬─────────┘
                        │                          │ يسحب
              ┌─────────▼────────┐        ┌────────▼─────────┐
              │ falah/verify.py  │        │    worker.py     │
              │ ٢٥ فحصًا — سلطة  │◀───────│ عمليةٌ مستقلّة   │
              │ الحكم الوحيدة    │ يعيد   │ Chromium+ffmpeg  │
              └─────────┬────────┘ الفحص  └────────┬─────────┘
                        │                          │
              ┌─────────▼────────┐        ┌────────▼─────────┐
              │ falah.db (قراءة) │        │ exports/<uid>/   │
              │ ٢٥٧ م.ب · ٣٢ جدول│        │ حجمٌ مشترك       │
              └──────────────────┘        └──────────────────┘
```

## الحجم المقيس

| | |
|---|---|
| بايثون | **٤٩ ملفًّا · ١٢٬٢٧٤ سطرًا** |
| واجهات | `falah-app.html` ١٬٠٧٩ سطرًا (٥٥ ك.ب) · `falah-agent.html` ٨١٣ سطرًا (**١٫١٤ م.ب**) |
| اعتماديات | **٤**: playwright · cryptography · imageio_ffmpeg · pillow |
| قاعدة المحتوى | ٢٥٧ م.ب · ٣٢ جدولًا (منها ١٢ جدولَ FTS) |
| قاعدة التطبيق | ١٥ جدولًا · ١٬٢٥٤ صفًّا اليوم |
| التحقّق | **٩٢٩ بندًا** في ست حزم |

---

# ٢ · التدفّقات

## طلبُ قراءة (بطاقة)

```
GET /card?kind=quran&surah=94&ayah=5
  → do_GET يطابق CONTENT_PATHS
  → proxy_content: Shim فوق api.H (بلا شبكة — استدعاءٌ داخل العملية)
  → api.do_GET (٣٠٨ أسطر، سلسلة elif)
  → quran_card: 8 استعلامات
  → verify.py: ٢٥ فحصًا
  → JSON
```
**المقيس: ٢٫١ م.ث.**

## طلبُ كتابة (تصدير)

```
POST /app/export
  → guard_csrf (X-FALAH + Origin)
  → body_json (سقفٌ ١ م.ب → ٤١٣)
  → app_post (١٩٨ سطرًا، سلسلة if)
  → user() → جلسةٌ مجزَّأة
  → open_project → إعادةُ فحصٍ لكل عنصر
  → billing.check → require(ratio) → require(skin)
  → live_for(idem_key) → مكرَّر؟ يُردّ بالقائمة
  → rate_ok(export)
  → billing.consume  ← الحجز
  → jobs.enqueue     ← الطابور
  → 202 {job}
```
**المقيس: ٤ م.ث · ١٠٠ متزامنًا في ٣٧٨ م.ث.**

## التصيير (العامل)

```
claim (BEGIN IMMEDIATE · سقفُ تزامنٍ ٤ · not_before)
  → perform: open_project ← **يُعاد الفحص وقت التصيير**
  → carousel.render_plan → render.build_html → Chromium
  → beat(progress) كل مرحلة
  → complete | fail(backoff) | reap(stale)
```
**المقيس: ٣٫٢ث للبطاقة · ٢٦ث للمقطع.**

## المصادقة والتخويل

| الطبقة | الآلية |
|---|---|
| كلمة المرور | PBKDF2-SHA256 · ٢٤٠ ألف دورة · ملحٌ لكل حساب |
| الجلسة | رمزٌ عشوائيّ **يُخزَّن مجزَّأً** · كعكة HttpOnly+SameSite=Strict+Secure |
| CSRF | ترويسة `X-FALAH: 1` + `Origin` المطابق |
| الملكية | `owned(c, user_id, pid)` — كلُّ استعلامٍ مقيَّدٌ بـ`user_id` |
| الرسائل | «ليس لك» = «لا وجود له» — فلا تُعدّ الموارد بفروق الرسائل |

**لا أدوار.** الإدارة بمفتاحٍ مشترك `FALAH_ADMIN_KEY` — وهذا أهمُّ دَينٍ أمنيّ باقٍ.

---

# ٣ · الأداء المقيس

| المسار | الوسيط |
|---|---:|
| `/healthz` | ١٫١ م.ث |
| `/readyz` | ٢٫٢ م.ث |
| `/card` (آية بتفسيرٍ وترجمة · ٨ استعلامات) | ٢٫١ م.ث |
| `/card` (حديث · ٥ استعلامات) | ٢٫٥ م.ث |
| البحث (قرآن · حديث · موسوعة) | ٢٫٠–٢٫٨ م.ث |
| `/chapters` · `/templates` · `/topics` | ٢–٣٫٥ م.ث |
| `/series?count=5` | ٧٫٧ م.ث |
| **`/health`** | **٦٢٫٤ م.ث** ← يعدّ صفوف القاعدة كلَّها |
| `POST /app/export` | ٤ م.ث |
| التصيير (عامل) | ٣٫٢ث بطاقة · ٢٦ث مقطع |

**خلاصة: طبقةُ القراءة سريعةٌ ولا تحتاج عملًا.** الثقلُ كلُّه في التصيير،
وقد خرج من دورة الطلب في P0-1.

---

# ٤ · الدَّين التقنيّ — مرتَّبًا بالأثر

### د١ · `api.do_GET` = ٣٠٨ سطرًا في دالّةٍ واحدة
سلسلةُ `elif` تحمل ٢٢ مسارًا. كلُّ مسارٍ جديد يطيلها. لا اختبارَ وحدةٍ
لمسارٍ بعينه — يُختبر كلُّه عبر الشبكة. **الأثر: كلُّ إضافةٍ تلمس ملفًّا
واحدًا فتتضخّم مساحةُ الانحدار.**

### د٢ · `app.app_post` = ١٩٨ سطرًا
المشكلة نفسها في طبقة الكتابة، ومعها أثرٌ أمنيّ: كلُّ مسارٍ جديد يجب أن
يتذكّر كاتبُه **يدويًّا** أن يفحص الملكية والحصّة وحدَّ المعدّل. النسيانُ
ممكنٌ ولا شيء يمنعه بنيويًّا.

### د٣ · لا أدوار
`FALAH_ADMIN_KEY` مفتاحٌ واحدٌ مشترك: من يملكه يملك كلَّ شيء، ولا يُعرف
مَن فعل. `events` يسجّل، لكن بلا هويّةِ إداريّ.

### د٤ · لا حذفٌ منطقيّ
حذفُ مشروعٍ نهائيّ. لا استرجاعَ لخطأِ نقرة.

### د٥ · العربية مكتوبةٌ في الشيفرة
كلُّ رسالةٍ للمستخدم نصٌّ عربيٌّ داخل الدوالّ. لا i18n. والتوسّعُ لغةً
ثانيةً يعني لمسَ كلِّ ملفّ.

### د٦ · `falah-agent.html` = ١٫١٤ م.ب
سطرٌ واحد فيه **٧٤٥ ك.ب** من JSON مضمَّن (`var DB = {…}`) — مادّةُ العرض
كلُّها في الصفحة لتعمل بلا اتصال. مقصودٌ في أصله، ثقيلٌ في أثره.

### د٧ · خطوط Plex: امتدادٌ يكذب
`fonts/plex*.woff2` **ملفّاتُ TrueType** لا WOFF2 (التوقيع `\x00\x01\x00\x00`
لا `wOF2`)، وتُقدَّم بنوع `font/woff2` ويُعلَن `format('woff2')` في CSS.
كروميوم يقبلها بالشمّ — **قِسناه، والخطُّ يُحمَّل فعلًا** — لكنّ متصفّحًا
أشدَّ يرفضها. و٦٧٦ ك.ب بدل ~٢٠٠ ك.ب لو كانت WOFF2 حقًّا.

### د٨ · لا مراقبةَ مهيكلة
`log_error` سطرٌ على stderr. لا مقاييس، ولا تتبّعَ أخطاء، ولا زمنَ عمليات.
بعد النشر لن نعرف أن شيئًا يتباطأ إلا من شكوى.

---

# ٥ · نقاط الاختناق — ما ينكسر أوّلًا حين يكبر الحمل

| النقطة | الحدُّ اليوم | ما يكشفه |
|---|---|---|
| **عاملٌ واحد** | ٣٫٢ث/بطاقة ⇒ ~١٬١٠٠ بطاقة/ساعة | طابورٌ يطول عند ٤٠ مشتركًا نشطًا |
| **SQLite كتابةً** | كاتبٌ واحد في اللحظة | لم يُقَس تحت حملٍ مستدام — **مجهولٌ لا مطمئنٌّ له** |
| **`/health`** | ٦٢ م.ث | يُستدعى من الواجهة؛ لو صار في فحصٍ دوريّ أثقل |
| **التخزين** | ٢٬٠٤٨ م.ب/مستخدم | لا كنسَ تلقائيًّا للصادرات القديمة |
| **الذاكرة** | Chromium لكل تصيير | ٤ متزامنة × ~٣٠٠ م.ب — لم يُقَس تحت ضغط |
| **`falah.db`** | ٢٥٧ م.ب في الصورة | كلُّ نشرةٍ تحمل القاعدة كاملة |

---

# ٦ · مخاطر التوسّع

1. **العامل الواحد** أوّلُ ما يختنق. الحلّ موجودٌ ومجرَّب: `--scale worker=3`.
   لم يُقَس بأكثر من واحد.
2. **SQLite** يكفي آلافًا لا ملايين. الانتقالُ إلى Postgres قرارٌ كبير
   **لا يُتّخذ إلا بقياسٍ يوجبه** — ولم يُقَس بعد.
3. **الصادرات تتراكم** بلا كنس. عشرة آلاف مستخدمٍ × ٢ ج.ب = عشرون تيرابايت.
4. **قاعدة المحتوى في الصورة** تجعل كلَّ نشرةٍ ٣٠٠ م.ب.

---

# ٧ · القرار الثلاثيّ

## ✅ ما لا يُمَسّ — SECURITY CONTRACT

هذه **عقدٌ مغلق**. أيُّ تغييرٍ يمسّها يُوقَف حتى يُراجَع صراحةً:

| البند | الحارس |
|---|---|
| `verify.py` سلطةَ الحكم الوحيدة | `tests.py` · لا نسخَ لمنطقها في الطابور |
| SOURCE_LOCK — لا نصَّ يُولَّد ولا يُعدَّل | ٢٥ فحصًا · بصمة sha256 |
| عزلُ المستخدمين والمشاريع والمهامّ والصادرات | `isolation_test.py` — ٧٤ بندًا |
| منعُ حقن CSS/XSS (`safe_color`) | `tests.py` — ٦ حمولاتٍ عدائية |
| منعُ الخروج من المسار (نقاطٌ وترميزٌ ووصلات) | `isolation_test.py` — ١٧ مسارًا |
| SSRF (مخطَّطُ التلاوة · حجمُها) | `tests.py` — ٥ مخطَّطات |
| سلامةُ الحصّة (لا خصمَ مضاعف ولا ضائع) | `failure_test.py` — ٨ سيناريوهات |
| التفرّد عند التزامن | `failure_test.py` — ١٠ ← ١٠٠ |
| آلةُ حالات المهمّة | `tests.py` — كلُّ انتقالٍ مشروعٍ وممنوع |
| حدُّ المعدّل (يعدّ العملَ لا الطلب) | `isolation_test.py` |
| منعُ تسريب الأخطاء وبصمةِ الخادم | `leak_test.py` — ٥٤ بندًا |
| حدُّ حجم الطلب (٤١٣) | `isolation_test.py` |
| حارسُ الهجرات الهادمة | `tests.py` · `dbsafe.py` |

**الخطُّ الأساس: ٩٢٩ بندًا (٥٢٩ + ١٢٤ + ٥٣ + ٩٥ + ٥٤ + ٧٤). لا ينزل.**

## 🔧 ما يُحسَّن

| | لماذا |
|---|---|
| تفكيكُ التوجيه (د١ · د٢) | ليصير فحصُ الملكية والحصّة **بنيويًّا لا تذكُّريًّا** |
| الأدوار وسجلُّ التدقيق (د٣) | المفتاحُ المشترك لا يقول مَن فعل |
| المراقبة المهيكلة (د٨) | لنعرف قبل الشكوى |
| CSP · HSTS | ثغراتٌ مفتوحة |
| الحذفُ المنطقيّ (د٤) | استرجاعُ خطأِ النقرة |
| كنسُ الصادرات | التخزينُ يتراكم بلا حدّ |
| خطوط Plex (د٧) | امتدادٌ يكذب · ٤٧٦ ك.ب زائدة |

## ⛔ ما لا يُعاد كتابته

- **`falah.db` وبناؤها** — تعمل، ومقيسة، ولا شكوى منها.
- **`verify.py`** — أنضجُ ما في المشروع.
- **معمارية الطابور** — مجرَّبةٌ تحت الكسر المتعمَّد.
- **SQLite** — لا يُهجَر بلا قياسٍ يوجبه.
- **`api.py` منطقًا** — يُفكَّك توجيهُه لا يُعاد بناءُ حكمه.

---

# ٨ · التكاملات الخارجية

| | الحال |
|---|---|
| آبل (App Store Server API) | مكتوبٌ ومُختبَرٌ بسلسلةِ شهاداتٍ مولَّدة · **لم يُجرَّب على متجرٍ حقيقيّ** |
| جوجل (Play Developer API) | مكتوب · **لم يُجرَّب** |
| البريد (SMTP) | مكتوب · **لم يُجرَّب بخادمٍ حقيقيّ** |
| مصادر النصوص | تُنزَّل مرّةً وتُبنى · لا اتصالَ وقت الخدمة |
| التلاوات | تُجلب وقت التصيير — **مخطَّطٌ مشروطٌ وحجمٌ محدود** |
