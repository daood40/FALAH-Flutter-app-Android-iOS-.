# P0 — حالة الإطلاق

**الحالة: `HARDENED / P1-BLOCKED`**
آخر تحقّق: ٢٠٢٦-٠٩-٠٣ · `make gate` → PASS 14 · FAIL 0 · BLOCKED 1

> قاعدةُ هذه الوثيقة: **لا تُكتب PASS لشيءٍ لم يُشغَّل.** وما لم يُشغَّل
> يُكتب BLOCKED ويُقال لماذا. والأرقام مقيسةٌ من تشغيلٍ فعليّ لا مقدَّرة.

---

## PROVEN — شُغّل وقيس

| المجال | الدليل | العدد |
|---|---|---:|
| Tests | `make test` — وحدةٌ وتكامل | **٤٩٨** ✓ |
| Audit | `make audit` — عبر الشبكة على خادمٍ يعمل | **١٢٤** ✓ |
| UI | `make ui` — كروميوم حقيقيّ | **٥٣** ✓ |
| Failure Injection | `make failure` — قتلُ عاملٍ وإطفاءُ خادمٍ ونزعُ قاعدة | **٩٥** ✓ |
| Leak | `make leak` — كلُّ رمز حالةٍ يُستدرج ويُفتَّش | **٥٤** ✓ |
| **Isolation** | `make isolation` — أ ← موردُ ب، ومساراتٌ وحدود | **٧٤** ✓ |
| Security | `make security` — نطاقُه موثَّقٌ في `security_scan.py` | ٠ critical · ٠ high |
| Dependencies | `make deps` — `pip-audit` على `requirements.txt` | **٠ ثغرة** |
| Queue | حالاتٌ خمس · انتقالاتٌ محروسة · تراجعٌ أُسّيّ · سقفُ زمن | ✓ |
| Worker | الوضعان · نبضةٌ على القرص · قتلٌ واستئنافٌ بلا ضياع | ✓ |
| Idempotency | ١٠·٢٥·٥٠·١٠٠ متزامنًا → مهمّةٌ واحدة، صفرُ ٤٠٠ | ✓ |
| Quota | ثمانيةُ مساراتٍ مالية: لا خصمَ مضاعف ولا ضائع | ✓ |
| Database | `integrity_check` · `foreign_key_check` · فهارس موحَّدة | ✓ |
| Backup | نسخةٌ حيّة موقَّعةٌ ببصمتها وأعدادها | ✓ |
| Recovery | فُكّت · بصمةٌ مطابقة · **التطبيق أقلع عليها وخدم طلبًا** | ✓ |
| API Contract | ٦٠ مسارًا · صفرُ انحراف · لا نداءَ شبح | ✓ |
| Lint | `ruff` — استثناءاتٌ ضيّقةٌ موثَّقةٌ في `pyproject.toml` | ✓ |
| Types | `mypy` — ٤٨ ملفًّا، منها `build.py` | ✓ |

**المجموع: ٨٩٨ بندًا مشغَّلًا.**

---

## BLOCKED — لم يُشغَّل، ولا يُحسب نجاحًا

| المجال | السبب | ما يفكّه |
|---|---|---|
| **Docker Build** | عفريت docker غير مشتغلٍ في هذه البيئة | بيئةٌ فيها docker يعمل → `make docker` |
| **GitHub Actions** | لا مستودعَ بعيد | ربطُ المستودع → دفعةٌ واحدة |

`deploy/docker-compose.yml` و`Dockerfile` **مُراجَعان ومتحقَّقٌ من إعدادهما**
(`docker compose config` يمرّ، وخدمتان، وحجمان مشتركان، وفحصٌ لكلٍّ منهما)
— لكن **الصورة لم تُبنَ قطّ**. وهذا فرقٌ لا يُطوى.

و`.github/workflows/ci.yml` مكتوبٌ بالكامل، ومضمونُه كلُّه شُغّل محلّيًّا
بالأوامر نفسها عبر `make gate`. لكنه **لم يدُر في GitHub Actions**.

---

## READY BUT NOT APPLIED

### الهجرة `003_jobs_state_guard`

**جاهزةٌ ومُثبَتة، وغيرُ مطبَّقةٍ على قاعدة الإنتاج.**

| ما أُثبت | كيف |
|---|---|
| لا صفَّ يضيع | نسخةٌ من القاعدة الحقيقية، ٥٧ صفَّ مهمّةٍ في **الحالات الخمس** — نجت حرفًا بحرف |
| التكامل سليمٌ بعدها | `integrity_check` · `foreign_key_check` |
| الفهارس تعود كاملةً | الثلاثة، بتعريفاتها |
| القيود تمنع فعلًا | إدراجُ حالةٍ مجهولة وتقدّمٍ خارج ٠–١٠٠ يُرفضان |
| لا تتكرّر | إعادةُ التشغيل لا تُغيّر شيئًا |
| الاسترجاع يعمل | نسخةٌ فُكّت وأقلع التطبيق عليها |

**المسار الوحيد المسموح:**

```
make db-safe-migrate      # فحص ← نسخة ← اختبارُ استرجاع ← بروفة ← حارس
```

ثم — وبقرارٍ يدويٍّ منك وحدك:

```
FALAH_ALLOW_DESTRUCTIVE=1 python3 -m falah.migrate
```

**ولا يُتخطّى الحارس.** في `FALAH_ENV=production` يقف `falah/migrate.py`
نفسه — لا أداةٌ حوله — ما لم توجد نسخةٌ احتياطية **استُرجعت وتُحقّق منها**
(بيانُها يحمل `verified_at`). وحتى `FALAH_ALLOW_DESTRUCTIVE=1` لا يتخطّاه.

النظام يعمل بدونها: آلةُ الحالات مفروضةٌ في `falah/jobs.py`، والقيدُ حمايةٌ ثانية.

---

## DEFERRED — مؤجَّلٌ بقرار

- **كارثةٌ حقيقية**: عطبُ قرصٍ فعليّ أو فقدُ خادم. جُرّب الاسترجاع على
  قاعدةٍ محلّية، لا على انهيارٍ حقيقيّ.
- **حِملٌ مستدام**: قيست ذروةٌ لحظية (١٠٠ متزامنًا في ٣٧٨ م.ث)، لا ساعةٌ متصلة.
- **نسخٌ خارج الجهاز**: النسخ الاحتياطية على القرص نفسه. لا نسخةَ خارجية.
- **مراجعةٌ بشرية مستقلّة · اختبارُ اختراق · فحصُ صورة الحاوية** (trivy وأمثاله).
- **ترخيص خطوط IBM Plex** غير مشحونٍ معها (`fonts/OFL-Amiri.txt` موجود).

---

## الحدود المفروضة — كلُّها في الخادم لا في الواجهة

| الحدّ | القيمة | المتغيّر |
|---|---|---|
| جسم الطلب | ١ م.ب → **٤١٣** | `FALAH_MAX_BODY` |
| تصديرٌ في الساعة | ٣٠ → **٤٢٩** | `FALAH_RATE_EXPORT` |
| مقطعٌ في الساعة | ١٥ | `FALAH_RATE_VIDEO` |
| تسجيلٌ في الساعة (لكل عنوان) | ٢٠ | `FALAH_RATE_REGISTER` |
| مهامٌّ منتظرةٌ لكل مستخدم | ٥ | `FALAH_MAX_QUEUED_PER_USER` |
| جارٍ معًا | ٤ | `FALAH_MAX_RUNNING` |
| بطاقاتٌ في المهمّة | ٦٠ | `FALAH_MAX_CARDS_PER_JOB` |
| ثوانيَ المقطع | ١٨٠ | `FALAH_MAX_VIDEO_SECONDS` |
| تخزينٌ لكل مستخدم | ٢٠٤٨ م.ب | `FALAH_MAX_STORAGE_MB_PER_USER` |
| زمنُ المهمّة | ٦٠٠ث ثم تُقطع | `FALAH_JOB_TIMEOUT` |
| محاولاتُ الإعادة | ٢ بتراجعٍ أُسّيّ | `FALAH_MAX_RETRIES` |
| محاولاتُ الدخول | ٨ في ربع ساعة | — |
| حجمُ التلاوة | ٢٥ م.ب | `FALAH_MAX_AUDIO_BYTES` |

**والحدُّ يُعدّ العملَ المقبول لا الطلب**: مئةُ نقرةٍ على الزرّ نفسه مهمّةٌ
واحدة فتُحسب واحدة — فلا يُعاقَب تزامنٌ مشروع.

---

## بعد أن تجهّز البيئة الخارجية

لا يُعاد P0 من أوّله. تُنفَّذ أربعةٌ فقط:

```
1. docker build .                     ← يفكّ Docker Build
2. docker compose up + دخانٌ عليه      ← يفكّ Docker Smoke
3. دفعةٌ إلى المستودع                  ← يفكّ GitHub Actions
4. make db-safe-migrate ثم قرارُك      ← يفكّ Migration 003
```

وما عدا ذلك مُثبَتٌ ومحفوظٌ في `make gate`.
