# فَلاح — التدقيق والخطط

> كل رقمٍ هنا مقيسٌ بـ`python3 discovery.py`، وليس وصفًا. والحكم على ما
> وُجد لا على ما يُظنّ.

---

# ١ · PROJECT AUDIT

## ما هو المشروع فعلًا

نظامٌ لصناعة محتوًى بصريّ من نصوصٍ إسلامية موثَّقة. **١٢٬٣٢١ سطرًا** في
**٦٦ ملفًا** (بايثون ٧٬٨١٩ · واجهات ٢٬٥٨١). طبقتان في خادمٍ واحد: طبقة
محتوًى تقرأ من `falah.db`، وطبقة تطبيقٍ تكتب في `app.db`.

## ما يعمل — مُختبَرًا لا مُدَّعًى

| | القياس |
|---|---|
| اختبارات الوحدة | **٢٦٧ بندًا، كلها تمرّ** |
| فحص التنفيذ | **١٠١ بندًا** (قاعدة · مسارات · أمان · وكيل · تصيير · فيديو · أداء) |
| فحص المتصفّح | **٤٤ بندًا** (فرعان · رجوع · سلسلة · اتجاه · وصول) |
| المسارات | ٢٠ محتوًى · ١١ قراءة · ٢٤ كتابة |
| الاعتماديات الخارجية | **٤ فقط**: playwright · cryptography · imageio_ffmpeg · PIL |
| النصوص المفحوصة | ٦٬٢٣٤ آية · ١٢٬٨٥٦ حديثًا · ١٩٬٥١٧ قالبًا |
| الحماية | CSRF على كل كتابة ✓ · كعكة HttpOnly+SameSite ✓ · PBKDF2 ✓ · رمز جلسة مجزَّأ ✓ |
| النسخ الاحتياطي | `deploy/backup.sh` **واختبارٌ يستعيد منه فعلًا** ✓ |
| PWA وقشرة أصلية | ✓ |

## ما لا يعمل — مرتّبًا بالخطر

### P0 — يكسر الإنتاج

**١. التصيير داخل دورة الطلب.** قياسٌ حقيقي: **٣٫٢ ثانية للبطاقة الواحدة**،
و**٢٦ ثانية للمقطع**. سلسلة من خمس بطاقات ≈ **١٦ ثانية داخل طلبٍ واحد**.
خلف nginx (٦٠ث) أو Cloudflare (١٠٠ث) سيسقط الطلب، وكل تصييرٍ يحجز خيطًا
من `ThreadingHTTPServer`. **عشرة مستخدمين معًا = خادمٌ متوقّف.**

**٢. لا CI/CD ولا lint ولا type check.** لا شيء يمنع دفع شفرةٍ مكسورة.

**٣. لا مراقبة ولا تتبّع أخطاء.** بعد النشر لن نعرف أن شيئًا سقط إلا من
شكوى مستخدم.

**٤. المشروع ليس مستودع git.** لا تاريخ، ولا رجوع، ولا مراجعة.

### P1 — أمان

| النقص | الأثر |
|---|---|
| لا Content-Security-Policy | XSS مخزَّن يصبح قابلًا للاستغلال |
| لا Strict-Transport-Security | هجوم خفض البروتوكول |
| **حدُّ المعدّل على الدخول والاستعادة فقط** | يُقصف `/app/export` (٣٫٢ث لكل نداء) حتى يسقط الخادم |
| لا أدوار (Roles) | الإدارة بمفتاحٍ مشترك واحد، لا أدوار ولا سجلّ من فعل |
| لا CORS في طبقة التطبيق | القشرة الأصلية عبر النطاقات لن تعمل، أو ستُفتح على مصراعيها |

### P2 — منتج وتشغيل

لا حذف منطقي (١٣ جدولًا من ١٣) · لا لوحة إدارة · لا i18n (العربية مكتوبة
في الشفرة) · لا إشعارات · لا بيئة staging.

## ما فُحص وتبيّن أنه سليم — ولا يُعدّ ثغرة

- **ثلاثة استعلاماتٍ مبنيّة بدمجٍ نصّي** (`projects.update`,
  `auth.delete_account`): أسماء الأعمدة والجداول من **قوائم بيضاء مكتوبة
  في الشفرة**، والقيم كلها مُمرَّرة بمعاملات. لا حقن.
- **`tests.py` فيها «BEGIN PRIVATE KEY»**: نصُّ اختبارٍ يتحقّق أن لا مفتاح
  في الشفرة — لا مفتاح فيها.
- **جداول FTS بلا مفتاح أساسي**: هذا بناء SQLite FTS5 نفسه.

---

# ٢ · ARCHITECTURE PLAN

## القائم اليوم

```
المتصفّح / القشرة الأصلية
        ↓
   app.py  ── جلسة · CSRF · حصص · مشاريع
        ↓ (استدعاء داخل العملية، بلا شبكة)
   api.py  ── بطاقات · بحث · خيارات
        ↓
falah/verify.py ── الفحوص الـ٢٥ — **سلطة الحكم الوحيدة**
        ↓
   falah.db (قراءة فقط)     app.db (كتابة)
```

الفصل سليم ولا يُهدم. الخلل في مكانٍ واحد: **التصيير يقع في الخيط نفسه.**

## المستهدف

```
        app.py ── يستقبل، يتحقّق، يضع مهمّة، يردّ فورًا (٢٠٢)
                      ↓
              jobs (طابور في app.db)
                      ↓
        worker.py ── عمليةٌ منفصلة تُصيّر وتُحدّث الحالة
                      ↓
        المتصفّح يستطلع /app/jobs/<id> حتى تكتمل
```

**لماذا طابورٌ في SQLite لا Redis؟** لأن المشروع كلّه بلا اعتماديات
خارجية عمدًا، والحِمل المتوقّع (آلاف لا ملايين) يحتمله SQLite بـWAL
و`BEGIN IMMEDIATE`. وإضافة Redis تضيف خدمةً تُراقَب وتُنسخ وتسقط. تُضاف
حين يثبت القياس أنها لازمة، لا قبله.

**ما لا يتغيّر:** `verify.py` تبقى سلطة الحكم الوحيدة، ولا يُنسخ منطقها
إلى العامل.

---

# ٣ · DATABASE PLAN

## القائم

| | falah.db | app.db |
|---|---|---|
| الحجم | ٢٦٩٫٢ م.ب | ٠٫٢ م.ب |
| الجداول | ٣٢ | ١٣ |
| بلا مفتاح أساسي | ٣ (جداول FTS — طبيعي) | صفر ✓ |
| بلا فهرس | ٦ (منها FTS) | صفر ✓ |
| بلا حذفٍ منطقي | ٣٢ | ١٣ |

## التغييرات

1. **جدول `jobs`** — الطابور: `id, user_id, kind, payload, state, attempts, error, created_at, started_at, finished_at, result`. مع فهرسٍ على `(state, created_at)`.
2. **حذفٌ منطقي** على `projects` و`project_items` و`exports`: `deleted_at`. لا يُطبَّق على `users` — حذف الحساب يجب أن يمحو فعلًا (وعدٌ قطعناه في الواجهة).
3. **`role`** على `users`: `user | moderator | admin`.
4. **هجرات مرقَّمة** بدل `CREATE TABLE IF NOT EXISTS` وحده — الجدول القائم لا يتغيّر بتعديل السكيما.

**قرارٌ خطِر يحتاج إذنك:** أي هجرةٍ تحذف عمودًا أو جدولًا. لن أنفّذ شيئًا
من ذلك بلا موافقةٍ صريحة. المخطَّط أعلاه **إضافةٌ فقط**.

---

# ٤ · SECURITY PLAN

| # | العمل | الحال |
|---|---|---|
| ١ | CSP صارمة + HSTS + `Permissions-Policy` | ناقص |
| ٢ | حدُّ معدّل على كل مسارٍ مكلف (`export`, `video`, `agent`) بحصّةٍ لكل مستخدم ولكل IP | ناقص |
| ٣ | أدوار `user/moderator/admin` + تحقّقٌ في الخادم لا في الواجهة | ناقص |
| ٤ | CORS بقائمةٍ بيضاء للقشرة الأصلية | ناقص |
| ٥ | سجلّ تدقيق للعمليات الحسّاسة: من · ماذا · متى · النتيجة | جزئي (`events`) |
| ٦ | فحص أمنيّ آليّ في CI | ناقص |
| ٧ | تدوير `FALAH_ADMIN_KEY` وربطه بدورٍ لا بمفتاحٍ مشترك | ناقص |

**ما هو قائمٌ ومختبَر:** CSRF بطبقتين · عزل المستخدمين (٣ اختبارات) ·
قفل المصدر · حظر الشراء غير المحقَّق · لا تسريب أثر تنفيذ.

---

# ٥ · AI PLAN

**القاعدة الحاكمة، وهي منفَّذة اليوم لا مخطَّطة:**

```
SOURCE_LOCK = TRUE
```

الذكاء الاصطناعي في فَلاح **لا يكتب نصًّا شرعيًّا ولا يُعدّله**. دوره
الوحيد الاختيار: أيّ سورة، أيّ آية، أيّ باب، أيّ رقم. ثم يذهب إلى
القاعدة فيأخذ ما اجتاز الفحوص الخمسة والعشرين.

| الضمانة | كيف تُنفَّذ |
|---|---|
| بصمة sha256 لكل نصّ | أي تغيير حرفٍ أو حركة يكسرها — واختبارٌ يُثبته |
| مصدران مستقلان للقرآن | تنزيل + Quran.com؛ المختلَف فيه يُحجب |
| لا حكم بلا قائل | الدرجة ومن حكم بها وأصل نصّه |
| المحجوب يُذكر سببه | ولا يُستبدل بشيءٍ مولَّد |
| قفل المصدر في المشروع | العنصر يُخزَّن بموضعه وبصمته؛ الانحراف يمنع التصدير |

**ما ينقص:** لا شيء في هذه الطبقة. وهي أنضج ما في المشروع.

---

# ٦ · TESTING PLAN

| الطبقة | الحال | الناقص |
|---|---|---|
| Unit | ٢٦٧ بندًا ✓ | — |
| Integration + API | `audit.py` ١٠١ بندًا ✓ | — |
| Security | عزل · CSRF · شراء مزوَّر ✓ | حدُّ المعدّل · الأدوار |
| UI / E2E | `ui_audit.py` ٤٤ بندًا ✓ | مسار الاشتراك والإحالة |
| Performance | زمن الاستجابة · ٣٠ طلبًا متوازيًا ✓ | حِملٌ حقيقيّ على التصدير |
| Regression | تُشغَّل الحزم الثلاث بعد كل تغيير ✓ | آليًّا في CI |

**يُضاف:** اختبار الطابور (مهمّة تُوضع → تُنفَّذ → تُسلَّم)، واختبار
أن تصديرين متزامنين لا يتداخلان.

---

# ٧ · DEPLOYMENT PLAN

```
git push
   ↓ lint (ruff)
   ↓ type check
   ↓ tests.py + audit.py + ui_audit.py
   ↓ فحص أسرار
   ↓ بناء الصورة
   ↓ staging → دخانٌ آليّ
   ↓ production (بيدك، لا آليًّا)
```

بيئات: `development` · `staging` · `production`. **الإنتاج ليس مختبرًا.**

القائم: `Dockerfile` ✓ · `docker-compose.yml` ✓ · `backup.sh` **مع اختبار
استعادةٍ فعليّ** ✓.
الناقص: CI · staging · مراقبة · تنبيهات.

---

# ٨ · IMPLEMENTATION ROADMAP

| المرحلة | العمل | لماذا الآن |
|---|---|---|
| ~~**P0-1**~~ ✅ | ~~طابور مهامّ + عامل~~ **منجَزٌ ومتحقَّقٌ منه** | كان ٣٫٢ث للبطاقة و٢٦ث للمقطع داخل الطلب · صار الردّ ٤ م.ث |
| ~~**P0-2**~~ ✅ | ~~git + CI~~ **منجَزٌ ومتحقَّقٌ منه** | بوّابةُ جودةٍ · كسرٌ متعمَّد · هجراتٌ محروسة · ٥ أعطابٍ كُشفت |
| **P0-3** | مراقبة: تسجيل مهيكل · تتبّع أخطاء · `/healthz` | بلا ذلك نحن عميان بعد النشر |
| **P1-1** | CSP · HSTS · حدّ معدّل على المكلف | ثغراتٌ مفتوحة |
| **P1-2** | أدوار + سجلّ تدقيق كامل | الإدارة بمفتاحٍ مشترك اليوم |
| **P1-3** | CORS للقشرة الأصلية | القشرة لن تعمل بدونه |
| **P2-1** | حذف منطقي + هجرات مرقَّمة | استرجاع الخطأ |
| **P2-2** | لوحة إدارة | مراجعة · حجب · منح · طابور |
| **P2-3** | i18n | التوسّع لغويًّا |

**القاعدة في كل مرحلة:** `PLAN → IMPLEMENT → TEST → FIX → VERIFY`، ولا
تُعدّ مرحلةٌ مكتملةً قبل أن تمرّ الحزم الثلاث كاملةً.

**ما لن أفعله بلا إذنك:** هجرةٌ تحذف بيانات · تغيير معماريّ جذري · نشرٌ
إلى الإنتاج · أي شيء يكلّف مالًا.

---

# ملحق · محضر إنجاز P0-1 (طابور المهامّ)

## ما بُني

| الملفّ | الدور |
|---|---|
| `falah/store.py` | جدول `jobs` — **إضافةٌ محضة** (`CREATE TABLE IF NOT EXISTS`)، لا هجرة هادمة، لا إذنَ لازم |
| `falah/jobs.py` | الطابور: `enqueue · claim · beat · complete · fail · reap · sweep · perform · run_once` |
| `worker.py` | عمليةٌ مستقلّة: `--once · --drain · --status · --workers N` |
| `app.py` | `/app/export` و`/app/video` → **٢٠٢**؛ `/app/jobs` و`/app/jobs/<id>` للاستطلاع؛ عاملٌ داخليّ اختياريّ |
| `falah-app.html` | `waitJob()` — استطلاعٌ متمهّل (٤٠٠م.ث ← ٣ث) مع خطوةٍ عربيةٍ ونسبةٍ ظاهرة |
| `falah/billing.py` | `release()` — ردّ الحصّة المحجوزة عند الفشل النهائيّ |
| `deploy/` + `Dockerfile` | `worker` خدمةٌ مستقلّة · حجم `falah-exports` مشترك |

## القياس — قبل وبعد

| | قبل | بعد |
|---|---|---|
| ردّ `/app/export` | ٣٫٢ث × عدد البطاقات | **٤ م.ث** |
| ردّ `/app/video` | ≈٢٦ث | **٤ م.ث** |
| عشرة تصديرات معًا | ≈٣٢ث حبسًا | **٤٢ م.ث** |
| `/health` أثناء التصيير | يتباطأ | **٥٣ م.ث** |
| إغلاق التبويب | يضيع العمل | يكمل، ويُوجد عند العودة |

## قراراتٌ اتُّخذت وسببها

- **حجز الحصّة عند الوضع لا بعد التصيير.** لو بقي الفحصُ وحده لأغرق حسابٌ
  مجانيّ الطابورَ بمئة مهمّة قبل أن يُصيَّر منه شيء. والمبدأ محفوظ: تُردّ
  كاملةً عند الفشل النهائيّ (`billing.release`) — فلا يُحاسَب أحدٌ على ما لم يخرج.
- **أخطاء المستخدم لا تُعاد محاولتها.** «المشروع فارغ» يبقى فارغًا مهما كُرِّر.
  ما يُعاد هو العطب العابر فقط، ومرّتين لا أكثر.
- **العامل يفتح المشروع بـ`open_project` فيُعاد الفحص وقت التصيير** — أدقّ من
  الفحص وقت الطلب، إذ قد يمرّ بينهما وقت. و`verify.py` تبقى السلطة الوحيدة:
  لا سطرَ من منطقها في `jobs.py`، والاختبار يحرس ذلك.
- **الرقم لا يكشف عمل غيره:** كل قراءةٍ مقيَّدةٌ بـ`user_id`.

## عطبان حقيقيّان كشفهما الفحص أثناء المرحلة

1. **خمسة مسارات قراءةٍ موجودةٌ في `api.py` ولا يمرّرها `app.py`** —
   `/options` `/chapters` `/chapter/hadiths` `/templates` `/template`.
   كانت ترجع ٤٠٤ من خادم التطبيق وحده. أُصلحت، ووُضع اختبارٌ يقارن قائمة
   `CONTENT_PATHS` بما في `api.py` فلا تنفتح الثغرة صامتةً مرّةً أخرى.
2. **صورة الحاوية تنقصها ملفّاتٌ يقدّمها الخادم** — `manifest.webmanifest`
   و`sw.js` و`icons/`. أي أن تطبيق الويب التقدّميّ كان سيتعطّل في الإنتاج
   وحده دون أن يظهر شيءٌ في التطوير. أُضيفت، ومعها اختبارها.

## التحقّق

| الحزمة | قبل المرحلة | بعدها |
|---|---|---|
| `tests.py` | ٢٦٧ ✓ | **٣٠٤ ✓** |
| `audit.py` | ١١٧ ✓ (و٧ ساقطة) | **١٢٤ ✓** |
| `ui_audit.py` | ٤٤ ✓ | **٥٣ ✓** |

وجُرِّب الوضعان معًا: عاملٌ داخل الخادم (`python3 app.py`)، وعاملٌ منفصل
(`FALAH_INLINE_WORKER=0` + `python3 worker.py`) — كلاهما يُسلّم النتيجة.


---

# ملحق · محضر إنجاز P0-2 (الأساس الهندسيّ)

## أعطابٌ حقيقية كُشفت في هذه المرحلة

### ١ · نصُّ الاستثناء يُرسل إلى المستخدم
`{"project": "أبجد"}` كان يردّ **٥٠٠** ونصَّه `ValueError: invalid literal
for int()...`. نصُّ الاستثناء يصف بنيةَ الشيفرة، وهو نصفُ خريطةٍ لمن يبحث
عن ثغرة. صار **٤٠٠** ورسالةً عربية، والتفصيل يُسجَّل عندنا.

**وتبعَه عطبٌ ثانٍ:** كتبتُ «يُسجَّل عندنا» ثم اكتشفت أن `log_error`
يمرّ عبر `log_message` وقد أُسكت — فكانت الدعوى بلا سجلّ. كُتب مسجّلٌ حقيقيّ.

### ٢ · سباقٌ في منع التكرار (كشفه الكسر المتعمَّد وحده)
ستُّ نقراتٍ متزامنة على «صدِّر»: مهمّةٌ واحدة وحصّةٌ واحدة — صحيح. لكن
اثنتين من الستّ رجعتا **٤٠٠**. السبب: سباقٌ بين فحص التكرار والإدراج،
والفهرسُ الفريد يحسمه بـ`IntegrityError` فيصير خطأً. الآن يُردّ الثاني
بالمهمّة القائمة و**٢٠٢**. لم يكشفه اختبارٌ تسلسليّ — كشفه التزامن المتعمَّد.

### ٣ · `zip` تقتطع في فحص تسلسل الآيات
```python
all(r["ayah"] == n for r, n in zip(rows, range(ayah, to+1)))
```
`zip` تقف عند الأقصر. مدًى ناقصُ آيةٍ (`rows` أقصر) كان يمرّ ويُقال عنه
«متسلسل». في **محرّك التحقّق** — أهمّ شيفرةٍ في المشروع. الآن يُقارن الطول
أوّلًا، و`strict=True` تمنع الاقتطاع الصامت. كشفه `ruff` لا اختبار.

### ٤ · `store.init()` يسقط على قاعدةٍ أقدم من عمودٍ جديد
`CREATE TABLE IF NOT EXISTS` يُنشئ المفقود ولا يمسّ الموجود، فقاعدةٌ
أُنشئت قبل `idem_key` بقيت بلا العمود، ثمّ سقط `CREATE INDEX` عليه. هذا
بالضبط ما وُجدت الهجرات له، وقد كشف نفسَه أوّلَ تشغيل.

### ٥ · فحص الحاوية يستدعي إحصاءَ القاعدة كل ٣٠ ثانية
`HEALTHCHECK` كان على `/health` — يفتح قاعدة المحتوى ويعدّ صفوفها. وهو
يخلط الحياةَ بالجاهزية: سقوطه يقتل الحاوية، وسببُه قد يكون قرصًا بطيئًا
لا عمليةً ميتة. صار `/healthz`، وأُضيف `/readyz` لمعناه الآخر.

## أخطاءُ اختبارٍ صحّحتُها — لا تُحسب إنجازًا وتُذكر للأمانة

ثلاثةُ «حقنِ عطبٍ» لم تحقن شيئًا، وكانت ستمرّ كذبًا:
- `chmod 000` على `falah.db` — العملية تعمل بصلاحية root فتتجاوز الصلاحيات.
  استُبدل بإزاحة الملفّ نفسه.
- `PLAYWRIGHT_BROWSERS_PATH=/nonexistent` — `render.py` يحدّد مسار المتصفّح
  بنفسه ويتراجع إلى الافتراضيّ. استُبدل بملفٍّ مكان مجلّد الصادرات.
- ثلاثُ مهامّ من الطلب نفسه — مستحيلٌ بالتصميم بعد مفتاح التفرّد. استُبدل
  بثلاثة حسابات.

**الدرس:** حقنٌ لا يَحقِن يجعل الاختبار يمرّ ويوهم بأمانٍ ليس موجودًا.

## قراراتٌ اتُّخذت وسببها

- **٧٢٩ من ٨٦٠ بلاغِ `ruff` أُسقطت بقرارٍ مكتوب** — `E701/E702` هي أسلوب
  هذا المشروع (`if not r: return None`) مطّردًا في ١٢ ألف سطر. إعادةُ
  تنسيقها فرقٌ هائل بلا فائدةٍ سلوكية. الباقي — أخطاءٌ حقيقية — أُصلح.
  والإسقاط في `pyproject.toml` بسببه مكتوبًا، لا مخفيًّا لتحسين رقم.
- **`mypy` على ما يعمل في الإنتاج فقط** (`app.py` · `worker.py` · `falah/`)
  — نظيفٌ تمامًا. و`build.py` أداةُ بناءٍ تُشغَّل خارج الخدمة، وتوسيعُ النطاق
  إليها مذكورٌ في المخاطر لا مُدَّعًى أنه تمّ.
- **`falah.db` و`raw/` خارج المستودع** — ٤٢٢ م.ب مولَّدةٌ ومنزَّلة. المستودع
  ٨٫٣ م.ب على ١٠١ ملف. وخطُّ التكامل يحفظ القاعدة في ذاكرةٍ مؤقّتة بمفتاحٍ
  يتغيّر بتغيّر شيفرة البناء.
- **مفتاح التفرّد = بصمةُ (صاحب + نوع + حمولة)** — لا رمزٌ يرسله العميل.
  العميل قد ينساه أو يزوّره؛ والبصمة تُشتقّ من الطلب نفسه فلا تحتاج ثقةً به.

## ما لم يُنجَز، وأقوله صراحةً

- **ثغرات الاعتماديات لم تُفحص.** لا `pip-audit` في هذه البيئة. الخطوة
  مكتوبةٌ في `requirements.txt` تنتظر شبكةً تصلها.
- **خطّ التكامل لم يُشغَّل على GitHub** — لا مستودعَ بعيد. شُغِّل مضمونُه
  كلُّه محلّيًّا بـ`make gate` بالأوامر نفسها.
- **`docker build` لم يُنفَّذ** — يتطلّب `raw/` (١٦٥ م.ب) داخل سياق البناء
  وشبكةً للتثبيت. تحقّقتُ من `docker compose config` ومن اتّساق الملفّات.
- **الهجرة ٠٠٣ موقوفةٌ في قاعدة التطوير** بانتظار إذنك — تفصيلها أدناه.
- **ترخيص خطوط IBM Plex غير مشحونٍ معها.** الأمر يخصّ `fonts/plex*.woff2`،
  و`fonts/OFL-Amiri.txt` موجود. بندٌ للتراخيص لا للهندسة.
